---
type: project-state
project: GAIP 本地静态预览版
updated: 2026-08-26
---

# 当前项目状态

> 所有 Codex 对话的短入口。只记录当前仍然有效的状态；历史细节进入 [[knowledge/变更/变更索引|变更索引]]。

## 当前基线

- 分支：`main`
- 最近已发布提交：`15231ab`（Add alternate AI Agent entry animation）
- 运行形态：本地静态预览，根目录 HTML 薄壳 + Umi Hash SPA + 本地 Mock
- 频道配置唯一来源：`shared/config/channels.js`
- 原始构建产物：`web/`，默认只读

## 正在进行

- [[knowledge/变更/当前未发布变更|当前未发布变更]]
- AI Agent 相关样式、入口动效、本地 Mock 和素材存在未提交修改；这些是建立知识库之前已有的用户工作，不得覆盖或擅自回退。
- Obsidian + 双链 + 索引工作流已经建立并通过结构检查，当前尚未提交。
- 财富值中心及其“导入工作台 / 导入记录 / 我的财富值”三个子频道已完成本地实现，等待随当前未发布变更一起发布。
- 客户中心列表卡片“已关联保单”标签已新增本地专属补丁，位置与样式已按参考站当前构建同步，等待随当前未发布变更一起发布。
- 全局组件目录已新增“折叠式多选下拉”共享组件，代码检查已通过，等待本地 `file://` 页面目视确认。

## 待验证

- AI Agent 当前未提交改动的交互与素材完整性。
- 财富值中心已通过语法、Hash 无刷新、跨入口、三个子频道、本地 Mock 交互以及主导航鼠标/键盘收起展开验证；钱包图标、上下箭头状态、箭头居中、父项 hover/选中态、父项文字列、父子项缩进、我的财富值港币图标和标题方块对齐已按参考站当前构建规则补正，提交前仍需复核最终 Git 差异。
- 客户中心“已关联保单”标签补丁已通过语法、Hash 导航、直接入口和跨入口浏览器验证；提交前仍需复核最终 Git 差异。
- 全局多选组件已通过 JavaScript 语法、资源接入和导航验证；浏览器自动化受 `file://` 安全策略限制，仍需在 `全局组件/index.html` 手动确认展开层、折叠计数和清空状态。

## 待发布

- 知识库工作流：`AGENTS.md`、`PROJECT_STATE.md`、`knowledge/`，已通过双链检查和本地导航验证。
- 财富值中心入口、频道资源、导航、本地 Mock、本地素材及共享虚拟频道识别更新。
- 登录页视频英雄图测试入口：新增 `index-login-video-test.html`、测试专属 CSS/JS 和项目内视频副本；原 `index.html`、`登录.html` 与 `web/` 构建产物未修改。
- 以 `git status` 和 `git diff` 的实际结果为准；本文件不自动代表可以提交。
- 发布前必须核对 [[knowledge/变更/当前未发布变更|当前未发布变更]] 与 Git 差异。

## 已知高风险区域

- [[knowledge/公共模块/主导航与Hash路由|主导航与 Hash 路由]]：不得改为整页跳转。
- [[knowledge/公共模块/频道资源加载|频道资源加载]]：频道改版资源必须从任意入口都可用。
- [[knowledge/公共模块/全局框架与样式|全局框架与样式]]：顶栏、侧栏、面包屑、字体和 AI 入口是跨频道能力。
- [[knowledge/公共模块/本地Mock系统|本地 Mock 系统]]：纯静态预览依赖本地数据拦截，不可当作无用数据删除。

## 下一步

1. 完成当前 AI Agent 改动的功能验证并补全变更记录。
2. 发布前复核财富值中心在 `file://` 和本地 HTTP 两种打开方式下的最终外观。
3. GitHub 发布对话读取本文件、当前未发布变更、`git status` 和 `git diff` 后再提交。

## 快速入口

- [[knowledge/INDEX|项目知识总索引]]
- [[knowledge/页面/页面索引|页面索引]]
- [[knowledge/公共模块/公共模块索引|公共模块索引]]
- [[knowledge/决策/决策索引|决策索引]]
- [[knowledge/变更/变更索引|变更索引]]
- [[docs/README|运行与目录说明]]
- [[AGENTS|AI 维护规则]]
