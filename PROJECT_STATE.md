---
type: project-state
project: GAIP 本地静态预览版
updated: 2026-09-02
---

# 当前项目状态

> 所有 Codex 对话的短入口。只记录当前仍然有效的状态；历史细节进入 [[knowledge/变更/变更索引|变更索引]]。

## 当前基线

- 分支：`main`
- 最近已发布功能提交：`ac68be7`（虚拟频道跨页面刷新路由修复；见 [[knowledge/变更/2026-09-02-虚拟频道刷新路由修复]]）
- GitHub Pages：`https://huangheping.github.io/gaip-style-preview/`
- 运行形态：根目录 HTML 薄壳 + Umi Hash SPA + 本地 Mock；“本地”默认指项目文件和既有 `file://` 预览。
- 主导航为项目级强制契约：新增频道前必须读取 [[knowledge/公共模块/主导航与Hash路由#主导航交互与样式契约|主导航交互与样式契约]]。
- 频道配置唯一来源：`shared/config/channels.js`。
- 原始构建产物：`web/`，默认只读。

## 正在进行

- 弹窗预览的源 `@gaip-modal` 自登记、自动生成索引和通用注册器调用已在本地完成；组织架构独立操作日志与批量导入成员流程均已进入预览。“全局组件目录”显示实时 30/0/17 状态，不在首页加载全部 iframe；独立弹窗预览页也已改为视口按需创建 iframe，配置中心使用无背景空宿主，正式频道弹窗打开后删除频道 DOM。跨任务强制规则、ADR、目录/DOM/导航回归均已补齐，尚未发布，详见 [[knowledge/变更/当前未发布变更#弹窗源自登记与自动索引|弹窗索引]]、[[knowledge/变更/当前未发布变更#弹窗预览按需加载与空宿主收尾|预览性能收尾]]。
- 配置中心成员表“身份”列已在本地完成，尚未发布；以紧凑标签展示人管、佣金、线索管理、线索跟进并复用现有角色状态，详见 [[knowledge/变更/当前未发布变更#配置中心成员身份列|当前未发布变更]]。
- 组织架构页右上角“操作日志”已恢复为独立的组织架构日志弹窗，和“配置中心 → 操作日志”二级频道的数据、控制器与状态完全分开；本地完成、尚未发布，详见 [[knowledge/变更/当前未发布变更#组织架构独立操作日志恢复|当前未发布变更]]。
- 组织架构独立日志表格已改为显式七列网格并统一多行内容对齐；弹窗已加宽，表格区恢复独立双向滚动、固定分页和不透明吸顶表头，滚动正文不再透到表头区域。“导出成员名单”展示状态已加入 Mock，并复用操作类型列统一标签色。真实多节点导出、Excel 节点列、三个新增渠道和 8 列字段模型均未实现，详见 [[knowledge/变更/当前未发布变更#组织日志“导出”操作状态|当前未发布变更]]。
- 组织架构顶部保持六大渠道 Tab、搜索、批量导入成员和独立操作日志的单行结构；“批量导入成员”已接入弹窗内三步流程。第一步使用渠道单选下拉与可搜索、可展开收起的目标节点树，并按紧凑版式把当前目标放在范围卡片右上、示例流程放在上传标题右侧；上传采用紧凑按钮与同行大小提示，选中文件后显示独立附件行，模板下载与格式限制合并至底部提示条。第二步以目标节点为主信息，将人数统计和失败处理说明合并为弱提示；18 行校验 Mock 在独立双向滚动的吸顶表头表格中展示。后续包含真实 Excel 模板、部分成功、失败明细下载、返回确认和结果页。三步统一使用 `840px` 外框高度，弹窗主体不滚动，长组织树和长表格只在各自区域内滚动，详见 [[knowledge/变更/当前未发布变更#组织架构批量导入成员弹窗流程|当前未发布变更]]。
- [[knowledge/变更/当前未发布变更|当前未发布变更]] 还保留弹窗预览触发修复、用户明确不发布的登录视频测试入口，以及未被正式页面引用的本地试验素材、财富背景候选图和资讯参考分包。

## 待验证

- 组织架构专属操作日志弹窗的原生 Esc、顶层遮罩与视觉仍需在既有 `file://` 本地预览手工确认；7 列字段、独立 Mock、分页、关闭、焦点恢复代码、与二级日志控制器隔离及生命周期已通过 DOM 回归。二级日志页自身的筛选与导出回归继续通过。
- AI Agent 登录后/节点重建入口恢复已通过代码测试；真实登录、跨频道和视频/WebGL 效果继续作为手工视觉验收项。

## 待发布

- 登录页视频英雄图测试入口：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`；用户明确不随本次发布。
- 其余未发布候选文件见 [[knowledge/变更/当前未发布变更|当前未发布变更]]；它们没有进入正式提交 `ac68be7`。

## 已知高风险区域

- [[knowledge/公共模块/主导航与Hash路由|主导航与 Hash 路由]]：不得改为整页跳转。
- [[knowledge/公共模块/频道资源加载|频道资源加载]]：频道改版资源必须从任意入口都可用。
- [[knowledge/公共模块/全局框架与样式|全局框架与样式]]：顶栏、侧栏、面包屑、字体和 AI 入口是跨频道能力。
- [[knowledge/公共模块/本地Mock系统|本地 Mock 系统]]：纯静态预览依赖本地数据拦截，不可当作无用数据删除。

## 下一步

1. 后续发布前继续核对本文件、[[knowledge/变更/当前未发布变更|当前未发布变更]]、`git status` 和 `git diff`。
2. 新增或修改可展开主导航时运行 `node scripts/test-expandable-main-nav.cjs` 与 `./scripts/verify-local-navigation.sh`。
3. 若用户确认需要，再单独发布登录视频测试入口或其他候选素材；未获确认不得误提交。

## 快速入口

- [[knowledge/INDEX|项目知识总索引]]
- [[knowledge/页面/页面索引|页面索引]]
- [[knowledge/公共模块/公共模块索引|公共模块索引]]
- [[knowledge/决策/决策索引|决策索引]]
- [[knowledge/变更/变更索引|变更索引]]
- [[docs/README|运行与目录说明]]
- [[AGENTS|AI 维护规则]]
