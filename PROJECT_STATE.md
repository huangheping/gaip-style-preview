---
type: project-state
project: GAIP 本地静态预览版
updated: 2026-09-01
---

# 当前项目状态

> 所有 Codex 对话的短入口。只记录当前仍然有效的状态；历史细节进入 [[knowledge/变更/变更索引|变更索引]]。

## 当前基线

- 分支：`main`
- 最近已发布功能提交：`5d597a4`（工作台图文对齐、操作日志控件修正，同时发布 AI 入口恢复 `ee5c3e9`；见 [[knowledge/变更/2026-08-31-工作台对齐与共享控件修复]]）
- 运行形态：本地静态预览，根目录 HTML 薄壳 + Umi Hash SPA + 本地 Mock
- 项目协作约定：“本地”默认指项目文件和既有 `file://` 预览；不得把 `127.0.0.1`/`localhost` 当成交付地址。必要的临时 HTTP 自动化验证须在结束后关闭服务和测试标签页。
- 主导航已固化为项目级强制契约：新增频道前必须读取 [[knowledge/公共模块/主导航与Hash路由#主导航交互与样式契约|主导航交互与样式契约]]，复用叶子导航/父项纯开合、父子当前态、统一对齐、原始图标、键盘与跨频道隔离规则，不得在频道内另写一套。
- 主导航滚动层已纳入同一契约：滚动条贴侧栏右缘且保持菜单坐标，默认颜色降为 14% 中性灰，并通过层级隔离避免被百宝箱上延底图遮挡。共享框架/区域脚本版本为 `20260901-7` / `20260901-1`，DOM 与实际渲染验证通过，尚未发布。
- 频道配置唯一来源：`shared/config/channels.js`
- 原始构建产物：`web/`，默认只读

## 正在进行

- 配置中心已在本地完成并纠正视觉重写：组织架构改用原站 DOM/CSS/图标，保留新增操作日志二级页面；并排视觉对照、两条无刷新导航路径与关键交互已验证，尚未发布。见 [[knowledge/页面/配置中心]]。
- 已补齐部门“⋮”菜单的本地增改删/管理员设置，并修复组织页日志按钮图标与文字对齐；功能回归和 HTTP 浏览器检查通过。file 页面受自动浏览器 URL 安全策略限制，仍需用户刷新后确认。
- 配置中心组织树默认只展示根渠道与一级部门；组织根节点本地 Mock 已补到 24 名成员，表格按每页 10 人分页。添加/编辑成员弹窗的管理员行固定为人管、佣金两个复选项；线索独立放在转介绍人下方，以“无/线索管理员/线索跟进人”单选，并已改为与转介绍人完全相同的三项等宽、内边距和左对齐方式，两处入口继续与“设置管理员”共用角色状态。成员弹窗标题关闭区和底部操作区固定，仅中间表单滚动。表格工具栏“设置管理员”按钮保持黑底白字并复用原图标。管理员弹窗按附件规则提供四个竖向 Tab，右侧头部仅保留规则和约束标签并以 `16px` 上下 padding 自适应高度；长成员列表只在右侧成员区滚动，左侧角色 Tab、规则栏和底部操作区固定。佣金 0/1、线索身份互斥、键盘切换、取消回滚和保存恢复均保留。配置中心操作日志右上角导出按钮使用绿底白字/白图标，并直接复用组织页导出按钮的 Ant 下载 SVG；日志表头统一为业务表格的浅灰底、深色 `14px` 文字。顶部频道增加时 Tab 保持完整且不滚动，搜索框负责收缩至 `120px`；空且未聚焦时隐藏清除图标，聚焦/输入后显示。配置中心外壳/内容/模板/JS 当前为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`、频道注册表缓存为 `20260901-48`，尚未发布。
- 配置中心成员搜索已按红框参考图移到顶部频道栏、“操作日志”左侧，最大 `333×72px` 并可缩至 `120px`；输入框只保留左右 `1px` 边框，上下无边框，默认底色为 `rgba(47,54,64,.03)`，hover、聚焦、输入和清空时边框/底色不变。清除图标初始隐藏，聚焦或有内容时显示，清空并失焦后隐藏。Tab 保持完整展示且不出现滚动条。操作按钮在下一排右对齐。页面取消 `1228px` 固定最小宽度并由表格承担内部横向滚动。表格标签只缩小为 `20px` 高/`12px` 字号，颜色与内容不变。1280×720 实测搜索框为 `285.32×72px`、与操作日志间距 `24px`、页面无自身横向溢出；当前资源版本同上，尚未发布。
- 配置中心 CSS 已完成职责分层：`config-center-content.css` 独立承载 202 条原站主体规则及组织树、工具栏、表格、弹窗和日志内容覆盖；`config-center.css` 只保留主导航、顶部区域与挂载骨架。未拆分的 `organization-source.css` 仅作对照快照，不再运行时加载。组织页和操作日志页实际渲染、样式边界回归及控制台检查通过，尚未发布。
- 配置中心已修复全局 AI Agent 入口不可点击：不再禁用整个主内容节点，只禁用被覆盖的业务内容子项，并明确保留原 AI 入口。未修改或抽离 `AI Agent/` 的脚本、CSS、素材与 DOM；实际点击已成功打开原 Agent 弹窗，尚未发布。
- 财富值中心与配置中心的主导航父项已统一为纯展开/收起：修复财富值子项当前态点击配置中心会误跳其他频道的问题。配置图标默认态直接引用与线上构建包逐字节一致的 SVG，当前态直接对同一 `<img>` 使用 CSS 滤镜同步变绿，避免外部 SVG 蒙版失效导致图标消失；配置中心补齐普通导航灰色 hover，二级项缩进改为 55px。普通频道、财富值、配置中心的图标/一级文字/二级文字坐标已实测统一为 36px/67px；两类父级在二级当前态时只显示品牌绿前景，父级保持透明，绿色浅底和左边框仅显示在当前二级项。DOM 回归、两条 HTTP 浏览器路径和控制台检查通过，尚未发布。
- 财富值中心“我的财富值”顶部概览当前使用用户提供的 `2500280795_huge.jpg` 原图：已复制为 `wealth-overview-user-wide-20260901.jpg`，尺寸 `3416×420`，页面以 `center / cover` 引用，不重新生成或调色。概览高度保持 `236px`，右侧构成卡片保持 90% 白色半透底；左侧两组指标之间为 `44px` 高短竖线，两侧间距各为常规大屏 `56px`、`1500px` 以下 `44px`；“我的财富值”明细表头为 `#F8F5F3`。精细胡桃木+玻璃版本、信封试用图和更早候选均保留但不再引用。财富值 CSS 为 `20260901-19`，频道注册表缓存为 `20260901-48`，尚未发布。

- [[knowledge/变更/当前未发布变更|当前未发布变更]]
- 全局顶栏“操作日志”入口已移除，旧缓存入口会被清理；配置中心日志二级页及组织页日志按钮保留。日期与用户区之间的分割线已缩短为 20px 并保持垂直居中。日志 CSS/JS 为 `20260901-1`、全局框架为 `20260901-7`，代码回归通过，待刷新确认。
- 全项目已盘点 45 类弹层；`全局组件/弹窗预览.html` 当前展示 28 个真实源弹窗，待接入为 0。原有 13 项继续调用共享 API 或完整本地控制器；新增 15 项通过正式 Hash 频道页和真实 DOM 点击入口打开，预览不复制 DOM/CSS、不修改只读 `web/`。全部 iframe 改为主动加载并提供逐项成功/失败状态；真实频道弹窗打开后由预览专用中性底层遮住频道正文，只保留原弹窗、确认框及遮罩。17 项不进入预览：6 类抽屉、用户明确排除的“GAIP Agent 助手主面板”，以及 10 项经当前路由/源码复核确认已下线、不可达或并非独立弹窗的旧登记。配置中心四类弹窗仍通过 `window.__GAIP_CONFIG_DIALOGS__` 与正式页共用状态和 `showModal()`。序列 1 尚未加入全局组件目录。
- 工作台图文对齐、日志控件与 AI 入口修复已同步 GitHub，静态/代码检查通过，待补充下列真实浏览器验收。
- 登录页视频英雄图测试入口已完成，但用户明确本次不同步，继续保留本地。
- AI Agent 试验素材和资讯中心参考站分包继续保留本地，不属于正式页面依赖。

## 待验证

- AI Agent 登录后/节点重建导致 V1/V2 入口失效已修复，顶部日期切换方式不变；共享动效脚本版本 `20260831-1`。生命周期与状态恢复代码测试通过，真实登录、跨频道及视频/WebGL 效果待手工验收。
- 工作台行情、分享图标和「查看全部」对齐：`file://` 页面受浏览器安全策略限制，本轮需刷新后手工确认视觉、点击交互与跨频道切换。
- 配置中心保留的操作日志弹窗原生 Esc 与键盘焦点循环仍需手工确认；模块筛选、关闭按钮、内嵌日志页和代码生命周期已验证。

## 待发布

- 配置中心频道同步及共享日志页面挂载能力；当前任务仅要求本地同步，不提交或推送 GitHub。
- 全局顶栏日志入口移除及共享日志/框架缓存更新；配置中心日志功能保留，当前未提交或推送。

- 本轮正式页面修改已发布，发布范围及提交号见 [[knowledge/变更/2026-08-31-工作台对齐与共享控件修复]]。
- 登录页视频英雄图测试入口：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`；用户明确本次不同步。
- 其余本地保留文件见 [[knowledge/变更/当前未发布变更|当前未发布变更]]。

## 已知高风险区域

- [[knowledge/公共模块/主导航与Hash路由|主导航与 Hash 路由]]：不得改为整页跳转。
- [[knowledge/公共模块/频道资源加载|频道资源加载]]：频道改版资源必须从任意入口都可用。
- [[knowledge/公共模块/全局框架与样式|全局框架与样式]]：顶栏、侧栏、面包屑、字体和 AI 入口是跨频道能力。
- [[knowledge/公共模块/本地Mock系统|本地 Mock 系统]]：纯静态预览依赖本地数据拦截，不可当作无用数据删除。

## 下一步

1. 刷新任一正式入口，确认顶栏无“操作日志”按钮；进入配置中心确认日志二级页和组织页日志按钮仍可用，并手工确认弹窗 Esc 与键盘焦点循环。
2. 刷新 `全局组件/弹窗预览.html`，手工确认新增 15 个正式频道触发弹窗的视觉与点击；自动浏览器受 `file://` 安全策略限制，本轮只完成源码与回归验证。
3. 若用户确认需要，再单独发布登录页视频英雄图测试入口。
4. 后续 GitHub 发布继续先核对本文件、当前未发布变更、`git status` 和 `git diff`。
5. 不要误提交本地试验素材或参考站 `web/` 分包。

## 快速入口

- [[knowledge/INDEX|项目知识总索引]]
- [[knowledge/页面/页面索引|页面索引]]
- [[knowledge/公共模块/公共模块索引|公共模块索引]]
- [[knowledge/决策/决策索引|决策索引]]
- [[knowledge/变更/变更索引|变更索引]]
- [[docs/README|运行与目录说明]]
- [[AGENTS|AI 维护规则]]
