---
type: project-state
project: GAIP 本地静态预览版
updated: 2026-09-16
---

# 当前项目状态

> 所有 Codex 对话的短入口。只记录当前仍然有效的状态；历史细节进入 [[knowledge/变更/变更索引|变更索引]]。

## 当前基线

- 最新功能提交：`e0b96bebb7a15a3464f853c4b38fd084e0bc92cf`（薄荷入职引导与共享专家库更新）。
- GitHub Pages 构建 `35046532380` 成功；线上入口：https://huangheping.github.io/gaip-style-preview/
- 发布记录：[[knowledge/变更/2026-09-16-薄荷入职引导与共享专家库更新|2026-09-16 薄荷入职引导与共享专家库更新]]。

## 已发布能力

- 薄荷0915需求已完成并同步GitHub：培训准备章节调整、共享专家库新增周克勤William及照片/双标签/二维码；下载资料不属于本项目处理范围。详见 [[knowledge/变更/2026-09-16-薄荷入职引导与共享专家库更新|发布记录]]。

- 学习中心 V1.1 本地预览：课程学习、课程管理、新增/编辑/草稿预览、课节上传状态、学情统计/详情/导出、课程与学情日志。
- 共享组件：表格、可配置筛选栏、日期选择器、下划线 Tab、组织树、弹窗字段/多行输入/标题/遮罩/按钮与表单内容标准。
- 全局组件目录、弹窗真实源登记、视觉参数台账和维护流程已同步。
- 全页统一使用本地 HarmonyOS Sans SC Regular/Bold；其他未引用字重已清理。
- 导航继续使用单文档 Hash 无刷新切换，频道资源从任意正式入口都可用。
- 共享 Tab 使用静态下划线，当前项 Bold、其余 Regular；产品中心、组织架构、学情统计和组件库同源。
- 组织架构批量导入第二步与成员佣金归属唯一性已更新；产品卡片使用正式机构 Logo，并支持跨入口加载。

## 验证状态

- `npm test` 全部通过；学习中心专项 DOM 回归全部通过。
- Chromium 专项通过：字体、筛选、日期、表格、Tab、弹窗、课程创建/编辑/上传/日志/学情和响应式边界。
- 产品中心直开及工作台切入的机构 Logo 浏览器回归通过，资源加载、尺寸和单例保护正常。
- 本机 Chrome 119 的原生 dialog 内嵌 popover 有旧引擎崩溃，嵌套组织树路径仅在该旧版本跳过；同一交互的主页路径与弹窗生命周期已分别通过。
- 视频播放、完整 Umi 登录链路和 WebGL 图像渲染仍不由静态回归代替真实环境验收。

## 当前未发布

- 直播标签已采用黑底金色文字/图标（`#EFCE88`），形状与尺寸不变；浏览器配色和布局检查通过，记录合并于 `design-changes/learning-live.json`。

- 直播横卡封面已放大，标签接入用户视频图标；第一场标题与简介已按指定文案更新，旧默认示例一次性迁移。保留原比例、单场半宽与已有字号；独立浏览器布局/图标、迁移及导航检查通过。参数合并于 `design-changes/learning-live.json`。

- 活动中心与学习中心已接入共享轮播切换控件：活动外观与原生轮播保留，直播为左右圆形箭头＋底部短条指示点，手动按组循环，单组隐藏。组件目录新增同源预览。独立浏览器、目录/表单 DOM 和导航检查通过；完整file页面待刷新验收。参数见 `design-changes/shared-carousel-controls.json`。

- 学习中心前两个直播示例已分别换为用户提供的产品服务说明会、双嘉宾封面（750×320原图），独立素材不影响课程封面；已有占位数据一次性更新，用户上传和其他记录保留。图片一致性、迁移及独立浏览器检查通过。

- 学习中心直播与课程卡片 hover / focus-visible 已共用课程原有样式规则；保留单场半宽靠左，悬停阴影、上浮、封面微放大及键盘焦点一致。独立浏览器样式对比通过，视觉参数见 `design-changes/learning-live.json`。

- 学习中心首页按最新确认恢复直播横卡：单场半宽靠左、双场并排、更多按组切换、无直播整体收起；直播位于内容滚动区，桌面原头部高度不变，管理入口按权限显示。独立开播时间控制“距开播”→“直播中”，未配置时显示待定。Mock 栏继续隐藏、默认超级管理员，课程数据不变。实现与验证见当前变更及 `design-changes/learning-live.json`，完整 file 页面待用户验收。
- 直播 Banner 已追加 8 条本地 Mock（3 上架、3 草稿、2 下架）和 15 条模拟日志；一次性批次保留用户数据，刷新不重复。新增展示批次仅为原三条上架示例补公开标题、简介及开播时间；首页按组展示。素材仅本地示例，不代表真实直播。
- 学习中心 V1.2 直播 Banner 本地首版：草稿需确认上架、首页轮播、独立直播管理及课程/直播合并日志；复用共享筛选、表格和弹窗，新增可选不分页。专项 DOM/独立 Chromium 通过，完整业务页仍待验收；直播 ID 地址转换和真实上传/调度不在本地实现内。见 `docs/learning-center-v12-local.md`、`design-changes/learning-live.json`。仅 Documents 原项目，未发布。

- 下划线Tab新增薄荷入职引导顶部章节、方案中心视图切换，组件目录登记扩展到五频道。独立原页面结构浏览器回归通过；入职小节胶囊、进度与业务规则保留，完整页面视觉待确认。
- Tab 目录展示已分为两张白色预览卡，状态与说明置于卡片外；独立浏览器桌面/窄屏检查通过，参数见 `design-changes/component-catalog.json`。
- 共享下划线 Tab 已同步产品中心、组织架构和学情统计：选中名称加粗，括号及数字常规字重并保留名称后的间距；产品复用原React数量外层节点，不拆改。三个频道专项浏览器回归通过，参数见 `design-changes/shared-tabs.json`。
- 共享折叠多选的边框状态、箭头与下拉选项视觉已对齐筛选器；独立 Chromium 对照及筛选栏回归通过。记录：`design-changes/shared-multi-select.json`；完整业务页未逐页视觉验收。
- 共享“所属组织”选择树首次打开已改为只显示顶级组织、父节点默认收起；已有下级选择仅展开祖先路径，搜索匹配仍自动展开。学习中心真实筛选器与配置中心组织管理回归通过。
- 另有用户明确排除的本地候选：登录视频测试入口、AI Agent creative-8/9 素材与 `备份.mp4`、财富值概览候选图、资讯参考分包。
- 精确文件清单见 [[knowledge/变更/当前未发布变更|当前未发布变更]]；未经用户明确确认不得混入后续发布。

## 已知高风险区域

- [[knowledge/公共模块/主导航与Hash路由|主导航与 Hash 路由]]：不得改为整页跳转。
- [[knowledge/公共模块/频道资源加载|频道资源加载]]：频道改版资源必须从任意入口可用。
- [[knowledge/公共模块/全局框架与样式|全局框架与样式]]：顶栏、侧栏、面包屑、字体和 AI 入口是跨频道能力。
- [[knowledge/公共模块/本地Mock系统|本地 Mock 系统]]：本地状态、迁移标记和旧数据不可当作无用内容删除。

## 快速入口

- [[knowledge/INDEX|项目知识总索引]]
- [[knowledge/页面/页面索引|页面索引]]
- [[knowledge/公共模块/公共模块索引|公共模块索引]]
- [[knowledge/决策/决策索引|决策索引]]
- [[knowledge/变更/变更索引|变更索引]]
- [[docs/README|运行与目录说明]]
- [[AGENTS|AI 维护规则]]
