---
type: project-state
project: GAIP 本地静态预览版
updated: 2026-09-04
---

# 当前项目状态

> 所有 Codex 对话的短入口。只记录当前仍然有效的状态；历史细节进入 [[knowledge/变更/变更索引|变更索引]]。

## 当前基线

- 分支：`main`
- 最近已发布功能提交：`8451acc`（公告管理、配置中心二级日志细化、组织成员列表取消分页；见 [[knowledge/变更/2026-09-03-公告管理与配置中心列表优化]]）
- GitHub Pages：`https://huangheping.github.io/gaip-style-preview/`
- 运行形态：根目录 HTML 薄壳 + Umi Hash SPA + 本地 Mock；“本地”默认指项目文件和既有 `file://` 预览。
- 主导航为项目级强制契约：新增频道前必须读取 [[knowledge/公共模块/主导航与Hash路由#主导航交互与样式契约|主导航交互与样式契约]]。
- 频道配置唯一来源：`shared/config/channels.js`。
- 原始构建产物：`web/`，默认只读。

## 正在进行

- 普通弹窗的位置已收敛为共享居中基础组件：Ant Modal、原生 dialog 与已登记自定义弹窗统一在当前视口水平/垂直居中，桌面/窄屏安全边距为 24/12px；抽屉、GAIP Agent 主面板和嵌套确认边界保持独立。35 个真实弹窗预览同时统一为 820px 视口，避免不同 iframe 高度造成视觉假偏移；自动回归已通过，待用户刷新现有 `file://` 预览做最终视觉确认。详见 [[knowledge/变更/当前未发布变更#全局弹窗居中定位基础组件|当前未发布变更]]。
- 全局弹层遮罩已收敛为唯一共享基础组件：原生 dialog、Ant Modal / Drawer 和本地自定义弹层统一使用 45% 黑色、160ms 动效与无模糊规则；15 个正式入口和真实弹窗预览直接加载同一文件，业务样式只保留尺寸、内容结构与层级。自动回归已通过，待用户刷新现有 `file://` 弹窗预览做最终视觉确认。详见 [[knowledge/变更/当前未发布变更#全局弹窗遮罩基础组件|当前未发布变更]]。
- 全局侧栏“GLORY 百宝箱”已继续替换为用户最新提供的 388×112 横版图片，移除英文说明与入口圆角、扩展为与底部背景容器两端对齐，并把侧栏 footer 的底部内边距统一清零；16 个入口已切换到最新图片缓存版本，自动回归通过，待现有 `file://` 页面刷新确认。详见 [[knowledge/变更/当前未发布变更#全局侧栏百宝箱横版入口|当前未发布变更]]。
- 真实弹窗预览已精简为“信息展示 13 / 表单操作 16 / 操作确认 6”三类；分类写入真实源登记，预览保留 01–35 原编号，并在页首提供可点击定位的三栏分类总览，便于先核对归类再查看真实弹窗。详见 [[knowledge/变更/当前未发布变更#弹窗预览三类用途目录|当前未发布变更]]。
- 操作确认统一组件 v1.1.3 已完成：05、12、17、25、26、35 六项真实弹窗均由共享组件接管；保留用户手动调整后的标题、正文和间距参数，并统一重建 16px 关闭图标、32px hover 点击区与居中规则。第 12 项旧配置中心哈希样式类已在接入时移除，第 17 项不再携带业务内复制的关闭 SVG。未来新增操作确认弹窗必须调用同一共享组件。详见 [[knowledge/变更/当前未发布变更#操作确认统一组件与六项接入|当前未发布变更]]。
- 配置中心三处组织节点树已增加打开/闭合文件夹状态，左侧树与批量导入树随折叠切换，调整节点树按父/叶节点展示。详见 [[knowledge/变更/当前未发布变更#组织树展开文件夹状态|当前未发布变更]]。
- “编辑对话名称”作为全局复用操作弹窗已单独进入“全局组件 / 弹窗预览”，编号为 18；Agent 主面板、审核历史及内部关闭确认仍排除。详见 [[knowledge/变更/当前未发布变更#AI Agent 弹窗预览范围校正|当前未发布变更]]。
- 调整管理员节点的二次确认已移除感叹号图标，原节点与权限变化改为分层文字排版，确认操作使用红色危险按钮；主弹窗与二次确认均可在弹窗组件预览中独立打开。详见 [[knowledge/变更/当前未发布变更#调整管理员节点二次确认样式|当前未发布变更]]。
- [[knowledge/变更/当前未发布变更|当前未发布变更]] 还保留用户明确不发布的登录视频测试入口，以及未被正式页面引用的本地试验素材、财富背景候选图和资讯参考分包。

## 待验证

- 组织架构专属操作日志弹窗的原生 Esc、顶层遮罩与视觉仍需在既有 `file://` 本地预览手工确认；7 列字段、独立 Mock、分页、关闭、焦点恢复代码、与二级日志控制器隔离及生命周期已通过 DOM 回归。
- AI Agent 登录后/节点重建入口恢复已通过代码测试；真实登录、跨频道和视频/WebGL 效果继续作为手工视觉验收项。

## 待发布

- 全局弹窗定位/遮罩/确认组件、35 项用途目录、配置中心组织树与管理员确认、侧栏横版百宝箱，以及本地领先 GitHub 的公告管理与配置中心提交，已完成自动回归，待本次同步。
- 登录页视频英雄图测试入口：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`；用户明确不随本次发布。
- 其余未发布候选文件见 [[knowledge/变更/当前未发布变更|当前未发布变更]]；它们没有进入正式提交 `8451acc`。

## 已知高风险区域

- [[knowledge/公共模块/主导航与Hash路由|主导航与 Hash 路由]]：不得改为整页跳转。
- [[knowledge/公共模块/频道资源加载|频道资源加载]]：频道改版资源必须从任意入口都可用。
- [[knowledge/公共模块/全局框架与样式|全局框架与样式]]：顶栏、侧栏、面包屑、字体和 AI 入口是跨频道能力。
- [[knowledge/公共模块/本地Mock系统|本地 Mock 系统]]：纯静态预览依赖本地数据拦截，不可当作无用数据删除。

## 下一步

1. 后续发布前继续核对本文件、[[knowledge/变更/当前未发布变更|当前未发布变更]]、`git status` 和 `git diff`。
2. 新增或修改弹窗时更新真实源登记并运行自动索引生成、弹窗预览回归；修改主导航时运行可展开主导航与 Hash 导航回归。
3. 若用户确认需要，再单独发布登录视频测试入口或其他候选素材；未获确认不得误提交。

## 快速入口

- [[knowledge/INDEX|项目知识总索引]]
- [[knowledge/页面/页面索引|页面索引]]
- [[knowledge/公共模块/公共模块索引|公共模块索引]]
- [[knowledge/决策/决策索引|决策索引]]
- [[knowledge/变更/变更索引|变更索引]]
- [[docs/README|运行与目录说明]]
- [[AGENTS|AI 维护规则]]
