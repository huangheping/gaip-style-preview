# 配置中心原站视觉纠偏验收

日期：2026-08-31。范围：配置中心及组织架构页面；保留新增操作日志二级入口。

## 对照证据

- 原站：`https://glory-gaip-web.t3.test.noahgrouptest.com/web/config/organization`，当前登录账号可见状态，仅浏览、不提交表单。
- 本地：`http://127.0.0.1:8765/配置中心.html#/workspace?gaip-channel=config&gaip-view=organization`。
- source visual truth path：`/tmp/gaip-config-fidelity-qa/source-page-final.jpg`、`source-editor-final.jpg`。
- implementation screenshot path：`/tmp/gaip-config-fidelity-qa/local-page-final.jpg`、`local-editor-final.jpg`。
- viewport：两页均核对为 1326×1035 CSS px、devicePixelRatio 1。浏览器返回原站截图 1307×1020、本地 1326×1035；对照时将原站截图等比例近似归一至 1326×1035，不能据此声称像素完全一致。另检查了 1280×720 与窄窗口的固定宽度行为。
- state：薄荷经纪人、组织树展开、根部门选中、8 条成员记录；弹窗为第一位成员编辑态。原站个人字段与本地示例字段不同，不作为视觉差异。
- full-view comparison evidence：`/tmp/gaip-config-fidelity-qa/compare-page-final.png`，已将两页放入同一张对照图检查。
- focused region comparison evidence：`/tmp/gaip-config-fidelity-qa/compare-details-final.png`（页签、树、表格）；`compare-editor-final.png`（完整编辑内容）。源截图含原站账号信息，仅留临时目录，不加入项目资源。
- 最终菜单补查：`/tmp/gaip-config-fidelity-qa/local-navigation-final.jpg`；展开箭头的两个伪元素变换矩阵与原站一致。

## 发现、修复与复核

1. [P1，已修复] 上轮手工重写了组织结构、图标与颜色。改用原站 DOM 模板、组织页 CSS、Ant 运行时样式和真实图标；上述最终整页、细节、弹窗对照均已复核。
2. [P2，已修复] 初次提取的运行时 CSS 被截断，造成树缩进、表单控件缺样式；CSSOM 对含变量的边框简写序列化不完整，复选框勾选符号缺失。改为分段保存原始 style 文本，源 CSS 与组织 CSS 采用相同作用域。最终树缩进为 12px，复选框勾选边框为原站 2px 白色实线，已截图复核。
3. [P2，已修复] 原生弹窗适配被源样式覆盖，宽度撑满窗口。提高挂载适配规则优先级，恢复原站 850px 宽度；源内容高度 726px、本地 727px，1px 差异属于容器边界，不影响布局。最终完整表单对照通过。
4. [P2，已修复] 导航箭头叠加了自定义旋转。移除额外旋转，使用原组件展开态；最终两侧伪元素计算变换一致，菜单展开/收起验证通过。

比较历史：最初手绘版本及第一次源样式迁移均有上述 P1/P2，未通过；修复截断、边框和弹窗宽度后重新截图，形成 `*-final` 证据；最后补查并修复菜单箭头。当前无待处理 P0/P1/P2。

## 五项视觉检查

- 字体：复用源 Ant 系统字体栈、14px 表格文字及源页签/表单字号和权重；截断与换行结构保留。截图缩放带来的抗锯齿差别不作像素级断言。
- 间距与结构：保留 72px 页签头、原部门树/表格双栏、48px 搜索与操作按钮、表格列结构及 850px 表单。页面和局部对照未发现新的层级或布局偏移。
- 颜色：原站 #025b52 操作按钮、#24d4c9 选中控件、浅绿选中背景、金色身份标签及中性灰表格。复用源变量和 CSS，不另设配色方案。
- 图像与图标：配置中心图标、黄色文件夹、删除图标及原 DOM 内的搜索/下载/加号/持牌/关闭图形直接来自源资源；未绘制替代图标。
- 文案与内容：六个渠道名称、原部门树、表头、操作文案保持原样。仅新增用户要求的操作日志二级项；人员身份和联系方式替换为示例，日志保留既有 28 条模拟记录。

## 功能验证与边界

- 工作台 → 配置中心 → 资讯；客户 HTML → 配置中心 → 产品，两条路径的文档会话标记均不变。
- 菜单展开/收起、树折叠、成员搜索、编辑邮箱与转介绍类型保存及再次打开回显、日志模块筛选、顶栏日志弹窗开关与页面筛选隔离通过。
- 导航检查、JS 语法、`git diff --check` 和操作日志自动回归通过。自动回归覆盖导出文件结构和页面/弹窗实例隔离；此次未重新自动化验证浏览器实际下载和原生 Esc/焦点循环。
- 现有本地顶栏、其他频道顺序、AI 入口均保留；这些与原站的差别不在此次配置中心同步范围。
- 原站采用固定桌面宽度，窄窗口需横向滚动；本地保持这一结构，未设计移动版。矮窗口内编辑弹窗使用内部滚动以保证按钮可访问。
- 原生对话框初始焦点的可见焦点框保留，为预期的可访问性差异。部门管理等外围操作未接入后台；本地修改刷新即重置，不是真实权限或审计系统。
- 没有修改线上配置、`web/` 原始构建文件或发布代码。

## 后续批注修复：部门菜单与日志按钮

- 用户指出部门“⋮”缺少点击行为、组织页日志图标与文字错位。本轮将部门管理纳入本地交互范围；前轮只验证了组织树展开与成员编辑，未验证该菜单。
- [P1，已修复] 部门按钮未绑定菜单。已核对原站根部门的添加子部门/设置管理员、子部门的修改名称/添加子部门/设置管理员/删除，并复用实际菜单 SVG 与弹窗 DOM。新增本地名称校验、删除确认和非空保护、管理员保存及渠道隔离。
- [P2，已修复] 图标 wrapper 受文字基线影响，原 SVG 无 viewBox，画布与实际图形范围不同。补齐 `viewBox="0 0 15 16"`，保持所有路径不变，以 20px flex 容器居中。运行时图标与文本中心线均为 92.5px。
- 新证据：`/tmp/gaip-department-fix-qa/menu-comparison.png` 将原站与本地菜单放在同一图片对照，菜单均为 126×72px；`/tmp/gaip-department-fix-qa/log-alignment.png` 为修复后的按钮局部。鼠标展开不主动聚焦首项，键盘展开保留可见焦点。
- `scripts/test-config-center.cjs` 验证点击不误选部门、Esc/外部关闭、空名与重名校验、增改删、非空保护、管理员保存/取消、渠道隔离、日志按钮和跨频道卸载。原日志回归、导航检查通过；两条真实 HTTP 浏览器导航路径均未整页刷新。
- 自动浏览器禁止打开 file URL，本轮没有绕过；真实运行验证使用既有本地 HTTP 预览，文件入口由静态检查与使用 file URL 的 DOM 测试覆盖。仍需用户刷新原 file 页面确认其浏览器显示。没有改动线上数据。

final result: passed
