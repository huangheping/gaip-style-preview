---
type: release-note
date: 2026-08-31
status: published
commit: 5d597a4
---

# 工作台对齐与共享控件修复

## 发布结果

- 已同步到 `huangheping/gaip-style-preview` 的 `main`。
- 样式功能提交：`5d597a4ff6f87c78e75438d8b34af0fc0c5674d1`，25 个正式文件。
- 同时发布此前仅在本地的 AI 入口修复：`ee5c3e9cd94b1245491b6909aea94fb9502c54a5`。
- Git HTTPS 直连超时；使用已登录的 GitHub CLI 经 API 上传 Git 对象，逐一核对 blob、tree、commit SHA 与本地一致，再以 `force: false` 快进更新分支。远程分支查询已确认功能提交。

## 工作台行情与分享图标对齐

- 行情边框恢复白色；轨道随容器等高伸展，国旗和文字统一 20px 行盒，文字补偿 1px 视觉中心。
- 分享图标保留原路径与 18px 显示尺寸，校正 SVG viewBox 的不对称留白。
- 文件：`features/workspace/workspace-update.css`、`features/workspace/workspace-update.js`。

## 工作台查看全部入口对齐

- 字符箭头替换为 12×16px 居中 SVG，移除零行高及向下偏移；图文间距 4px，禁止压缩和折行。
- 保留按钮原有最小宽高、字号、位置与跳转事件；14 个正式入口的工作台 CSS/JS 缓存号统一为 `20260831-2`。

## 操作日志筛选图标与聚焦边框

- 复用原站 Ant Design outlined 图形，新增 `shared/assets/control-down.svg`、`control-search.svg`、`control-calendar.svg`，由 `shared/styles/global-operation-log.css` 作为背景图引用。
- 下拉图标 12px，搜索/日历图标 14px；保留日期原生点击热区，不支持 WebKit 指示器的浏览器保留原生入口。
- 搜索、日期、筛选和分页下拉的悬停/聚焦边框统一品牌绿，无额外阴影/外框，日期错误仍显示红边；按钮与表格键盘焦点框不变。
- 正式入口日志 CSS 缓存号为 `20260831-6`，日志 JS 仍为 `20260831-4`。更新 `scripts/test-operation-log.cjs` 与框架笔记，不改日志数据、弹窗脚本和导航。

## AI Agent 登录后入口与版本切换恢复

- `AI Agent/AI Agent入口动效.js` 持续监听相关结构变化，登录、Hash 切换及 React 节点重建后重绑入口，恢复选中版本、处理状态与最小化状态。
- 保持顶部日期切换 V1/V2 的现有行为；挂载幂等，按生命周期清理旧 WebGL、Lottie、视频和提示定时器。
- 正式入口动效脚本缓存号为 `20260831-1`；回归脚本为 `scripts/test-agent-entry.cjs`，同步 AI Agent 笔记。

## 验证与待确认

- 本次重跑 JS 语法、日志代码回归（含筛选、日期、分页、XLSX/XML/CRC）、AI 生命周期回归、导航静态检查和 diff 检查，全部通过。
- 核对 14 个正式入口的 476 处本地资源引用、缓存版本与日志图标依赖；未暂存测试或参考素材。
- 工作台 SVG 中心、Flex 规则和跳转事件保持已由前次静态断言验证。
- 浏览器访问受安全策略限制，未新增真实浏览器验收：工作台视觉与点击、日志原生下拉/日历、Esc/焦点循环、AI 真实 Umi 登录/跨频道和视频/WebGL 效果仍待手工确认。代码测试不代表这些项目已验收。

## 发布边界

- 正式入口：`index.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`。
- 第 15 个登录视频试验入口虽在本地同步缓存版本，但未发布；`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/` 继续保留本地。
- AI creative-8/9、备份视频和 `web/p__news__index.f64a0a2e.async.js` 参考资源未发布；本次没有修改 `web/` 基线。
- 项目状态、当前变更清单、变更索引及相关模块笔记同步归档。
