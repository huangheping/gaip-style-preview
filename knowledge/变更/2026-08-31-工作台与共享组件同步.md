---
type: release-note
date: 2026-08-31
status: published
commit: 5665580
---

# 工作台与共享组件同步

## 发布内容

- 工作台：资讯来源文案、文章分享按钮、字号/颜色/间距统一、桌面自适应与本地 Medium/Bold 字重。
- 全局海报分享：共享 JS/CSS、13 款模板、图片/字体/图标及组件预览；工作台和资讯中心复用同一套实现，依赖全部纳入仓库。
- 全局侧栏：同步“GLORY 百宝箱”图片、176px 宽度与 11px 圆角。
- 产品详情：保留已有 Mock 内容和卡片样式；统一 14 个正式入口的 `local-preview.js?v=20260831-1`，通过频道配置加载版本化产品 CSS，避免从其他入口切换时读到旧资源。
- 正式入口配置版本统一为 `channels.js?v=20260831-1`；资讯/产品页面笔记和共享模块说明同步更新。
- 客户中心整页视觉规格调整此前已被用户撤回，本次未重新引入。

## 操作日志

- 顶栏顺序：操作日志 → 日期 → 用户区；16px 用户指定 SVG、14px 字号、32px 高度/圆角、通高分割线和对齐留白。
- 模拟数据弹窗：模块/操作类型/日期/账号筛选、分页、展开长内容、关闭与筛选状态保留。
- Excel 导出全部筛选结果，纯本地 OOXML，不采集真实操作、不接入服务器。

## 验证

- 65 个正式文件；12 个独立 JS、16 段内联脚本语法通过；506 处本地资源引用检查无缺失，排除文件未被暂存。
- 导航静态检查、日志 jsdom 回归、XLSX ZIP CRC/XML/列与全量数据检查、Git diff 检查通过。
- 本地浏览器验证日志遮罩/布局、模块筛选和关闭按钮；工作台/资讯海报模板均能加载，资讯文章标题正确传入海报；产品详情包含 4 个新版卡片。
- 工作台 → 资讯 → 产品，以及客户中心 HTML → 工作台 → 资讯 → 保单 → 学习，主文档 loaderId 始终不变，日志入口始终唯一。
- 原生 Esc 与键盘焦点循环未获可靠自动化结论，仍保留手工确认项；不把 jsdom 测试等同原生键盘验收。

## 发布边界

- 功能提交：`5665580`，推送到 `huangheping/gaip-style-preview` 的 `main`。
- 不发布：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`、AI Agent creative-8/9 与备份视频、资讯参考站分包。
- 产品 CSS 是用户此前直接修改的基线文件，本次仅保留同步，不继续修改 `web/`；以后频道专属改版仍应迁至 `features/product/`。

