---
type: release
date: 2026-09-07
status: 已发布
commit: 8b1af87
---

# 2026-09-07 官方工程基线与测试依赖

## 发布结果

- 功能提交：`8b1af87`（`Add reproducible repository checks and guidance`）。
- 完整提交：`8b1af876923dcda045e5a225ce49fbad0c4a883a`。
- 分支：`main`，已推送至 GitHub `origin/main`。
- 本次提交共 11 个文件、736 行新增、9 行删除；不修改网站运行时、HTML/CSS、Agent/Skill 或 CI。

## 更新内容

- 新增开发专用 `package.json` 与 `package-lock.json`，固定 Node 24 / npm 11 和 `jsdom@26.1.0`，统一提供 `npm test` 及分项检查入口。
- 新增 `docs/official-engineering-baseline.md`，记录依赖、命令及可选本地环境/worktree 接入依据。
- 新增 `docs/browser-regression-checklist.md`，区分真实浏览器验收与静态/JSDOM 检查。
- 更新维护流程、项目状态、文档入口与知识索引。

## 验证

- `npm test` 退出码 0：静态导航、弹窗目录，以及导航、配置中心、公告管理、操作日志、AI Agent 五个 DOM 套件均通过。
- `git diff --cached --check` 通过；锁文件与开发依赖已随提交发布。
- 用户此前手工确认四项行为：切换工作台后刷新仍保留该页、关闭操作日志后导航可点击、Esc 关闭后可再次打开、登录后及跨频道 AI V1/V2 可切换。
- 原生弹窗焦点/顶层、实际 Umi 主文档请求、浏览器视觉、视频播放与 WebGL 仍未由自动化验证。

## 排除项

- 登录页视频英雄图测试入口、AI Agent 试验素材、财富值候选图和 `web/` 资讯参考分包继续只保留在本地，未进入提交。

## 关联文档

- [[docs/official-engineering-baseline|有官方依据的工程基线]]
- [[docs/browser-regression-checklist|浏览器行为回归清单]]
- [[docs/maintenance-workflow|维护流程]]
- [[当前未发布变更]]
