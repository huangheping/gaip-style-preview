---
type: release
date: 2026-08-26
commit: 08a950b
dependency-fix: 7575d44
---

# 2026-08-26 频道与全局组件更新

## 结果

- 发布提交：`08a950b`（Publish GAIP channel and shared UI updates）
- 资讯中心依赖修复：`7575d44`（Fix news center asset dependencies）
- 新增资讯中心虚拟频道和财富值中心三个子频道。
- 发布客户中心“已关联保单”行内标签补丁。
- 发布 AI Agent V2 正式入口、处理/完成/最小化状态联动和处理态视频素材。
- 新增全局 AI 内容提示与折叠式多选下拉组件，并建立 `全局组件/` 目录预览。
- 建立 `PROJECT_STATE.md`、`knowledge/` 与 WorkBuddy 项目文档工作流。

## 主要范围

- 频道入口与配置：`资讯中心.html`、`财富值中心.html`、`shared/config/channels.js` 及根目录正式 HTML 薄壳。
- 频道源码：`features/news-center/`、`features/wealth-center/`、`features/customer/`。
- 资讯中心基线样式与头图已归入 `features/news-center/`，不依赖新增 `web/` 分包。
- 共享能力：Hash 虚拟频道识别、面包屑、区域标记、导航样式、全局 AI 提示与多选组件。
- AI Agent：`AI Agent/` 下正式 CSS/JS、本地 Mock 和 V2 处理态视频。
- 文档：`AGENTS.md`、`PROJECT_STATE.md`、`knowledge/`、`workbuddy/`、`全局组件/`。

## 验证

- 18 个相关 JavaScript 文件通过 `node --check`。
- `./scripts/verify-local-navigation.sh` 通过。
- 15 个本次发布 HTML 入口的本地资源引用检查通过，`git diff --check` 通过。
- 浏览器验证工作台可进入资讯中心与财富值中心，Hash 切换不整页刷新；两个虚拟频道直接刷新仍正常。
- 资讯中心显示 25 条本地 Mock 资讯；客户中心“已关联保单”位于 `cTags` 行内且 `position: static`。
- AI Agent V2 入口和视频加载正常；全局 AI 内容提示弹层可打开。
- 全局多选组件可展开、折叠计数、清空，并可用 Esc 关闭。

## 本次明确未发布

- `index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- AI Agent 的 `creative-8/9` 试验素材和 `备份.mp4`。
- 资讯中心参考站异步脚本 `web/p__news__index.f64a0a2e.async.js`。

## 关联

- [[../页面/资讯中心]]
- [[../页面/财富值中心]]
- [[../页面/客户中心360]]
- [[../公共模块/AI Agent]]
- [[../公共模块/全局框架与样式]]
