---
type: current-changes
updated: 2026-09-03
---

# 当前未发布变更

## 批量导入弹窗整窗拖拽上传

- 状态：已完成，待用户在既有本地预览中验收。
- 目标：第一步允许将 Excel 文件拖入批量导入弹窗任意区域，并用品牌色半透明遮罩反馈可放下上传；把支持格式说明移到上传按钮旁的文件限制文案中。
- 范围：仅调整批量导入弹窗第一步的拖拽交互、状态反馈与提示文案，沿用现有文件格式、10MB 校验和后续校验流程。
- 实际文件：`features/config-center/config-center.js`、`features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、配置中心资源版本与弹窗自动索引，以及本记录、`PROJECT_STATE.md`、`knowledge/页面/配置中心.md`、`knowledge/公共模块/频道资源加载.md`。
- 验证：配置中心完整 DOM 回归覆盖整窗任意区域拖入、遮罩显示/离开恢复、底部区域放下文件、文件名回显和校验按钮启用；弹窗自动索引检查、31 个可预览弹窗注册表检查、Hash 无刷新导航、JS 语法及 `git diff --check` 均通过。配置中心主体样式更新至 `20260903-23`、交互脚本更新至 `20260903-34`，15 个正式根入口的频道注册表缓存更新至 `20260903-44`。

## 组织架构三级节点测试数据

- 状态：已完成，待用户在既有本地预览中验收。
- 目标：在薄荷经纪人渠道增加可直接验收的三级组织节点及成员数据，用于检查树层级、节点搜索、完整路径、成员列表和调整节点流程。
- 范围：六个频道均在“测试部门 / 测试1部”下新增四个三级节点；薄荷经纪人保留 24 人，其余频道各 4 人，所有 Mock 成员均放入叶子节点，有子节点的父级不直接挂人，首屏成员 06–09 覆盖四类角色。左侧树、调整节点和批量导入目标节点共用按频道隔离的同一份节点数据，不改变线上数据或接口。
- 验证：配置中心完整回归覆盖六频道各 23 个节点、叶子节点直接成员、测试1部与其直属子节点集合完全相等、测试部门等于测试1部与测试2部的去重并集、研发部更深层父子集合也完全相等、超过 10 人后分页可继续查看全部后代成员、频道根与两个一级分支并集完全相等、左侧树与调整节点完整路径一致、批量导入切换六渠道后节点和路径一致，以及角色在频道根与三级列表的状态一致；频道资源 JS 更新至 `20260903-34`，正式根入口的频道注册表缓存更新至 `20260903-44`；弹窗预览内的配置中心资源同步增加版本号，避免已打开预览继续使用旧逻辑。

## 弹窗视觉盘点与标准化分层

- 状态：已完成视觉盘点；本轮未修改任何弹窗 CSS、DOM 或交互。
- 目标：以弹窗自动索引中的真实源为准，整理全部可预览弹窗，区分可全局统一的设计基础、按类型复用的布局模板和必须保留的业务个性。
- 范围：31 个可预览弹窗；确认、表单、详情、表格、流程、系统提示六类；不包含抽屉和已排除项。
- 实际文件：`knowledge/公共模块/弹窗视觉规范与盘点.md`、`knowledge/公共模块/公共模块索引.md`、`knowledge/公共模块/全局框架与样式.md`、`全局组件/README.md`、`PROJECT_STATE.md`、本记录。
- 约束：盘点结论只作为下一阶段设计基线；未获确认前不创建全局覆盖选择器，不改变真实弹窗视觉。
- 验证：自动索引当前为 31 个可预览弹窗；31 个 ID 在盘点文档中各出现一次，无遗漏或重复；`git diff --check` 通过。

## 组织架构成员调整节点

- 状态：已完成，待用户在既有本地预览中验收。
- 目标：根据产品文档补齐成员跨组织节点调整流程，统一界面文案为“调整节点”。
- 范围：成员行“更多”和编辑成员弹窗共用入口；目标节点搜索与选择；原节点管理员二次确认；成员归属迁移、成功提示及组织架构独立操作日志。
- 约束：目标节点限制在当前渠道；原节点为当前节点时禁用；若成员是原节点组织架构管理员，迁移时移除该身份且不自动授予目标节点管理员身份。
- 实际文件：`features/config-center/config-center.js`、`features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、`scripts/test-popup-preview.cjs`、`全局组件/弹窗自动索引.generated.js`、`shared/config/channels.js`、16 个根 HTML 薄壳、`knowledge/页面/配置中心.md`、`knowledge/公共模块/频道资源加载.md` 与 `PROJECT_STATE.md`。
- 验证：配置中心完整 DOM 回归通过，覆盖两个入口、当前节点禁用、搜索/清空、普通成员迁移、管理员二次确认与身份移除、旧节点角色清理、成功提示、独立日志写入及公共预览入口；弹窗自动索引为 `48 total / 31 ready / 0 pending / 17 excluded`；操作日志专项回归、频道导航校验、JS 语法与 `git diff --check` 均通过。临时 HTTP 仅用于自动化视觉检查，已关闭；交付仍使用既有 `file://` 本地预览。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
