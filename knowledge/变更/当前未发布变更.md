---
type: current-changes
updated: 2026-08-26
---

# 当前未发布变更

> 这是协作清单，不代替 `git status` 或 `git diff`。每次开始任务先登记，结束任务补充实际结果。

## 建立项目知识库

- 状态：待发布
- 目标：用 Obsidian 双链和分层索引规范不同 Codex 对话之后的工作。
- 范围：`AGENTS.md`、`PROJECT_STATE.md`、`knowledge/`
- 实际文件：`AGENTS.md`、`PROJECT_STATE.md`、`knowledge/` 下 27 个 Markdown 文件。
- 验证：知识库内部双链无断链；`./scripts/verify-local-navigation.sh` 通过；Git 状态确认未修改既有 AI Agent 工作文件。
- 来源：当前 Codex 对话

## AI Agent 既有修改

- 状态：待发布
- 说明：同步 AI Agent 面板尺寸与边界样式、入口 V2 动效、处理/完成/最小化状态联动和本地 Mock 事件。
- 实际文件：修改 `AI Agent/AI Agent.css`、`AI Agent/AI Agent入口.css`、`AI Agent/AI Agent入口动效.js`、`AI Agent/AI Agent改版.css`、`AI Agent/AI Agent本地Mock.js`；新增正式引用素材 `AI Agent/素材/机器人猫小面罩代码滚动悬浮入口循环动画v2.mp4`。
- 排除：未被正式入口使用的 `creative-8/9` 和 `备份.mp4` 保留本地，不进入本次发布。
- 验证：相关 JS 通过 `node --check`；本地 HTTP 下 AI Agent V2 入口和正式视频加载正常，跨频道入口仍存在。

## 新增财富值中心频道

- 状态：待发布
- 目标：在现有本地静态版框架中新增“财富值中心”，合并“导入工作台”“导入记录”“我的财富值”三个子频道的参考站样式与演示交互。
- 范围：新增频道入口、频道配置、虚拟频道导航、`features/wealth-center/` 专属实现、`assets/wealth-center/` 本地素材和本地 Mock；不修改 `web/` 构建产物。
- 数据：全部使用本地 Mock，不依赖测试环境接口。
- 参考：`https://glory-gaip-web.t3.test.noahgrouptest.com/web/wealth/importWorkbench`
- 追加目标：主导航“财富值中心”hover 态与普通主菜单一致，子菜单文字列继续微调对齐。
- 追加目标：我的财富值顶部“港币”胶囊使用用户附件 SVG 图标，概览标题与左侧青色方块视觉对齐。
- 实际文件：新增 `财富值中心.html`、`features/wealth-center/` 四个源码文件、`assets/wealth-center/wealth-overview.jpg`、`assets/wealth-center/hkd-currency.svg`、`knowledge/页面/财富值中心.md`；修改 `shared/config/channels.js`、`shared/scripts/channel-entry-navigation.js`、`global-breadcrumb.js`、`channel-regions.js`、`learning-nav.js`、所有根目录 HTML 的共享资源版本号，以及相关项目状态、页面/公共模块笔记和 `docs/channel-structure.md`。根据参考站当前构建资源补正财富值中心原始钱包图标、Ant 子菜单箭头、父子项 DOM 和 `24px / 55px` 对齐规则；箭头状态改为展开显示向上、收起显示向下的两个独立 SVG，并覆盖 Ant 默认 `10px` 宽度与 `translateY(-2px)` 偏移；父菜单补齐普通主菜单的 `4px` 左边框占位，icon 容器使用 `18px` 对齐列，钱包 SVG 保持 `16px`；父项 hover 态和选中态对齐普通主菜单；我的财富值“港币”胶囊替换为用户附件 SVG，本月标题方块改为按标题行高居中。
- 验证：全部 JS 通过 `node --check`；`./scripts/verify-local-navigation.sh` 通过；浏览器验证三个子频道、失败筛选、提交状态、批次选择、关键词弹窗、详情抽屉、财富值范围筛选均正常；验证财富值父项可用鼠标及键盘收起/展开，激活状态不再强制回弹，子项选择和跨入口打开正常；验证点击箭头所在位置可在上箭头/下箭头间切换，箭头盒子为 `12px × 12px` 且垂直中心偏差为 `0px`；验证财富值父项 icon 左边与文字左边分别为 `x=36`、`x=67`，和其他主频道一致；验证财富值父项 hover 颜色/背景与普通主菜单一致，子菜单文字左边为 `x=67`；验证 `file://` 和本地 HTTP 下“港币”图标加载为 `12px × 12px`，星号已移除，标题方块与文字中心偏差为 `0px`；`活动中心 → 财富值中心 → 活动中心` 文档会话 ID 不变，确认 Hash 无刷新。
- 来源：当前 Codex 对话

## 客户中心已关联保单标签同步

- 状态：待发布
- 目标：将客户中心列表卡片内“已关联保单”标签的位置与样式同步到参考站 `https://glory-gaip-web.t3.test.noahgrouptest.com/web/customer` 当前构建。
- 范围：新增 `features/customer/` 专属补丁，在频道配置中登记资源，并提升根目录 HTML 的频道配置版本号；不修改 `web/` 构建产物。
- 参考：参考站当前构建 `umi.80948343.js`、客户中心分包 `p__customer__index.871ec900.async.js` / `p__customer__index.f2fa1fbf.chunk.css` 中，“已关联保单”作为 `cTags` 标签行内的普通 `cTag` 渲染，不再使用本地旧构建的右上角绝对定位 `bindTag`。
- 实际文件：新增 `features/customer/customer-center.css`、`features/customer/customer-center.js`；修改 `shared/config/channels.js` 登记客户中心资源；根目录 HTML 的 `shared/config/channels.js` 版本号先同步到 `20260825-8`，后续随财富值中心补丁统一升至 `20260825-9`；更新 `knowledge/页面/客户中心360.md`、`PROJECT_STATE.md` 和本变更记录。
- 验证：`node --check features/customer/customer-center.js` 通过；`./scripts/verify-local-navigation.sh` 通过；本地 HTTP + Chrome 验证 `客户中心360.html#/customer` 中“已关联保单”父节点为 `cTags___DWzQw`、class 为 `cTag___SCuoG`、高度为 `18px`、位置为 `static`；验证 `工作台.html#/workspace` Hash 切换到 `#/customer` 后同样加载客户专属 CSS/JS 并保持标签在 `cTags` 行内。
- 来源：当前 Codex 对话

## 登录页视频英雄图测试入口

- 状态：待发布
- 目标：不修改原 `index.html` 和 `登录.html`，新增一个测试入口，用用户附件视频替换登录页左侧英雄图以便预览效果。
- 范围：新增测试 HTML、登录页测试专属 CSS/JS 和项目内视频副本；不修改 `web/` 构建产物和正式入口。
- 实际文件：新增 `index-login-video-test.html`、`features/login-video-test/login-video-test.css`、`features/login-video-test/login-video-test.js`、`assets/login-hero-test/login-hero-preview.mp4`；后续隐藏原 slogan 图片，改为在视频中心渲染粗体文字 `WELCOME GLORY ADVISOR INTELLIGENCE PLATFORM`；主标题和 logo 共用内容层级变量，桌面主标题保持单行显示并使用最粗字重。
- 验证：`node --check features/login-video-test/login-video-test.js` 通过；`./scripts/verify-local-navigation.sh` 通过；本地 HTTP 与 `file://` + Chrome headless 截图确认登录页可打开，右侧登录表单稳定，左侧英雄区加载为视频背景并叠加黑色透明遮罩，原 slogan 图片已隐藏，视频中心粗体文案显示正常，桌面主标题单行展示，测试页 logo 在深色视频上保持可读。
- 来源：当前 Codex 对话

## 新增资讯中心频道

- 状态：待发布
- 目标：新增资讯中心虚拟频道，提供资讯分类、精选筛选、搜索、详情、分享和内容管理演示，并接通工作台资讯入口。
- 实际文件：新增 `资讯中心.html`、`features/news-center/` 页面源码、Mock 与图标；修改频道注册、共享虚拟频道识别、导航、面包屑、区域标记、工作台资讯跳转和所有正式根入口资源版本。
- 排除：为比对参考站保留在本地的 `web/p__news__*` 与 `web/static/bgHeader.*` 不属于本地实现，不进入本次发布。
- 验证：JS 语法、Hash 导航检查和 HTML 资源检查通过；本地浏览器从工作台无刷新进入资讯中心后显示 25 条资讯，直接刷新仍正常。

## 全局 AI 内容提示

- 状态：待发布
- 目标：将 AI 内容声明作为全局共享弹层，在正式根入口和 AI Agent 内共用同一 API。
- 实际文件：新增 `shared/scripts/global-ai-notice.js`、`shared/styles/global-ai-notice.css`；所有正式根 HTML 入口加载共享资源；`AI Agent/AI Agent本地Mock.js` 复用全局 API。
- 验证：共享 JS 通过语法检查；所有正式 HTML 的资源路径存在，本地浏览器加载脚本时无相关错误。

## 全局多选下拉组件预览

- 状态：待发布
- 目标：把活动中心筛选区正在使用的“大号多选下拉 + 已选项折叠计数”样式整理为真实共享组件，并加入全局组件目录预览。
- 范围：`shared/` 下新增共享组件 CSS/JS，更新 `全局组件/` 的注册、调用和预览；不修改活动中心页面和 `web/` 构建产物。
- 参考：活动中心筛选区“产品解决方案中心 / 品牌与市场中心 / +2...”多选器。
- 实际文件：新增 `shared/styles/global-multi-select.css`、`shared/scripts/global-multi-select.js`；更新 `全局组件/index.html`、`components-registry.js`、`components-preview.js`、`components-preview.css`、`README.md`、`PROJECT_STATE.md` 和 `knowledge/公共模块/全局框架与样式.md`。
- 实现：默认 40px 大号控件，前两项标签可见，其余显示 `+ N ...`；支持展开复选、清空、点击外部关闭、Enter/Space/方向键/Esc，并提供 `mount/get/setValue/open/close/destroy` 接口。
- 验证：三个 JavaScript 文件通过 `node --check`；`git diff --check` 与 `./scripts/verify-local-navigation.sh` 通过；资源引用与注册项检查通过。本地 HTTP 浏览器验证预览页可展开 5 个选项、4 个默认选中项正确折叠，清空后选中数归零，Esc 可关闭下拉；同页全局 AI 提示弹层正常打开。
- 来源：当前 Codex 对话中的浏览器标注

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
