---
type: current-changes
updated: 2026-09-01
---

# 当前未发布变更

## 财富值中心我的财富值背景图替换

- 状态：本地已完成并验证，未提交/发布。
- 目标：替换“我的财富值”顶部概览区背景图，使其更贴合财富值中心频道内容，并沿用 `codex://threads/01a040cb-1fd5-77a0-9eec-b2695ee3dbaa` 的 Lovart 营销活动生成器蒸馏流程和 MKV01 风格。
- 实现：当前按用户提供的 `2500280795_huge.jpg` 原图替换概览背景，复制为 `assets/wealth-center/wealth-overview-user-wide-20260901.jpg` 后直接以 `center / cover` 引用，不重新生成、不调色。原图尺寸 `3416×420`，本身为超宽横幅，绿色信封和木质区域居中显示。概览高度继续为 `236px`，构成卡片继续使用 90% 白色半透底与轻阴影。概览左侧两组财富值指标之间保留 `44px` 高短竖线，两侧间距各为常规大屏 `56px`、`1500px` 以下 `44px`。“我的财富值”明细表头为 `#F8F5F3`。精细胡桃木+玻璃版本及信封试用图保留但不再引用。
- 范围：新增 `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`；修改 `features/wealth-center/wealth-center.css`、`shared/config/channels.js`、16 个根入口频道配置缓存号及相关说明。不改财富值 Mock、表格、筛选、导航交互或 `web/` 构建产物。
- 版本：财富值 CSS 为 `20260901-19`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：本地背景尺寸为 `3416×420`，页面仅引用 `wealth-overview-user-wide-20260901.jpg`；财富值样式已登记为 `20260901-19`，16 个根入口均加载频道注册表 `20260901-48`；`node --check`、频道导航静态检查和 `git diff --check` 通过。自动刷新 `file://` 页面受浏览器安全策略限制，不绕过策略执行截图验证。

## 配置中心操作日志导出与顶部适配

- 状态：本地已完成并验证，未提交/发布。
- 目标：配置中心“操作日志”二级页右上角的导出按钮使用绿底白字/白图标并复用正确的组织页下载图标，同时把日志表头统一为业务表格样式；顶部频道 Tab 后续增加时，搜索成员框继续收缩并保留操作日志入口。
- 实现：配置中心内联日志页右上角 `[data-log-export]` 保持 `147×48px`，改为 `#025B52` 绿底与白色文字/图标，hover 和键盘聚焦加深至 `#155347`。挂载日志页后直接从 `source.toolbar` 的组织架构“导出Excel”按钮克隆完整 `.ant-btn-icon`，替换共享日志原图片图标并统一文字；共享日志弹窗及其他频道不变。日志表头改为 `#F5F5F5` 底色、`#2F3640` 文字、`14px/500` 字体和 `10px 16px` 内边距。顶部 Tab 保持完整展示且不产生滚动容器，搜索框改为 `flex:1 1 333px`，在新增 Tab 或空间不足时由 `333px` 收缩至 `120px`；右侧操作日志入口禁止收缩。成员搜索初始为空且未聚焦时隐藏清除图标，聚焦或存在内容时显示，清空并离开焦点后再次隐藏。
- 范围：调整配置中心外壳/内容 CSS、配置中心回归、缓存版本与说明；不改操作日志共享 DOM/脚本、主导航、顶部全局栏、AI Agent 或 `web/`。
- 版本：配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：配置中心回归逐节点比对日志按钮与组织页下载图标，确认原图片图标已移除，并覆盖日志按钮尺寸/绿底白字/hover、日志表头底色/文字/字号/字重、搜索 `120px` 下限、Tab 无滚动容器、清除图标四种状态和右侧入口不收缩；共享操作日志、主导航、AI Agent、Hash 导航、语法和 diff 检查通过。

## 配置中心成员弹窗固定头尾

- 状态：本地已完成并验证，未提交/发布。
- 目标：添加/编辑成员弹窗固定标题关闭区和底部操作区，仅让中间表单内容滚动，与项目其他长内容弹窗保持一致。
- 实现：成员弹窗内容改为纵向 Flex，标题区固定不收缩；把原本位于 `.ant-modal-body` 内的 `.footer___UhMLM` 移到 `.ant-modal-content` 直属底部，作为不收缩的独立操作区。中间 `.ant-modal-body` 单独 `overflow-y:auto`，使用 `6px` 低对比度滚动条并限制滚动链；弹窗外框隐藏自身滚动，关闭按钮继续固定在内容层右上角。
- 范围：调整 `features/config-center/config-center.js`、`config-center-content.css`、`scripts/test-config-center.cjs`、缓存版本和相关说明；不改设置管理员弹窗、部门弹窗、角色交互、主导航、顶部栏、AI Agent 或 `web/`。
- 版本：配置中心内容 CSS 为 `20260901-18`，交互 JS 为 `20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：配置中心回归确认底部按钮区已成为内容层直属节点且不在滚动 Body 内，标题、Body、底部的固定/滚动 Flex 规则齐全；添加/编辑字段、角色同步、主导航、AI Agent、Hash 导航、语法和 diff 检查通过。

## 配置中心线索字段对齐修正

- 状态：本地已完成并验证，未提交/发布。
- 目标：撤回上一轮误改的管理员排列，只让“线索”模块完全复用“转介绍人”的三项布局、控件内边距和左对齐方式。
- 实现：管理员模块恢复 `112px` 标签列、右侧两列及原有间距；删除线索模块单独的 Grid 和居中规则，使 `.clueRoleOptions___Y4mLs` 只继承与 `#referrerType` 相同的 `.typeGroup___cnCo5` flex、`16px` 间距、等宽选项、`12px 24px` 内边距和默认左对齐。
- 范围：仅调整 `features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、缓存版本和相关说明；不改字段、角色交互、主导航、顶部栏、AI Agent 或 `web/`。
- 版本：配置中心内容 CSS 当前为 `20260901-18`，交互 JS 当前为 `20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：配置中心回归确认线索模块不存在独立 Grid 对齐覆盖，字段顺序、保存/回显、佣金唯一性与线索互斥保持通过；主导航、AI Agent、Hash 导航、语法和 diff 检查通过。

## 配置中心成员编辑角色字段

- 状态：本地已完成并验证，未提交/发布。
- 目标：在添加成员和编辑成员弹窗中补齐管理员与线索角色字段；管理员固定展示“组织架构管理员（人管）”“上级佣金归属人（佣金）”，线索作为独立模块放在转介绍人下方，展示“无”“线索管理员”“线索跟进人”。
- 实现：管理员模块采用独立浅灰行，左侧标签为“管理员”，右侧两个项目复选框固定按人管、佣金排序，桌面宽度下文字保持单行；窄屏时选项改为纵向排列但单项文字仍不换行。线索模块紧跟转介绍人，以三个等宽项目单选项和一行权限说明展示。添加时默认“无”，编辑时从当前部门角色状态回显；保存会同步更新组织管理员、佣金和线索角色，持牌地区只读取原地区复选组，不会混入新增角色文字。
- 数据规则：两处入口共用 `adminRoleAssignments` 与成员 `admin` 状态。佣金已有其他归属人时当前成员的佣金项禁用；线索管理员与线索跟进人用单选自然互斥，并继续受现有超级管理员/人管/线索管理员权限矩阵约束。
- 范围：仅调整 `features/config-center/config-center.js`、`config-center-content.css`、`scripts/test-config-center.cjs`、缓存版本和相关说明；不修改主导航、顶部栏、AI Agent 或 `web/`。
- 版本：配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：添加成员弹窗的字段顺序、固定文案、默认“无”和线索紧邻转介绍人已覆盖；编辑成员中选择佣金和线索跟进人后，“设置管理员”弹窗能同步回显，反向清除后编辑弹窗恢复未选/“无”。配置中心完整交互、语法检查、主导航、AI Agent 与 Hash 导航回归通过。

## 配置中心管理员长成员列表滚动 Mock

- 状态：本地已完成并验证，未提交/发布。
- 目标：在本地组织根节点补足长成员列表，验证“组织架构管理员”人数较多时的浏览体验；滚动限制在右侧成员列表内，左侧角色 Tab、当前角色说明和底部操作区保持固定。
- 实现：本地组织根节点由 8 人扩为 24 人，沿用既有成员行、地区、持牌与转介绍标签结构；新增成员使用虚构姓名、账号和联系方式，且不复制原有管理员身份。表格继续每页显示 10 人，管理员弹窗右侧成员区在固定工作区内独立滚动；`6px` 滚动条默认 18% 中性灰，hover 时为 30%，并通过 `overscroll-behavior: contain` 防止滚动传递到弹窗外层。
- 范围：仅调整 `features/config-center/config-center.js` 的本地 Mock、`scripts/test-config-center.cjs`、缓存版本与相关说明；不修改主导航、顶部栏、AI Agent 或 `web/` 原站基线。
- 版本：配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：配置中心 DOM 回归确认表格为 24 条、每页 10 条、共 3 页，组织管理员列表为 24 人并包含“示例成员24”；滚动容器、固定工作区、细滚动条及 hover 颜色规则均已覆盖。主导航、AI Agent、Hash 导航和代码语法回归通过。应用浏览器的安全策略禁止自动读取 `file://` 标签页，最终视觉滚动待用户在既有本地预览刷新后确认。

## 配置中心管理员弹窗角色化改版

- 状态：本地已完成并验证，未提交/发布。
- 目标：参考用户截图，将原有单列管理员复选列表改为说明区、四类角色分组、成员选择卡片和固定底部操作区；字段采用“组织架构管理员（人管）”“上级佣金归属人（佣金）”“线索管理员”“线索跟进人”。
- 交互判断：组织架构管理员、线索管理员和线索跟进人允许多选，上级佣金归属人允许 0 或 1 人；佣金组使用可取消的复选框视觉，选中后其他成员禁用，再次点击当前成员即可清空并恢复全部选项。左侧四个竖向 Tab 固定显示实时已选人数，右侧只展示当前角色的规则与成员列表；上下方向键、Home、End 支持键盘切换 Tab。
- 附件规则：依据 `/Users/hhp/Downloads/【R】GAIP-组织架构多管理员角色`，同一成员可同时具备人管、佣金和一项线索身份；线索管理员与线索跟进人互斥；上级佣金归属人允许 0 或 1 人。超管可配置全部角色，人管管理员只可配置人管/佣金，线索管理员只可配置可见范围内的线索跟进人。本地预览采用超级管理员权限视角，四组均可编辑，代码保留三类权限矩阵。
- 实现：弹窗最大宽度收敛为 `900px`，采用标题、规则说明与节点路径、左侧 `220px` 竖向角色 Tab、右侧单一成员面板和固定底部操作区。Tab 仅保留角色名称与次级“已选 x 人”；当前项使用通栏白底、黑色主文字和贯穿整行的 `#24D4C9` 左竖线。右侧面板不再重复显示角色标题；顶部只保留规则说明与约束标签，高度按内容自适应并使用上下 `16px` padding。成员选择直接使用项目现有 Ant 复选框 DOM 与变量样式；佣金组删除独立“清除选择”按钮，改为最多勾选 1 人，已选成员保持可取消、其他成员进入禁用态。两类线索身份仍自动互斥。
- 数据边界：组织架构管理员继续复用既有 `member.admin` 与成员表格管理员标签；其余三类角色增加当前会话内、按渠道和部门隔离的本地分配状态，刷新重置，不接入线上权限、佣金、线索或操作日志系统。
- 版本：配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：DOM 与实际点击确认佣金组全部为项目复选框且不存在清除按钮。初始 24 人均可选；选中一人后仅该成员保持可取消，其余 23 人禁用，“已选 1 人”同步更新；再次点击后已选归零且 24 人全部恢复。取消回滚、保存恢复、线索角色互斥、空部门禁用、主导航和 Hash 导航回归均通过。

## 配置中心表格工具栏设置管理员入口

- 状态：本地已完成并验证，未提交/发布。
- 目标：在成员表格上方左侧新增“设置管理员”按钮，沿用右侧“导出Excel”按钮的尺寸、字体和图文排布，并与表格左边缘、右侧操作按钮处于同一行；根据后续反馈改为黑底、白色文字和白色图标。
- 复用边界：按钮图标直接克隆部门“⋮”菜单中既有的“设置管理员”原始 SVG；点击继续调用既有部门管理员编辑入口和同一弹窗，不复制图标路径、弹窗模板或管理员业务逻辑。只调整配置中心主体内容与交互，不修改主导航、顶部栏、AI Agent 或 `web/` 原站基线。
- 实现：从现有“导出Excel”节点克隆按钮结构，替换为直接从 `source.departmentUi.menu` 的管理员菜单项克隆出的 SVG，并将按钮放在工具栏首项；按钮背景/边框为 `#2F3640`，文字和 SVG 描边通过 `currentColor` 统一为白色，hover/键盘聚焦时使用 `#464C55`，按下时使用 `#20262E`。工具栏保持 `space-between`，右侧导出/添加组合不变。新入口把当前 `state.department` 传入既有 `editDepartment(id, 'admin', trigger)`，因此继续使用同一弹窗模板、当前部门成员和保存逻辑。
- 版本：配置中心外壳/内容/模板/JS 当前为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：DOM 回归确认新按钮与导出按钮结构一致、SVG 与部门菜单原图标逐节点一致、工具栏左右 DOM 顺序正确，且两种入口生成的管理员弹窗内容一致。1326×1035 实际渲染中，新按钮和导出按钮均为 `147×48px`、顶部均为 `145px`，新按钮与表格左边均为 `600px`；后续颜色调整确认按钮为 `#2F3640` 黑底，文字与图标为白色。点击继续打开既有“设置管理员”弹窗。配置中心、可展开主导航、AI 入口与 Hash 导航回归均通过；未修改主导航、顶部栏、AI Agent 或 `web/`。

## 配置中心内容样式分层

- 状态：本地已完成并验证，未提交/发布。
- 目标：将配置中心主体内容样式从主导航、顶部栏和页面挂载骨架中抽离，后续调整组织树、表格、工具栏、弹窗和操作日志时不再触碰导航与顶部样式。
- 范围：保留 `organization-source.css` 作为原站样式快照；`config-center.css` 只负责主导航、顶部栏、顶部搜索和挂载骨架；新增 `config-center-content.css` 并通过频道注册表加载。同步配置中心回归、资源缓存和页面/模块文档，不修改 `web/`。
- 实现：从原站快照的 231 条规则中抽出 202 条内容规则，连同本地组织树、工具栏、表格、标签、弹窗、部门菜单和内嵌操作日志覆盖统一放入 `config-center-content.css`；余下 29 条页面顶部、Tab、日志按钮和搜索组件基线进入外壳 `config-center.css`。运行时不再加载未拆分的 `organization-source.css`，该文件仅作为逐规则对照快照保留。
- 边界：内容文件不含 `.gaip-config-menu`、`.header___Vhyog`、`.tab___UxqK9`、顶部搜索和顶部日志按钮；外壳文件不含组织侧栏、主体工具栏、成员表格和编辑弹窗规则。Ant 运行时基线仍独立由 `ant-source.css` 提供。
- 版本：本条拆分完成时内容/外壳 CSS 为 `20260901-5`/`20260901-14`；当前配置中心内容/外壳/模板/JS 已更新至 `20260901-18`/`20260901-16`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：DOM 回归新增样式职责边界、完整内容规则和资源登记断言。实际渲染只加载 Ant 基线、内容 CSS 和外壳 CSS；组织页顶部仍为 `72px`，搜索框、`348px` 组织侧栏、表格表头和 `20px/12px` 标签计算值与拆分前一致；操作日志内容页为无边框 `flex` 容器，控制台无错误。配置中心回归、可展开主导航回归和 Hash 导航检查通过。

## 配置中心保留全局 AI Agent 入口

- 状态：本地已完成并验证，未提交/发布。
- 问题：配置中心虚拟页此前将整个 `.ant-pro-layout-content` 设为 `inert`/`aria-hidden`。全局 AI Agent 悬浮入口也是该节点的直接子项，因此视觉上存在但被连带禁用，并被配置中心内容层挡住点击。
- 修复：配置中心页面改为挂载在主内容节点内，只对被覆盖的原业务内容子项设置 `inert`/`aria-hidden`；明确排除原有 `[class*="globalButton___"]` / `ai-assistant-entry` 节点。离开配置中心时逐项恢复原状态。未移动、复制或修改 AI Agent 节点，也未修改 `AI Agent/` 下的脚本、CSS、素材和版本逻辑。
- 版本：AI Agent 修复完成时配置中心 JS 为 `20260901-11`；当前配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：回归覆盖配置页挂载位置、底层内容禁用与恢复、AI 入口不继承 `inert`/`aria-hidden`、节点不移动和点击事件保留。实际页面命中测试确认入口位于最上层，点击后成功打开原有 `agentModal___Nxp06`；AI Agent 文件无代码改动，临时测试页和服务已关闭。

## 本地预览语义约定

- 用户确认“本地”始终指项目文件及既有 `file://` 预览，不代表需要启动或交付 `127.0.0.1` 地址。
- 已将该约定写入根目录 `AGENTS.md`：HTTP 服务仅可作为受限情况下的临时自动化测试手段，测试后关闭，且不替换用户已有标签页。

## 主导航项目级契约固化

- 原因：用户确认新增频道时反复出现父项误跳转、父子当前态颠倒、hover 缺失、图标消失、文字错位和箭头位置漂移，要求作为全项目规则长期保留。
- 规则：`AGENTS.md` 新增主导航强制契约；`knowledge/公共模块/主导航与Hash路由.md` 固化叶子项/父项/二级项点击边界、`is-open` 与 `is-current` 分离、父子当前态归属、40px/18px/13px/55px/16px 几何值、原始图标复用、键盘与 ARIA、跨频道监听排除项及必跑回归。
- 边界：本次只补项目规则和知识库，不改运行时代码；后续新增或修改任何频道导航都必须先复用该契约。

## 主导航滚动条与底图层级

- 状态：本地已完成并验证，未提交/发布。
- 目标：修复主导航变长后滚动条被百宝箱底图遮挡、距离侧栏右边过远和默认颜色过重的问题，并作为全部频道共享规则。
- 范围：只调整 `shared/scripts/channel-regions.js`、`shared/styles/channel-foundation.css`、根入口缓存版本及主导航公共文档；不修改 `web/` 和频道页面内容。
- 实现：区域脚本为菜单直接滚动父层添加 `.gaip-sidebar-nav-scroll`；滚动层 `z-index: 1`，百宝箱 footer 为 `z-index: 0`，底图继续上延但不再覆盖滚动条。滚动层用 `-8px` 右外边距贴到侧栏边缘、`12px` 右内边距保持菜单内容宽度；thumb 宽 `6px`，默认/hover/active 分别为 14%/28%/40% 中性灰。
- 版本：16 个根入口统一加载 `channel-foundation.css?v=20260901-5` 与 `channel-regions.js?v=20260901-1`。
- 验证：DOM 回归确认共享滚动层和百宝箱层级标记；实际渲染实测侧栏/滚动层右边均为 `212px`，菜单右边保持 `189px`，图标/文字左边保持 `36px`/`67px`；滚动层计算值为 `scrollbar-width: thin`、默认色 `rgba(47,54,64,.14)`，滚到最底部时 thumb 仍显示在底图之上。导航 Hash 回归继续通过。

## 主导航可展开父项统一

- 状态：本地已完成并验证，未提交/发布。
- 目标：统一“财富值中心”和“配置中心”父级导航的展开收起交互、箭头位置与旋转动效；父级点击只切换子导航，不改变频道或 Hash。
- 修复：配置中心默认态直接引用线上原始 SVG 文件；当前态直接对同一 `<img>` 使用 CSS 滤镜改色，不另画图形，也不依赖外部 SVG 蒙版，并使父项文字、图标同步变为品牌绿。补回 4px 左边框占位、统一 13px 图文间距和二级项 55px 左内边距，使配置中心、财富值中心与普通频道严格对齐；两类父项当前态都只改变文字、图标和箭头颜色，保持透明背景，绿色浅底与左边框仅属于当前二级项，父项 hover 使用灰色底；箭头固定在父项右侧并垂直居中；财富值子导航处于当前态时点击配置中心父级，不再被误判为离开财富值频道并跳往其他频道。
- 范围：`shared/styles/channel-foundation.css` 新增共享父项契约；修改 `features/wealth-center/wealth-nav.js`、`wealth-center.css`、`features/config-center/config-center.js`、`config-center.css`，新增 `scripts/test-expandable-main-nav.cjs`。财富值资源版本为 `20260901-7`，全局框架当前为 `20260901-7`；配置中心当前外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，全部根入口的频道注册表缓存为 `20260901-48`；不改业务页面数据，不修改 `web/`。
- 行为：两类父项使用与普通频道相同的 18px 图标列、13px 图文间距、4px 左边框占位、40px 行高和右侧 16px 箭头位置；单一向下箭头按开合旋转 180°/180ms，减少动态偏好下取消动画。鼠标、Enter/空格只切换子项，Esc 收起；二级项才改变频道。两个频道的离开监听统一忽略父项切换，修复财富值子项当前态点击配置中心时误退到其他频道的问题。
- 线上图标证据：从 `https://glory-gaip-web.t3.test.noahgrouptest.com/web/umi.82bdc605.js` 提取 `MenuIconConfig` 同段 data URI；解码后与 `features/config-center/assets/organization.svg` 均为 989 字节，SHA-256 均为 `1a4d8a89c5db7291afe6aa59cf9078e1715416700698d5aeb9a2198b896d9c59`，逐字节相同。默认态和当前态都保留同一个 `<img>`；当前态只用 CSS 滤镜改色，JS 不包含重画 path，也不使用外部 SVG 蒙版。
- 验证：新增 DOM 回归覆盖线上 SVG 指纹、无 JS 手绘 path、共享结构、父项 Hash 不变、财富值/配置中心双向隔离、双父级当前态、灰色 hover、55px 二级缩进、键盘开合和二级真实导航；配置中心既有部门菜单回归、导航静态检查和 JS 语法均通过。本地 HTTP 实测普通“工作台总览”、财富值中心、配置中心三者图标左边均为 `36px`、文字及二级项左边均为 `67px`，图标/文字/父项中心线完全一致；配置图标当前态随文字变为 `#025b52`，两类父项当前态均为透明背景和品牌绿前景，绿色浅底与 4px 左边框只出现在当前二级项。此前两条跨频道路径、固定箭头和控制台检查继续通过。
- 边界：自动浏览器仍不访问用户当前 `file://` 标签；代码与入口缓存已更新，用户刷新本地文件页即可加载。本轮未提交、未发布。

## 配置中心组织页默认树与控件一致性

- 状态：本地已完成并验证，未提交/发布。
- 目标：组织树首次进入只展开根渠道并展示一级部门；统一“导出Excel”与“添加成员”的文字/图标尺寸；移除表格位于最左端时错误出现的左侧滚动阴影；将渠道 Tab 当前态下横线统一为其他频道采用的尺寸。
- 范围：仅调整 `features/config-center/config-center.js`、`config-center.css`、配置中心回归、资源缓存版本及对应页面/状态文档；不修改 `web/` 原站基线和本地示例数据。
- 实现：部门折叠状态未被用户改动时，根渠道保持展开，所有含下级的一级部门默认收起，因此首次进入只展示一级；展开后继续保留原有多层树交互。表格根据真实 `scrollLeft` 同步 Ant 的 `ant-table-ping-left/right`，位于最左端时不再错误显示左阴影，向右滚动后仍保留位置提示。导出/添加按钮的文字与 `1em` 图标统一为 `16px`；渠道 Tab 当前态下横线从 `32×3px` 调整为与线上产品中心一致的 `71×4px`。
- 版本：本条完成时配置中心 CSS/JS 为 `20260901-7`；当前配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。源 DOM 与 `web/` 均未修改，原站 CSS 快照保留为对照文件。
- 验证：配置中心 DOM 回归新增默认树层级、一级手动展开/收起、表格初始无左 ping 及视觉数值断言。实际渲染确认默认可见项仅为根渠道和一级部门，两个按钮的文字/图标均为 `16px`，Tab 下横线为 `71×4px`，表格初始 `pingLeft=false` 且伪元素无阴影；横向滚动后 `pingLeft=true` 并恢复 Ant 阴影。对照线上产品中心当前态下横线计算值同为 `71×4px`。

## 配置中心搜索布局与表格标签精简

- 状态：本地已完成并验证，未提交/发布。
- 目标：按用户红框参考图将成员搜索框移入页面顶部频道栏，位于“操作日志”左侧；操作按钮放在下一排右侧，并让搜索区在可用宽度不足时收缩。只缩小表格内身份/地区/转介绍人标签的尺寸，不改变标签颜色与内容。
- 范围：配置中心专属 CSS、视觉数值回归、资源缓存号与页面/状态文档；不改组织数据、业务交互、原站 `web/` 基线或其他频道。
- 实现：渲染组织页后将原工具栏内的搜索节点移动到顶部 `.header___Vhyog`，DOM 顺序为“频道 Tab → 搜索 → 操作日志”。搜索框最大为 `333×72px`，使用 `flex:0 1 333px` 和 `200px` 最小宽度按可用空间收缩，与操作日志间距 `24px`；搜索输入仅保留左右 `1px` 边框，上下边框为 `0`，默认底色为 `rgba(47,54,64,.03)`。hover、点击聚焦、输入与清空时边框和底色均保持默认值，不再变为品牌绿；右侧清除图标在空内容、输入后和清空后持续显示并保持可点击。导出/添加按钮保持 `147×48px` 并在下一排右对齐。同时移除配置页原有 `1228px` 页面级最小宽度，让页面随侧栏后的可用宽度收缩，表格继续在自己的滚动容器内横向滚动。管理员、持牌/不持牌、地区和转介绍人标签统一从 `24px/14px/12px` 内边距缩至 `20px/12px/8px`，持牌图标从 `14px` 缩至 `12px`；颜色、圆角、图标和文字均保持原样。
- 版本：当前配置中心外壳/内容/模板/JS 为 `20260901-16`/`20260901-18`/`20260901-7`/`20260901-23`，16 个根入口统一加载 `shared/config/channels.js?v=20260901-48`。
- 验证：1280×720 实际渲染中配置页宽度为可用的 `1068px` 且页面级横向溢出为 `0`；顶部栏为 `72px`，搜索框位于频道 Tab 之后、操作日志之前，宽 `285.32px`、高 `72px`，与操作日志间距 `24px`；宽窗口最大为 `333px`。搜索框上下边框计算值为 `0px`、左右边框为 `1px`，底色为 `rgba(47, 54, 64, 0.03)`；默认、hover、聚焦、输入和清空后的左右边框颜色均为 `rgba(47,54,64,.12)`。清除图标在空内容、输入后和清空后均为可见，清空后输入值恢复为空。按钮下一行完整可见，表格继续内部横向滚动。管理员/持牌/地区标签计算值均为 `12px` 字号、`20px` 高、`0 8px` 内边距，持牌图标为 `12px`。配置中心 DOM 回归、导航回归与资源引用检查通过。

## 配置中心部门菜单与日志按钮修复

- 状态：本地已完成，未提交/发布。
- 目标：补齐组织树“⋮”的原站菜单及本地交互，修复组织页右上操作日志图标与文字错位。
- 范围：配置中心专属脚本、样式及所需源模板，缓存版本和相关页面笔记；不更改线上组织数据。
- 实际文件：`features/config-center/config-center.js`、`config-center.css`、`source-markup.js`；新增 `scripts/test-config-center.cjs`。`shared/config/channels.js` 更新专属脚本/CSS 版本至 `20260831-3`、模板至 `20260831-2`；根入口的频道配置缓存统一为 `20260831-4`（14 个既有正式入口、`配置中心.html` 及保留不发布的 `index-login-video-test.html`）。同步本记录、`PROJECT_STATE.md`、`knowledge/页面/配置中心.md`、`knowledge/公共模块/频道资源加载.md` 和 `design-qa.md`。
- 行为：原站根部门菜单为添加子部门/设置管理员，子部门另含修改名称/删除。复用原菜单图标和表单模板，本地新增/修改部门按渠道隔离，管理员选择与成员编辑共用数据；删除需确认，非空部门禁止删除。菜单支持外部点击、Esc、滚动/导航关闭，点击“⋮”不再误触部门选择。日志按钮保留原 SVG 路径，补齐 viewBox 和图标容器居中规则。
- 验证：DOM 集成回归覆盖菜单关闭、空名/重名校验、增改删、非空保护、管理员保存/取消、渠道隔离和导航清理；原日志回归、导航静态检查、JS 语法及 diff 检查通过。浏览器验证工作台 → 配置中心 → 资讯、客户入口 → 配置中心 → 产品，主文档标记不变；菜单尺寸与原站均为 126×72px，日志图标/文字中心线均为 92.5px。原站菜单与本地截图已并排检查。
- 边界：自动浏览器禁止访问 `file://` URL，未绕过该限制；本地运行检查使用已有 HTTP 预览服务，入口引用和文件协议下的脚本交互另由 DOM 回归覆盖，不声称已完成 file 页面的真实浏览器验证。全部业务修改仅影响本地会话数据，刷新重置；未修改线上组织或权限。

> 这是协作清单，不代替 `git status` 或 `git diff`。本轮正式修改已发布，见 [[2026-08-31-工作台对齐与共享控件修复]]；此前发布见 [[2026-08-31-工作台与共享组件同步]]。

## 登录页视频英雄图测试入口

- 状态：待发布（用户明确本次不同步）
- 目标：不修改原 `index.html` 和 `登录.html`，用独立测试入口预览登录页左侧视频英雄图。
- 文件：`index-login-video-test.html`、`features/login-video-test/login-video-test.css`、`features/login-video-test/login-video-test.js`、`assets/login-hero-test/login-hero-preview.mp4`。
- 验证：JS 语法、导航检查及本地预览已通过；等待用户另行确认是否发布。

## 本地试验与参考文件

- 状态：暂停，不发布
- AI Agent：`creative-8/9` 试验图片与纹理、`AI Agent/素材/备份.mp4`。
- 资讯中心参考站资源：`web/p__news__index.f64a0a2e.async.js`。
- 原因：这些文件不是当前正式页面运行依赖，保留本地用于比较或回退。

## 移除全局顶栏操作日志入口

- 状态：已完成本地修改与代码回归，待视觉确认，未发布
- 目标：移除用户标注的全局顶栏“操作日志”按钮；保留配置中心的“操作日志”二级页及组织页日志按钮所复用的日志组件。
- 范围：共享日志脚本/样式、顶栏共享样式、入口缓存版本、日志测试、全局框架笔记与 `PROJECT_STATE.md`。不删除配置中心频道、日志 Mock、Excel 导出或内嵌日志能力。
- 文件：`shared/scripts/global-operation-log.js`、`shared/styles/global-operation-log.css`、`shared/styles/channel-foundation.css`、`scripts/test-operation-log.cjs`，删除仅供顶栏入口使用的 `shared/assets/operation-log.svg`；16 个根入口统一更新日志 CSS/JS 为 `20260901-1`、全局框架 CSS 为 `20260901-6`；同步本记录、[[../公共模块/全局框架与样式|全局框架与样式]]、[[../公共模块/频道资源加载|频道资源加载]] 和 `PROJECT_STATE.md`。
- 实现：共享日志不再创建或监听 `.gaip-log-trigger`，初始化时清理旧缓存遗留按钮；保留 `show()` 供配置中心组织页按钮打开弹窗，保留 `mount(host)`/`destroy()` 供“操作日志”二级页挂载。顶栏日期、通高分割线、用户区及其间距不变。
- 验证：日志回归通过，覆盖顶栏无入口、旧入口清理、弹窗控制器、内嵌页面、筛选/分页/展开/XLSX 与全部入口版本；配置中心日志按钮/日志二级页专项集成、可展开主导航测试、导航静态检查、JS 语法及 `git diff --check` 通过。配置中心完整测试当前停在本任务未涉及的管理员说明文案断言，未修改该区域；浏览器实时视觉仍待刷新确认。

## 顶栏用户区分割线缩短

- 状态：已完成，待刷新确认
- 目标：将日期与用户区之间的分割线从通栏改为与文字行高一致的 20px；保留颜色、左右留白和垂直居中。
- 文件：`shared/styles/channel-foundation.css` 将用户区伪元素高度由 56px 顶栏高度改为 20px；16 个根入口统一将框架 CSS 缓存更新为 `20260901-7`；同步 `scripts/test-operation-log.cjs`、`PROJECT_STATE.md`、[[../公共模块/全局框架与样式|全局框架与样式]]、[[../公共模块/频道资源加载|频道资源加载]]。
- 验证：日志回归通过；本地导航校验通过；16/16 根入口版本一致；`git diff --check` 通过。原生弹窗焦点、实际 Umi 过渡与浏览器视觉仍需刷新页面人工确认。

## 全局弹窗独立铺开预览（序列 1）

- 状态：已被下方“全项目弹层盘点口径修正”纠正；本条 3 个仅代表共享层子集
- 目标：盘点当前共享层弹窗，并新建一个可直接通过 `file://` 打开的单 HTML 铺开预览页；预览使用真实共享组件 DOM/API 与共享 CSS，后续确认的弹窗样式修改仍回写共享源码并同步业务引用。
- 盘点：共享层共 3 个弹窗——AI 内容重要提示、文章海报分享、操作日志。AI Agent 主面板属于全局业务面板；活动报名、配置中心成员/部门、方案附件和财富值抽屉等属于频道专属界面，不计入共享弹窗数量。
- 文件：新增 `全局组件/弹窗预览.html`；画布结构与铺开覆盖内嵌在单 HTML，弹窗本体分别调用 `shared/scripts/global-ai-notice.js`、`global-poster-share.js`、`global-operation-log.js`，并直接加载对应共享 CSS、日志 Mock/导出器及真实海报 iframe。同步 `全局组件/README.md`、[[../公共模块/全局框架与样式|全局框架与样式]] 和 `PROJECT_STATE.md`。
- 边界：本序列不修改现有弹窗视觉和业务页面，也不把入口登记到 `全局组件/index.html`；后续样式调整必须回写对应 `shared/styles/global-*.css`，不可只改预览画布覆盖。
- 验证：静态 DOM 加载得到 3 个展示区、3 个真实弹窗容器、操作日志关闭按钮及 10 行 Mock 数据；内联脚本语法和 11 个本地资源引用检查通过，`git diff --check` 通过。自动浏览器安全策略禁止访问 `file://`，未改用 localhost 规避，真实视觉待用户双击打开确认。

## 全项目弹层盘点口径修正

- 状态：已完成代码与静态 DOM 检查，待 `file://` 刷新确认
- 原因：上一轮错误地把“全局中有多少弹窗”收窄为 `shared/` 共享层，只展示 3 个，遗漏 AI Agent 与各频道由本地源码维护的弹窗/抽屉。
- 目标：按“当前本地项目由我们维护、运行时能够出现的弹层样式类型”重新盘点并扩充 `全局组件/弹窗预览.html`；相同结构的新增/编辑合并，确认框、复杂表单、抽屉分别计算。预览继续直接加载各共享/频道真实 CSS，后续样式修改回写对应源码。
- 范围：弹窗预览 HTML、涉及弹层的共享/频道说明与项目状态；不修改业务弹窗 CSS/JS、频道交互、主导航或只读 `web/` 构建包。本轮仍不执行序列 2 的全局组件目录登记。
- 盘点：共 15 类——共享弹窗 3 类（AI 内容提示、文章海报分享、操作日志），AI Agent 1 类，方案中心 3 类（客户归属、解除关联确认、附件预览），活动中心 1 类，财富值中心 2 类（识别详情抽屉、保司关键词），资讯中心 1 类，配置中心 4 类（成员编辑、部门名称编辑、删除确认、设置管理员）。相同结构的新增/编辑合并；只读 `web/` 构建包中无本地维护源的内部弹窗不纳入可同步预览。
- 文件：扩充 `全局组件/弹窗预览.html` 为 15 个铺开展示区；本条最初仍复制了部分频道弹层 DOM，已由下方“弹窗预览改为真实工厂引用”继续纠正。当前版本不再沿用复制结构。
- 验证：静态 DOM 加载确认 15 个展示区、3 个共享组件、10 行日志数据、4 个配置中心模板和 12 个管理员示例成员卡均已挂载；内联脚本、33 个本地资源引用、共享日志回归、本地导航及差异格式检查通过。另用本机无头 Chrome 直接加载 `file://` 检查页面顶部与配置中心成员弹窗，真实字体、遮罩、表单和运行时角色字段均正常；应用内自动浏览器仍受 `file://` 控制策略限制，用户当前标签页需刷新查看。

## 弹窗预览改为真实工厂引用

- 状态：已完成代码、回归与 `file://` 视觉检查，待用户刷新确认
- 原因：现有预览只有共享弹窗和配置模板使用真实引用，方案、活动、财富值、资讯等频道弹层仍在预览 HTML 中复制 DOM，其他任务修改真实结构后不会自动同步。
- 目标：将本地维护弹层的结构生成收口到各自功能脚本并暴露只读预览工厂；`全局组件/弹窗预览.html` 只调用真实工厂、加载真实 CSS 和提供文档流画布，不再保存频道弹窗 DOM 副本。
- 范围：弹窗预览页与相关频道脚本的纯渲染入口；不改变正式页面交互、数据、路由、主导航、弹窗视觉或只读 `web/` 构建包。
- 实现：共享 3 类继续调用 `global-ai-notice.js`、`global-poster-share.js`、`global-operation-log.js` 的真实 API；AI Agent 调用 `AI Agent本地Mock.js` 的预览工厂；方案中心、活动中心、财富值中心、资讯中心分别调用各自功能脚本暴露的真实渲染工厂；配置中心把成员/部门对话框结构收口到 `source-markup.js` 的统一工厂，正式页面和预览共同调用。`弹窗预览.html` 中对应展示区均为空挂载容器，只传入示例数据，不保存弹窗 DOM 副本。
- 文件：`全局组件/弹窗预览.html`、`AI Agent/AI Agent本地Mock.js`、`features/proposal-center/proposal-center.js`、`features/activity/activity-sync.js`、`features/wealth-center/wealth-center.js`、`features/news-center/news-center.js`、`features/config-center/source-markup.js`、`features/config-center/config-center.js`；同步频道资源版本、16 个根入口缓存和相关说明。
- 验证：脚本语法、配置中心、AI Agent 入口、共享操作日志、Hash 本地导航和 `git diff --check` 均通过；动态加载确认 15 个展示区及 8 个频道工厂挂载成功，操作日志 10 行数据正常；无头 Chrome 直接打开 `file://` 并定位方案中心客户归属弹窗，显示真实工厂的 6 条客户数据，证明不再使用旧复制结构。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。

## 全频道及子页面弹层完整盘点

- 状态：已完成代码与离线 DOM 验证，待用户刷新 `file://` 预览确认视觉；未发布
- 原因：现有 15 类只覆盖共享层和部分频道首页/本地功能脚本，不能证明所有频道子页面的运行时弹窗、抽屉和确认框均已纳入。
- 目标：以频道注册表、全部 Hash 子路由、本地功能源码及只读 Umi 构建分包为范围，建立可追溯的弹层清单；所有能够由本地真实源码/API生成的弹层接入 `全局组件/弹窗预览.html`，无法安全抽取的只读构建弹层也必须登记来源、触发路径和覆盖状态，不再以“只读 web”直接排除。
- 范围：只做弹层盘点、真实引用工厂与铺开预览；不改弹窗业务含义、路由、主导航、线上数据或服务器请求。
- 引用标准：`弹窗预览.html` 只通过 `<link>` 直接加载每个弹层所属的真实 CSS；真实 CSS 发生变化后只需刷新预览，不允许在预览 HTML 中同步复制颜色、字体、按钮、间距、圆角或阴影。预览内联规则仅可把 fixed/top-layer 弹层改成文档流铺开并提供画布高度，不得覆盖组件视觉。
- 盘点结果：共 45 类。前 15 类继续调用共享 API 或本地功能真实工厂；新增 30 类覆盖 AI Agent 审核历史与会话、线索分配/详情/转化/创建、客户编辑/画像/沟通/删除/历史方案、活动记录与详情、产品详情/专家/附件、保单绑定、方案删除/类型/三类问卷/生成/上传、入职完成、资讯下架与全局账号切换。
- 历史结果：本条曾用真实 CSS 配合代表结构生成 45 个展示区；经用户视觉检查确认第 12 项初始化不完整、第 16 项起结构不匹配。该展示实现已由下方“真实入口与 iframe 隔离”整体撤销，45 类盘点名单继续保留。

## 弹窗预览改为真实入口与 iframe 隔离

- 状态：第一阶段已完成并通过离线动态验证，待用户刷新 `file://` 视觉确认；未发布
- 原因：第 12 项只调用配置中心基础模板工厂而未执行正式业务初始化；第 16 项起仅以通用 DOM 套用真实 CSS，无法匹配源组件结构。原 45 项展示不能作为真实弹窗预览。
- 目标：业务页面与预览页调用同一个完整 `open()`/`mount()` 源入口；每项通过独立 iframe 保留真实 top-layer、Portal、遮罩、视口约束和样式作用域。无法调用完整真实入口的项目只登记来源并明确标记待接入，不再生成模拟 DOM。
- 用户边界：抽屉不进入弹窗预览，不计入可预览数量；不得通过复制弹窗 HTML/CSS 补齐展示。
- 范围：`全局组件/弹窗预览.html`、弹层登记、必要的真实打开入口与文档；不改业务弹窗视觉、业务数据、主导航、Hash 行为或只读 `web/` 构建包。
- 实现：重写 `弹窗预览.html` 为父预览/子 iframe 同文件双模式。父页只生成预览卡片和待接入来源清单；子 iframe 根据 `?embed=<id>` 只加载该弹窗登记的真实 CSS/脚本并调用真实源结构。删除全部通用 Modal/Drawer/Popconfirm DOM、原始类名拼装和组件定位覆盖。`弹窗源登记.js` 现只保存 45 类元数据与资源，不生成任何弹窗 DOM。
- 图标修复：iframe 模式下，源组件的 `features/`、`shared/`和 `web/` 相对资源曾被误解析到 `全局组件/` 下，导致 SVG/图标空白。子 iframe 现在动态插入 `<base href="../">`，登记资源统一按项目根目录解析，不复制或改写图标。
- 分类：45 类中 6 类抽屉（财富值文件识别、Agent 审核历史、客户编辑、客户画像、活动详情、申请文件上传）完全不进入预览；39 类非抽屉中 9 类具备可验证真实源结构，30 类标记“待接入真实入口”且不渲染视觉。AI Agent 主面板因旧 `createPreviewModal()` 复制演示 DOM、配置中心 4 类因基础工厂后仍有正式初始化，均已从可预览项撤下。
- 验证：登记脚本和 HTML 内联脚本语法通过；45/9/30/6 的总数、真实资源存在性和“父页不出现抽屉”断言通过。jsdom 分别加载父页及 9 个 `?embed=` 子页，确认 9 个 iframe、30 个待接入卡片，以及 AI 提示、海报、操作日志、方案客户归属/解除关联/附件、活动报名、财富关键词和资讯详情的真实源节点全部成功打开。额外逐项解析 9 个子页的 CSS `url(...)` 及运行时 `img`/`iframe` 本地资源，缺失数为 0。

## 配置中心 4 个弹窗接入真实预览

- 状态：已完成代码、功能回归和 `file://` 真实 Chrome 视觉检查，待用户刷新确认；未发布
- 目标：将成员编辑、部门名称编辑、删除部门确认、设置管理员四类弹窗从“待接入”改为调用配置中心完整正式打开流程的 iframe 预览。
- 单一源约束：业务页与预览页必须共用 `config-center.js` 中的状态、数据、表单初始化、事件和 `showModal()` 流程；预览不复制弹窗 DOM/CSS，不改写视觉。
- 预定范围：`features/config-center/`、`全局组件/弹窗源登记.js`、`全局组件/弹窗预览.html`、配置中心回归与对应项目文档；不修改弹窗样式、正式页数据语义、主导航或 `web/` 基线。
- 实现：`config-center.js` 新增 `window.__GAIP_CONFIG_DIALOGS__`，页内成员和部门按钮均改调该控制器；四个打开函数继续使用既有成员/部门数据、角色权限、表单填充、事件与 `showModal()`。iframe 预览模式先挂载完整组织页状态再调用同一控制器，不从基础模板单独生成弹窗。`弹窗源登记.js` 的当前分类从 9/30/6 更新为 13/26/6。
- 实际文件：`features/config-center/config-center.js`、`shared/config/channels.js`、16 个根入口的频道注册表缓存版本、`全局组件/弹窗源登记.js`、`全局组件/弹窗预览.html`、`scripts/test-config-center.cjs`及对应 README/配置中心/全局框架/项目状态文档。配置中心 JS 版本为 `20260901-23`，根入口频道注册表缓存版本为 `20260901-37`。
- 验证：配置中心原有部门增改删、管理员角色、成员编辑、日志和导航回归通过；新增隔离预览回归确认四个公开入口都使用完整初始化。独立无头 Chrome 直接打开四个 `file://...弹窗预览.html?embed=` 地址并逐张查看截图，成员编辑、修改部门、删除确认和设置管理员的遮罩、图标、表单、成员列表和按钮均正常。操作日志、AI Agent 入口、可展开主导航和 Hash 无刷新导航回归通过；45/13/26/6 分类、预览资源缺失 0、16/16 根入口版本与 `git diff --check` 通过。

## 排除 Agent 主面板并完成其余弹窗真实接入

- 状态：已完成本地实现与代码验证，未发布；`file://` 视觉仍待刷新手工确认。
- 用户边界：“GAIP Agent 助手主面板”不再进入弹窗预览；已排除的 6 类抽屉继续不展示。
- 单一源约束：已有本地完整控制器的项目直接调用共享 `open()`/`mount()`；仅存在于只读 Umi 分包中的弹窗，必须通过本地正式页面路由和真实点击入口打开，预览不复制 DOM/CSS、不重写图标、不直接修改 `web/` 构建产物。
- 实现：新增 `shared/scripts/popup-preview-bridge.js`，只在 `gaip-popup-preview` 标记存在时运行；`local-preview.js` 负责按需加载。15 类 Umi 业务弹窗通过正式 Hash 页面和真实 DOM 点击入口打开；原 13 类继续使用共享 API/完整本地控制器。父预览页不保存这些弹窗的 HTML/CSS。
- 分类：45 类登记中 28 类真实可预览、0 类待接入、17 类不展示。17 类包括 6 类抽屉、Agent 主面板，以及 10 类经当前路由和源码复核确认已下线、入口不可达或并非独立弹窗的旧登记；这些项目保留原因说明，不生成假弹窗。
- 文件：`全局组件/弹窗源登记.js`、`全局组件/弹窗预览.html`、`shared/scripts/popup-preview-bridge.js`、`shared/scripts/local-preview.js`、`scripts/test-popup-preview.cjs`；16 个根入口的 `local-preview.js` 缓存版本统一为 `20260901-4`，预览桥为 `20260901-3`。未修改 `web/`。
- 补充修复：取消 28 个 iframe 的懒加载，新增父子状态回报与逐项失败提示；真实弹窗打开后以独立中性底层遮住频道正文，只保留原弹窗/确认框及其遮罩。修正会话重命名隐藏入口、线索“标记已转化”、活动报名记录和入职完成状态四类触发条件。
- 报名记录二次修复：不再按“我的报名”文字节点查找入口，改为点击正式源码中承载 `onClick` 的 `signUpContainer___*` 卡片；15 个 Umi 频道 iframe 改为最多 2 个并发的队列加载并设 60 秒父级超时，避免同时初始化造成可见节点迟于桥接超时。
- 验证：登记/桥接脚本语法通过；专项回归确认 45/28/0/17、15 个路由触发流程、17 个排除项及 16/16 根入口版本一致；配置中心、AI Agent、操作日志、可展开主导航和 Hash 导航回归通过。应用内浏览器安全策略拒绝接管 `file://` 页面，因此未绕过策略，视觉需刷新现有预览页手工确认。

## 配置中心频道同步

- 状态：已完成本地实现与验证，未提交/发布；用户仅要求同步本地。
- 目标：同步线上配置中心及当前账号可见的“组织架构”二级导航，新增“操作日志”二级页面。
- 新增文件：`features/config-center/config-center.js`、`features/config-center/config-center.css`、`配置中心.html`、`knowledge/页面/配置中心.md`。
- 代码修改：`shared/config/channels.js`、`shared/scripts/channel-regions.js`、`shared/scripts/global-operation-log.js`、`scripts/test-operation-log.cjs`。
- 入口修改：`index.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`。仅同步三个共享脚本缓存版本：频道配置 `20260831-2`、区域标记 `20260831-1`、日志 `20260831-5`。
- 本地保留：既有未跟踪 `index-login-video-test.html` 同步相同缓存号，但仍不纳入发布；其他试验素材与参考分包未改动。
- 文档修改：`PROJECT_STATE.md`、`docs/README.md`、本记录、`knowledge/页面/页面索引.md`、`knowledge/公共模块/主导航与Hash路由.md`、`knowledge/公共模块/频道资源加载.md`、`knowledge/公共模块/全局框架与样式.md`。
- 实现：配置中心采用注册表 views 和 Hash 虚拟频道；组织页含六个渠道、示例部门、虚构成员的筛选/分页/本地添加编辑/Excel 导出。操作日志页面与组织页日志弹窗共享组件，以独立实例隔离筛选和 ID；全局顶栏入口已于 2026-09-01 移除，不新增真实审计数据或线上依赖。
- 验证：导航静态检查、JS 语法、diff 检查、操作日志回归（含 XLSX/XML/CRC、页面与弹窗隔离）通过。浏览器确认工作台 → 配置中心 → 资讯、客户 HTML → 配置中心 → 产品、浏览器后退恢复；主文档会话标记不变。确认菜单折叠与展开、二级高亮、面包屑、组织搜索/编辑/分页、日志模块筛选和组织页日志弹窗打开关闭。
- 视觉修正：覆盖原站菜单 pointer-events:none 与强制子菜单展开规则；固定虚拟页宽度使导出/分页不溢出。桌面 1280×720 已确认，无新增控制台错误（仅原站 Sensors 离线告警）。
- 边界：不复制线上人员资料，不改线上配置、不写 `web/`。组织新增/编辑只保存于当前会话，刷新重置；未实现真实权限管理、后台审计或原站“更多”业务操作。原生 Esc/焦点循环仍沿用已有项目待手工确认项。

## 配置中心原站视觉纠偏

- 状态：已完成本地修正、视觉对照与交互回归；未提交/发布。
- 原因：用户指出上轮对图标、颜色和结构做了重写；原需求应按原站同步，不能视为改版授权。
- 范围：以实际捕获的原站 CSS、DOM 和图标替换配置中心自行绘制/简化的页面；保留新增操作日志二级入口、现有 Hash 路由及本地模拟数据边界。
- 新增文件：`features/config-center/source-markup.js`、`features/config-center/organization-source.css`、`features/config-center/ant-source.css`、`features/config-center/assets/organization.svg`、`features/config-center/assets/folder.png`、`features/config-center/assets/delete.svg`、根目录 `design-qa.md`。
- 修改文件：`features/config-center/config-center.js`、`features/config-center/config-center.css`、`shared/config/channels.js`、`shared/scripts/channel-regions.js`；上条登记的 14 个既有根入口与 `配置中心.html` 的频道配置缓存号改为 `20260831-3`，既有本地 `index-login-video-test.html` 同步但不发布。本记录、`knowledge/页面/配置中心.md`、`knowledge/公共模块/频道资源加载.md`、`knowledge/公共模块/全局框架与样式.md` 与 `PROJECT_STATE.md` 同步说明。
- 修正：采用原站 DOM 和实际图标，恢复六个页签、黄色文件夹与部门缩进、成员表格/标签、48px 品牌绿按钮和 850px 编辑表单。运行时 CSS 分段提取避免截断，保留原始 style 文本避免 CSSOM 丢失边框简写；源 CSS 仅在配置中心作用域应用。修复编辑选项回显与保存。
- 验证：1326×1035 CSS 视口下主页面、组织树/表格局部和编辑弹窗并排对照；补查 1280×720 和窄窗口的原站固定宽度行为。导航静态检查、JS 语法、diff 检查与操作日志回归通过；浏览器验证工作台 → 配置中心 → 资讯，以及客户入口 → 配置中心 → 产品，两条路径主文档标记不变；搜索、树折叠、菜单展开/收起、编辑保存/回显、日志模块筛选与组织页日志弹窗隔离通过。
- 边界：保留已有本地频道顺序、顶栏和 AI 入口；人员字段替换为示例，所有操作均为本地会话数据，不同步真实账号、权限或审计。不修改线上配置，不修改 `web/`，不发布。旧版手工绘制页面已被本条替代。
