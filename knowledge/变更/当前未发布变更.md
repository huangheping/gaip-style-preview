---
type: current-changes
updated: 2026-09-03
---

# 当前未发布变更

## 配置中心公告管理独立子页面

- 状态：已完成，待本地验收与发布。
- 目标：根据《GAIP&官网公告展示&问卷优化》产品需求，在配置中心新增独立“公告管理”子级页面，并从组织架构主逻辑中抽离维护。
- 范围：公告列表、状态自动计算、分页、空状态、长标题提示、新建/编辑公告表单、删除二次确认及本地 Mock；展示中的公告禁止删除，以文字需求为准。
- 组件：复用项目 Ant/全局按钮、表格、状态标签、分页、Tooltip、弹窗、输入框、日期/时间选择及校验视觉，不建立另一套全局样式。
- 边界：不实现官网公告展示与问卷重复提交；不修改组织架构页面和两个既有操作日志的数据/控制器。
- 文件：`features/config-center/announcement-management-{data,view}.js`、`features/config-center/announcement-management.css`、`shared/config/channels.js`、`features/config-center/config-center.js`、正式根入口资源版本、弹窗源登记与自动索引、`scripts/test-announcement-management.cjs`、配置中心/导航/全局组件知识笔记。
- 文档复核：按产品明细将生效时间段统一为 `开始时间 ~ 结束时间`；列表分页直接复用现有 Ant 表格分页 DOM，仅保留总数、上一页图标、页码和下一页图标。超过 7 页时固定展示首尾页、当前页左右各 2 页，并提供可点击的前后 5 页省略号跳转，24 页等长列表不会横向铺满。编辑时间段按“开始时间 / 结束时间”分组，每组日期与时间同行，中间使用范围符号连接。删除弹窗继续由公告真实控制器登记到全局弹窗目录，危险操作颜色改用 Ant 全局 `--ant-color-error`（回退 `#ff4d4f`）；正文配置仅出现在角色职责概述、未进入管理端字段明细，继续按已确认范围不添加。
- 视觉细化：公告内容区顶部增加与配置中心其他频道一致的 `1px` 浅灰分割线，并仅作用于公告管理页面。首屏新增一条简体、繁体、英文均为 100 字符的公告 Mock；列表按当前语言只显示单行省略标题，hover 或键盘聚焦时通过项目 Tooltip 分语言完整展示全部已填写标题。生效时间由原始时间戳堆叠改为“开始 / 结束”标签对齐的两行结构，并使用等宽数字。新建、编辑弹窗恢复项目既有“body 内表单、独立 footer”的结构，只有 body 滚动；表单内层固定保留左右 `24px`，滚动条仍贴弹窗右缘，避免输入框与内容贴边。操作按钮沿用原紧凑规格 `88×40px / 14px`。日期和时间选择器不再使用大小不一的浏览器原生可见图标，统一为 `20×20px` 的项目日历/时钟线性 SVG，原生选择器点击热区继续保留。三种标题输入改为 48–112px 自动增高文本域，达到上限后仅文本域内部滚动。分页上一页/下一页使用明确的 `14px` Ant 方向图标，不再出现空白占位。
- 验证：公告专属 DOM 回归、配置中心挂载隔离、弹窗目录生成与预览、可展开主导航、全站 Hash 导航与浏览器实际视觉均通过；全局弹窗目录现为 51 total / 34 ready / 0 pending / 17 excluded。

## 配置中心二级操作日志筛选与表格细节

- 状态：已完成，待本地验收与发布。
- 目标：完善配置中心独立“操作日志”频道的搜索、日期选择、查看类记录文案及表格可读性。
- 交互：搜索支持姓名、域账号与操作内容；点击日期输入框任意区域均打开日期选择器；变更前/后统一使用黑色正文，仅展开/收起操作保留品牌绿；操作内容与变更前/后复用同一套逐行渲染、四行折叠及“展开全部 / 收起全部”逻辑。
- 视觉：表头文字保持单行；查看类资讯 Mock 保留资讯标题、原文链接和资讯日期，只把原内容摘要替换为“查看：查看原文/查看详情”。
- 边界：不修改组织架构页面右上角的独立操作日志弹窗。
- 文件：`shared/scripts/global-operation-log.js`、`shared/styles/global-operation-log.css`、`shared/data/operation-log-mock.js`、`scripts/test-operation-log.cjs`、正式根入口资源版本与配置中心说明；明确不修改本地保留且不发布的 `index-login-video-test.html`。
- 验证：共享日志 DOM/XLSX 回归、配置中心弹窗与导航回归、正式根入口资源顺序、JS 语法、差异格式及浏览器实际样式均通过；操作内容搜索“查看原文”返回 4 条，8 个表头计算样式均为 `white-space: nowrap`，变更前/后计算颜色均为 `rgb(47, 54, 64)`。

## 组织架构成员列表取消分页

- 状态：已完成，待本地验收与发布。
- 目标：移除组织架构成员表的分页切片与分页控件，当前节点或搜索结果中的全部成员一次展示。
- 交互：成员表占用右侧剩余高度并独立双向滚动，表头吸顶、横向滚动条停留在可视区域底部，“操作”列固定在右侧；组织架构专属操作日志弹窗的分页保持不变。
- 文件：`features/config-center/config-center.js`、`features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、`knowledge/页面/配置中心.md`。
- 验证：成员根节点、父级汇总、叶子节点、六渠道切换及批量导入后列表数量均按不分页行为回归。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
