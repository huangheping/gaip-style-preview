---
type: current-changes
updated: 2026-09-07
---

# 当前未发布变更

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 仅采用 PR #1 的主导航顺序（可回退预览）

- 状态：本地实现与代码/DOM 检查完成，待用户刷新确认；未合并 PR、未提交/推送。
- 目标：工作台总览 → 线索中心 → 客户中心360 → 方案中心 → 产品中心 → 保单列表 → 资讯中心 → 活动中心 → 学习中心 → 薄荷入职引导 → 财富值中心 → 配置中心。
- 范围：只重排全局菜单；保留节点、事件、路由、原尺寸/颜色/图标/间距及业务内容。使用现有观察器，不引入 PR 的额外全页观察器。
- 回退基线：`f178f82b7576b80fd7758d82d1977edcc25b5475`；修改前三个目标源码与正式入口无未提交变更。用户试验文件保持独立，不得用整库 reset/checkout 回退。
- 实际文件：`shared/config/channels.js` 新增 `sidebarOrder`；`shared/scripts/channel-regions.js` 新增 `syncSidebarMenuOrder` 并接入现有循环；`shared/styles/channel-foundation.css` 新增 `.gaip-main-menu-ordered` 限定规则；16 个根 HTML 仅更新这三份资源的缓存版本。新增 `scripts/test-sidebar-order.cjs` 并接入 `package.json` 的 `test:navigation`。本记录、项目状态与导航笔记同步。
- 间距边界：flex 的边距不再折叠，因此只对顶层菜单及其父项标题做必要的 margin 兼容，以保留原 4px 间距/40px 父项高度目标；不接受 PR 的额外全页观察器或菜单宽度覆盖。浏览器实际几何仍未验证。
- 检查：`npm run check:navigation`、`npm run test:navigation` 通过；覆盖指定排序/旧名称别名、节点及原点击监听不变、二级菜单排除、折叠状态及节点重建；原父项纯开合、键盘及双向频道隔离回归通过。DOM 不证明真实渲染、原生箭头键按视觉顺序移动或主文档无刷新；file 工具受限，不重复绕过访问。
- 撤销方法（仅在用户要求时执行）：删除上述 `sidebarOrder`、排序函数/调用和独立 CSS 段，撤回新增测试及 npm 命令中的该测试，保留其他修改；16 个入口的本次版本 `20260907-menu-order-1` 按资源恢复：CSS `20260904-4`、区域脚本 `20260904-3`、频道配置正式入口 `20260904-73`，只有 `index-login-video-test.html` 的频道配置原为 `20260903-43`。再同步本任务状态，刷新页面。若已有后续改动，逐段撤回本次增量，不整文件恢复。
- 快速停用预览也可将 `sidebarOrder` 设为空数组并刷新（更新该配置缓存版本）；这样不添加排序 class，恢复原生布局。完整回退仍按上一条清理本次增量。试验入口本次仅动引用、不纳入未来发布。

## 主导航仅调整菜单高度

- 目标：按用户要求将左侧一级叶子项、可展开父项及二级菜单的高度/行高统一为 36px（用户预览 32px 后调整）；仅改高度，排序、外边距、字体、图标、宽度和导航逻辑不变。
- 范围：共享框架 CSS 及实际消费者的 CSS 缓存引用；保留上一次排序预览和所有试验素材。未授权提交或推送。
- 实际文件：`shared/styles/channel-foundation.css` 统一菜单项高度/行高，并同步现有父项高优先级规则；16 个根 HTML 的此 CSS 版本改为 `20260907-menu-height-36`，其他资源版本未变。测试提示去除过时的高度描述，导航契约/框架笔记与项目状态同步。
- 检查：`npm run check:navigation`、`npm run test:navigation`、`git diff --check` 通过；补充 JSDOM 检查一级叶子/父项/二级项在展开及折叠态为目标高度，分组容器和侧栏外业务菜单不受此规则约束，16 个入口引用一致。未做真实浏览器几何验证，file 工具受限，待用户刷新确认。
- 回退边界：仅撤去本节新增的菜单行高度规则并恢复父项原高度/行高，CSS 引用恢复 `20260907-menu-order-1`，同步文档；保留上一节排序与所有其他工作。未提交/推送。

## 活动中心导航图标替换

- 目标：仅替换活动中心导航图标为用户提供的 `活动中心.svg`；沿用 18px 图标列、当前颜色继承及导航交互，不影响资讯中心。
- 范围：原 SVG 保存在共享资产目录，活动频道使用独立图标标识，共享图标 CSS 内嵌原 SVG，相关入口缓存同步；原有排序和 36px 菜单高度保留。不提交/推送。
- 实际文件：`shared/assets/nav-activity.svg` 保存原图；`shared/config/channels.js` 的 activity 独立使用 `activity-center`；`shared/styles/learning-nav.css` 仅增加此图标的内嵌 SVG 规则；16 个根 HTML 同步图标 CSS 和频道配置缓存 `20260907-activity-icon-1`。弹窗独立预览不显示主导航、未改其登记。
- 检查：配置 JS 语法、导航静态与排序/开合 DOM 回归通过；内嵌 SVG 与附件字节一致，保存的 SVG 图形一致，18×18 viewBox、资讯中心映射与 16 个入口版本均已核对。未做真实浏览器视觉验收，待刷新确认。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
