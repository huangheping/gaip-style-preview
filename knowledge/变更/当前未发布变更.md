---
type: current-changes
updated: 2026-09-07
---

# 当前未发布变更

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 官方依据的非 Agent / Skill 工程优化

- 状态：本地已完成首批配置和文档，尚未发布；四项实际操作已由用户手工确认，自动浏览器验证仍受限。
- 目标：依照 OpenAI 本地环境/验证指导及 npm 官方规范，固定开发测试依赖并提供标准检查入口、真实浏览器验收清单和 worktree 使用说明。
- 范围：仅开发测试配置、维护说明及知识库状态；不改网站运行时、HTML/CSS、Agent/Skill，不启用 CI、不发布、不移动其他任务。
- 实际文件：`package.json`、`package-lock.json`、`.gitignore`、`docs/official-engineering-baseline.md`、`docs/browser-regression-checklist.md`、`docs/maintenance-workflow.md`、`docs/README.md`、历史审计的后续链接，以及 `PROJECT_STATE.md` / `knowledge/INDEX.md` / 本记录。
- 验证：`npm ci --include=dev --ignore-scripts --no-fund` 成功；`npm test` 退出码 0（静态及五个 DOM 套件）；锁文件一致性、开发依赖范围、文档本地链接、`git diff --check` 通过。安装审计 0 个已知漏洞，有 `whatwg-encoding@3.1.1` 间接依赖弃用提示。
- 剩余：AI 入口本轮代码检查及用户手工切换通过，但历史间歇失败原因仍未定位；主文档无刷新、父项纯开合/键盘、焦点恢复、视频/WebGL 未完成现场验证；应用 setup/actions 配置、worktree 创建均未执行。
- 边界：资源版本清单不是 OpenAI 的明确规范，本轮不实施；未运行的浏览器检查不得记为通过。

### 2026-09-07 用户手工回归确认

- 证据来源：用户在本任务逐项操作后的文字确认，不是助手自动点击或截图/Network 观测；助手访问 `file://` 被浏览器工具拒绝，未绕过。
- 记录时 HEAD：`8a58653`，工作区包含上文未提交开发配置/文档及已有试验文件；浏览器版本、视口、控制台和主文档请求未采集，不能据此给出完整浏览器验收结论。
- 已确认 1：从当前方案中心切换到工作台，刷新后仍是工作台，没有回到导入工作台。此次保留财富值 HTML 文件名的入口，不等同于直接从导入工作台初始视图完整回放。
- 已确认 2：配置中心 → 组织架构 → 页面操作日志，关闭后可以通过左侧导航切到方案中心。
- 已确认 3：操作日志可用 Esc 关闭，随后点击入口可以再次打开；未单独确认焦点恢复及遮罩节点数量。
- 已确认 4：用户按“从登录.html 登录、通过日期切换 V1/V2、再跨频道切换版本”的提问确认正常；不扩大为视频/WebGL 或所有登录场景通过。
- 本次只同步本记录、`PROJECT_STATE.md`、浏览器清单与工程基线的验证状态；保留 Obsidian 双链。未修改业务或测试代码，未重复运行无关套件，未提交/推送。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
