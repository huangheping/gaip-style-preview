---
type: current-changes
updated: 2026-08-31
---

# 当前未发布变更

> 这是协作清单，不代替 `git status` 或 `git diff`。已发布内容见 [[2026-08-31-工作台与共享组件同步]]。

## 登录页视频英雄图测试入口

- 状态：待发布（用户明确本次不同步）
- 目标：不修改原 `index.html` 和 `登录.html`，用独立测试入口预览登录页左侧视频英雄图。
- 文件：`index-login-video-test.html`、`features/login-video-test/login-video-test.css`、`features/login-video-test/login-video-test.js`、`assets/login-hero-test/login-hero-preview.mp4`。
- 验证：JS 语法、导航检查及本地预览已通过；等待用户另行确认是否发布。

## 本地试验与参考文件

- 状态：暂停，不发布
- AI Agent：`creative-8/9` 试验图片与纹理、`AI Agent/素材/备份.mp4`。
- 资讯中心参考站资源：`web/p__news__index.f64a0a2e.async.js`。
- 原因：这些文件不是当前正式页面运行依赖，保留本地用于比较或回退。

## 工作台行情与分享图标对齐

- 状态：已实现，待浏览器视觉确认，未发布
- 目标：行情条边框恢复白色，修正国旗与文字在行情容器内的垂直居中，并修正分享图标的图形对齐。
- 范围：`features/workspace/workspace-update.css`、`features/workspace/workspace-update.js`、15 个根入口 HTML 的工作台资源缓存版本、`PROJECT_STATE.md` 和本记录；保留当前布局、字号和交互。
- 实现：行情轨道随容器等高伸展，国旗及文字统一为 20px 行盒，文字补偿 1px 视觉中心；边框恢复白色。分享图标保留原路径，通过校正 SVG viewBox 的不对称留白居中，显示尺寸仍为 18px。
- 验证：JS 语法、`verify-local-navigation.sh`、`git diff --check` 及针对布局规则、SVG 图形中心和全部入口版本的静态断言均通过；分享点击事件未改动。CSS/JS 缓存版本随下方「查看全部」修正更新为 `20260831-2`。
- 待确认：当前 `file://` 页面被浏览器安全策略阻止自动化读取，本轮未完成实时视觉和点击回归；需要刷新页面确认，不将静态检查等同于浏览器验证。

## 工作台查看全部入口对齐

- 状态：已实现，待浏览器视觉确认，未发布
- 目标：修正资讯中心「查看全部」文字与箭头的错位，保持按钮位置、尺寸和跳转行为。
- 范围：`features/workspace/workspace-update.css`、`features/workspace/workspace-update.js`、15 个根入口 HTML 的工作台资源缓存版本、`PROJECT_STATE.md` 和本记录。
- 实现：移除字符箭头的零行高与向下位移，换为 12×16px 的居中 SVG；按钮保持原有最小宽高和字号，图文间距为 4px，禁止压缩与折行；所有入口的 CSS/JS 缓存版本统一为 `20260831-2`。
- 验证：`node --check`、`verify-local-navigation.sh`、`git diff --check` 通过；静态断言确认 SVG 几何中心、Flex 对齐、原有最小宽高和 15 个入口版本；JS 与基线比较确认本轮仅改按钮标记，跳转事件逻辑未变。
- 待确认：浏览器读取仍受安全策略限制，未完成实时视觉和跨频道点击回归；需要用户刷新确认。未发布，也未改动原站 `web/` 资源。

## 操作日志筛选图标统一

- 状态：已实现，待浏览器视觉确认，未发布
- 目标：下拉、搜索、日期控件补齐项目已有 Ant Design 图标，不改变筛选与原生选择行为。
- 范围：`shared/styles/global-operation-log.css`、从原站基线提取的三个共享 SVG、15 个根入口的日志 CSS 缓存版本、`scripts/test-operation-log.cjs`；同步本记录、框架笔记和 `PROJECT_STATE.md`。不改 `web/`、导航、数据或弹窗脚本。
- 新增资源：`shared/assets/control-down.svg`、`shared/assets/control-search.svg`、`shared/assets/control-calendar.svg`；viewBox 与 path 完整保留自 `web/umi.0b0663b5.js` 的 Ant Design outlined 图标定义。
- 实现：两个筛选下拉和每页条数下拉使用 12px 箭头；搜索框左侧 14px 搜索图标；起止日期右侧 14px 日历图标。用 CSS 背景图保留原控件 DOM；支持 WebKit 日历指示器的浏览器透明化原图标但保留点击热区，不支持的浏览器保留原生日期入口，避免重叠。
- 入口：`index.html`、`index-login-video-test.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`，仅将日志 CSS 版本更新为 `20260831-5`，日志 JS 保持 `20260831-4`。
- 验证：日志代码回归、导航静态检查通过；新增断言核对 SVG 与基线一致、背景图/内边距、原生日历热区未移除。未完成实时视觉及原生下拉/日历展开验证；此前 file:// 安全限制未绕过，需手工刷新确认。

## 操作日志控件聚焦边框统一

- 状态：已实现，待视觉确认，未发布
- 目标：日志筛选与分页下拉沿用项目品牌绿边框及无阴影聚焦样式，消除默认蓝色外框差异。
- 范围：日志共享 CSS、15 个根入口日志 CSS 缓存号、代码测试、状态及框架笔记。不改业务组件或筛选逻辑。
- 文件：`shared/styles/global-operation-log.css`、`scripts/test-operation-log.cjs`、上一条列出的 15 个 HTML（日志 CSS `20260831-6`）、`PROJECT_STATE.md`、本记录和 [[../公共模块/全局框架与样式|全局框架与样式]]。
- 实现：搜索/日期/筛选下拉/分页下拉统一悬停与聚焦边框 `var(--log-brand)`，聚焦移除额外 outline 和阴影；键盘仍以品牌绿边框提示焦点，日期范围错误保留红边。按钮及表格区域的键盘焦点框不变。
- 验证：日志回归、聚焦规则与错误边框断言、导航静态检查、diff 检查通过。受此前浏览器访问限制影响，实际焦点视觉仍需手工确认。

## AI Agent 登录后入口与版本切换恢复

- 状态：已提交本地，GitHub 推送未确认，仍待真实浏览器验收
- 本地功能提交：`ee5c3e9cd94b1245491b6909aea94fb9502c54a5`。GitHub 443 连接超时，远程 fetch 失败；不将本地提交视为已发布，网络恢复后需核对远程并完成普通 push。
- 发布计划：用户确认本次推送 GitHub。只发布动效脚本、回归测试、14 个已跟踪正式入口的动效版本号及关联知识记录；第 15 个登录视频试验入口继续仅留本地。HTML 内工作台/操作日志的其他改动不纳入。
- 目标：修复入口首次挂载后停止监听，导致登录/节点重建后 V1/V2 动效与顶部日期切换丢失的问题。
- 范围：`AI Agent/AI Agent入口动效.js`、新增代码回归测试、15 个根入口的动效脚本缓存版本；同步本记录、AI Agent 笔记与 `PROJECT_STATE.md`。不改样式、素材、Mock、主导航或业务页面。
- 实际文件：`AI Agent/AI Agent入口动效.js`、`scripts/test-agent-entry.cjs`、本记录、`knowledge/公共模块/AI Agent.md`、`PROJECT_STATE.md`；15 个根入口为 `index.html`、`index-login-video-test.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`，仅增加动效脚本缓存号 `20260831-1`。
- 实现：持续监听相关顶栏/入口结构变化并合并到动画帧；Hash 改变时补充检查。以节点实例及注入内容判定是否需要重挂载，保留选中版本、处理状态与最小化状态；旧 WebGL/Lottie/视频和提示定时器按生命周期清理。顶部日期仍是版本切换入口，不新增按钮。
- 验证：`NODE_PATH=/tmp/gaip-agent-diagnostic-20260831/node_modules node scripts/test-agent-entry.cjs` 通过，覆盖直接进入、顶栏先挂载、入口先挂载、整体/顶栏/图标内容重建、多次 Hash 切换、重复绑定防护、处理/完成状态、资源清理、退出后再次进入和全部入口缓存号；无关业务节点变化不会调度入口检查。同套测试在 HEAD 旧脚本上复现版本丢失，修复后通过。
- 其他检查：JS 语法、操作日志回归、导航静态检查和 `git diff --check` 通过。jsdom 位于临时目录，不引入项目运行依赖。
- 待手工确认：测试模拟 DOM 生命周期，不代表真实 Umi 登录导航、视频播放和 WebGL 视觉已验收；此前 file:// 浏览器限制未绕过。刷新后从登录进入，点击顶部日期切换 V1/V2，再切换工作台/线索中心验证。

## GitHub 正式修改同步

- 状态：静态与代码检查通过，准备本地提交，尚未推送
- 目标：按用户要求将已修改的正式页面内容更新至 `huangheping/gaip-style-preview` 的 `main`。
- 范围：本页工作台两项修正、操作日志图标/控件边框、已提交的 AI 入口恢复修复及关联文档；14 个已跟踪正式入口同步缓存版本。
- 排除：登录视频测试入口及资源、AI creative-8/9 与备份视频、资讯中心参考站分包；不使用全量暂存。
- 验证：JS 语法、操作日志（含 XLSX/XML/CRC）与 AI 生命周期代码回归、导航静态检查和 diff 检查通过；14 个正式入口的 476 处本地资源引用完整，缓存版本与三个日志图标依赖核对通过。
- 待确认：GitHub 连接暂未返回；真实浏览器视觉、原生控件与 Umi 路径验收仍未补充，不以代码测试替代。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
