---
type: current-changes
updated: 2026-09-04
---

# 当前未发布变更

## 全局弹窗居中定位基础组件

- 状态：已完成，待用户刷新现有本地预览做最终视觉确认。
- 目标：把普通弹窗在视口中的位置统一为水平、垂直居中，并以桌面端 24px、窄屏 12px 作为安全边距；不同业务弹窗继续保留各自宽高与内容排版。
- 边界：抽屉仍从侧边进入；GAIP Agent 助手主面板不接入；嵌套确认只在所属主弹窗内部居中；不在弹窗预览页复制真实弹窗 DOM 或业务样式。
- 实施：新增共享定位样式和轻量接入器，为现有及后续动态创建的 Ant Modal、原生 dialog 与已登记自定义弹窗添加明确定位标记；预览 iframe 使用统一视口高度，避免登记项自身高度造成视觉上的假偏移。
- 规则：正式弹窗在当前视口水平/垂直居中；桌面端安全边距 24px，640px 以下 12px。Ant Modal 清除框架默认 `top:100px`，原生 dialog 清除局部固定顶部偏移；较小弹窗不被放大，较大弹窗继续由真实源内部滚动规则处理。
- 排除：财富抽屉、Ant Drawer 与 `.agentModal___Nxp06` 主面板不会被接入；方案解绑、批量导入返回、调整管理员节点等嵌套确认只在所属主弹窗内部居中。
- 预览：`全局组件/弹窗预览.html` 的 35 个 iframe 全部使用同一 820px 视口，源登记中的历史 `height` 不再改变预览舞台，真实弹窗 DOM/CSS 仍来自各自源文件。
- 文件：新增 `shared/styles/global-modal-position.css`、`shared/scripts/global-modal-position.js`；15 个正式根入口、登录视频测试入口、全局组件首页和弹窗预览页均引用同一共享源；同步更新 `scripts/test-popup-preview.cjs`、`AGENTS.md`、`PROJECT_STATE.md` 与公共模块说明。
- 验证：JS 语法、动态新增弹窗接入及 Agent/抽屉排除、自动弹窗索引、52/35/17 弹窗登记、配置中心与公告管理 DOM、可展开主导航、Hash 无刷新导航及 `git diff --check` 全部通过。应用内浏览器因 URL 安全策略拒绝自动读取现有 `file://` 标签，未绕过限制；最终视觉需用户刷新现有预览确认。

## 全局弹窗遮罩基础组件

- 状态：已完成，待用户刷新现有本地预览做最终视觉确认。
- 目标：将所有真实业务弹窗的遮罩颜色、透明度、过渡和背景阻断规则收敛为唯一全局基础组件；弹窗层级与嵌套顺序仍由各真实弹窗生命周期控制。
- 边界：不修改业务弹窗内容、尺寸或个性布局；不在弹窗预览页复制遮罩样式；AI Agent 主面板仍不属于组件预览范围。
- 视觉基线：`rgba(0,0,0,.45)`、160ms 统一动效、无背景模糊，并兼容原生 `dialog::backdrop`、Ant Modal / Drawer 与本地自定义弹窗遮罩；减少动态偏好下将动效压缩至近乎即时。
- 唯一来源：新增 `shared/styles/global-modal-mask.css`；15 个正式根入口、登录视频测试入口、`全局组件/index.html` 与 `全局组件/弹窗预览.html` 均引用同一文件。弹窗预览没有复制遮罩规则，直接沿用正式页面共享源。
- 业务收敛：已移除 AI 提示、海报分享、操作日志、资讯详情、方案归属/解绑/附件、活动报名、财富关键词、配置中心原生 dialog、公告弹窗及两个嵌套确认层中的局部遮罩色、模糊和冲突时长；定位、尺寸和 `z-index` 原样保留。财富抽屉与 Ant Drawer 同样纳入统一遮罩视觉，但抽屉继续不进入弹窗组件预览。
- 回归约束：`scripts/test-popup-preview.cjs` 会检查共享令牌、原生/Ant/自定义选择器、15 个正式入口引用顺序以及历史局部遮罩值不得回流。
- 验证：自动索引、弹窗预览登记（52 总数 / 35 可预览 / 17 排除）、配置中心 DOM、公告管理 DOM、可展开主导航、Hash 本地导航、JS 语法和 `git diff --check` 全部通过。应用内浏览器因 URL 安全策略拒绝自动读取现有 `file://` 标签，未绕过限制；最终颜色观感需用户刷新现有本地标签页确认。

## 操作确认共享样式复核

- 状态：已完成，待用户在本地弹窗预览中确认。
- 目标：保留用户手动调整后的共享 CSS，复核 05、12、17、25、26、35 六个操作确认弹窗是否真正只由共享组件控制，并统一关闭图标、hover 点击区与图标居中。
- 结果：第 12 项接入时会移除弹窗外壳与按钮上的旧业务哈希样式类，避免旧配置中心 CSS 参与竞争；第 17 项以及 05、25、35 的确认模板不再复制关闭 SVG，26 的 Ant 系统确认框也由同一共享函数补齐或重建关闭图标。关闭按钮统一为 32px hover 点击区、16px 图标，hover/active 均保持图标几何中心不位移。
- 版本：共享组件升级为 v1.1.3；CSS `20260904-4`，JS `20260904-5`，频道配置缓存 `20260904-73`。
- 文件：`shared/scripts/global-modal.js`、`shared/styles/global-modal.css`、`features/proposal-center/proposal-center.js`、`features/customer/customer-center.js`、`features/config-center/config-center.js`、`features/config-center/announcement-management-view.js`、`shared/config/channels.js`、`全局组件/弹窗源登记.js`、`全局组件/弹窗自动索引.generated.js`、`全局组件/弹窗预览.html`、`全局组件/index.html`、正式根 HTML 薄壳、`scripts/test-config-center.cjs`、`scripts/test-popup-preview.cjs`、`PROJECT_STATE.md`、`knowledge/公共模块/弹窗视觉规范与盘点.md`。
- 验证：自动索引检查、弹窗预览登记回归、配置中心 DOM 回归、公告管理 DOM 回归、Hash 本地导航与 `git diff --check` 全部通过；临时 HTTP 只用于自动化视觉验收，已关闭。05、12、17、25、26、35 六项真实预览均确认组件版本为 v1.1.3，关闭点击区 32×32、图标 16×16、单一图标实例，水平和垂直中心偏移均为 0；第 12 项共享内容内边距为 24px 且不再包含旧哈希外壳类。

## 弹窗预览三类用途目录

- 状态：已完成，待用户本地视觉确认。
- 目标：将 35 个真实弹窗精简为“信息展示、表单操作、操作确认”三个一级分类，同时保留原 01–35 编号。
- 实现：用途分类写入真实弹窗登记的 `category` 字段；预览页根据登记自动分组并提供“全部 / 三分类”筛选，不按 ID 维护另一份分类名单，不修改弹窗 DOM/CSS。真实预览之前增加三栏“分类总览”，一次列出 01–35 全部名称；点击任一项可定位并短暂高亮对应真实弹窗卡片。
- 数量：信息展示 13、表单操作 16、操作确认 6。删除与解绑等高风险差异作为后续 `danger` 变体，不再拆分第四类。
- 文件：`shared/scripts/modal-registry.js`、`全局组件/弹窗源登记.js`、`全局组件/弹窗自动索引.generated.js`、`全局组件/弹窗预览.html`、`全局组件/index.html`、`features/config-center/config-center.js`、`features/config-center/announcement-management-view.js`、`scripts/generate-modal-catalog.cjs`、`scripts/test-popup-preview.cjs`、`AGENTS.md`、`knowledge/公共模块/弹窗视觉规范与盘点.md`、`knowledge/决策/ADR-004-弹窗源自登记与自动索引.md`。
- 验证：自动索引时效、52 项登记 / 35 项真实预览 / 17 项排除、三类数量、总览定位结构和 `git diff --check` 通过。

## 操作确认统一组件与六项接入

- 状态：已完成，待用户在本地弹窗组件预览中最终确认。
- 目标：让“操作确认”分类的 6 个现有弹窗和以后新增的确认弹窗共用唯一结构、视觉和状态规则。
- 边界：保留各页面原有确认、取消、关闭和数据逻辑；不改添加/修改部门等非确认弹窗，不使用全局 `.ant-modal` 覆盖。
- 实现：`shared/scripts/global-modal.js` v1.1.3 与 `shared/styles/global-modal.css` 统一提供 480/520px 尺寸、标题/正文/说明层级、32px 关闭控件、footer、40px 按钮、普通/危险/禁止状态和 Ant `Modal.confirm` 结构归一化。05 使用品牌色普通确认；12、25、26、35 使用危险确认；17 使用 520px 复杂危险确认。删除部门的禁止态继续使用普通说明与禁用按钮。用户本轮手动调整的共享 CSS 参数保持不变。
- 主要源码：`shared/scripts/global-modal.js`、`shared/styles/global-modal.css`、`features/proposal-center/proposal-center.js`、`features/customer/customer-center.js`、`features/config-center/config-center.js`、`features/config-center/announcement-management-view.js`。
- 同步文件：`shared/config/channels.js`、`全局组件/弹窗源登记.js`、`全局组件/弹窗自动索引.generated.js`、`scripts/test-popup-preview.cjs`、正式根 HTML 薄壳的缓存版本、`knowledge/公共模块/弹窗视觉规范与盘点.md`、`knowledge/页面/配置中心.md`、`PROJECT_STATE.md`。
- 可见性与规范修复：删除了 05、12、17、35 原有的局部确认外壳样式；25 将原 Popconfirm 代理到共享确认层并继续触发原按钮；26 由共享层归一原 Ant `Modal.confirm`，不复制组件 CSS。危险语义只由红色主按钮表达。
- 验证：6 项均完成真实截图验收；`generate-modal-catalog --check`、`test-popup-preview`、`test-config-center`、`test-announcement-management`、`test-expandable-main-nav`、`verify-local-navigation`、相关 JS 语法检查和 `git diff --check` 全部通过。
- 后续契约：现有和未来项目的操作确认弹窗都必须使用该共享组件；业务页不得复制外壳 DOM/CSS 或自建同类样式。当前参数为 480px 默认宽度（复杂信息 520px）、24px 内边距、用户手动调整后的 18/28px 标题与 14/24px 正文、40px 按钮、32px 关闭点击区；默认无警示图标，危险态只使用红色主按钮。

## 组织树展开文件夹状态

- 状态：已完成，待本地验收与发布。
- 目标：为组织节点增加与折叠状态一致的打开/闭合文件夹反馈。
- 交互：左侧组织树和批量导入节点树随展开、收起即时切换；调整节点树对当前完整展开的父节点显示打开文件夹，叶子节点继续显示闭合文件夹。
- 资源：新增 `features/config-center/assets/folder-open.svg`，尺寸与色值沿用原闭合文件夹。
- 文件：`features/config-center/config-center.js`、`features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、`knowledge/页面/配置中心.md`。

## AI Agent 弹窗预览范围校正

- 状态：已完成，待发布。
- 目标：仅把具备全局复用性质的“编辑对话名称”纳入“全局组件 / 弹窗预览”；Agent 主面板、审核历史和其他内部弹层继续排除。
- 实现：`agent-session-rename` 恢复为 `ready`，频道标识为“全局操作 / AI Agent”，并继续通过工作台 → Agent 更多操作 → 修改名称的真实流程打开；`agent-main`、`agent-history`、`agent-close-confirm` 保持 `excluded`。
- 文件：`全局组件/弹窗源登记.js`、`shared/scripts/popup-preview-bridge.js`、`scripts/test-popup-preview.cjs`、`knowledge/公共模块/弹窗视觉规范与盘点.md`。
- 边界：不修改 AI Agent 弹窗 DOM、CSS 或页面内交互，Agent 功能仍可在工作台自身入口使用。
- 验证：自动索引为最新；弹窗预览回归通过（52 项盘点、35 项组件预览、17 项排除、15 项路由触发），Hash 本地导航与 `git diff --check` 通过；本机真实源预览已成功打开“18 编辑对话名称”弹窗。

## 调整管理员节点二次确认样式

- 状态：已完成，待本地验收与发布。
- 目标：移除二次确认中的感叹号图标，重新梳理标题、原节点路径与权限变化的文字层级。
- 交互：确认调整使用红色危险操作按钮，并提供 hover、按下和键盘聚焦状态；取消操作保持原描边样式。“调整节点”主弹窗与“调整管理员节点确认”已分别进入弹窗组件预览，二次确认可直接打开。
- 文件：`features/config-center/config-center.js`、`features/config-center/config-center-content.css`、`scripts/test-config-center.cjs`、`knowledge/页面/配置中心.md`。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 全局侧栏百宝箱横版入口

- 状态：本地完成，尚未发布；待现有 `file://` 标签页人工刷新确认。
- 目标：用用户最新提供的横版“GLORY 百宝箱”图片替换全局侧栏现有入口，保持左右两端对齐，将 `.ant-pro-layout .ant-pro-sider-footer` 的底部内边距清零，并取消图片入口圆角。
- 实现：`shared/assets/sidebar-hub-wide.20260904.png` 已替换为最新附件（388×112，SHA-256 `bf187b4e6753ddff16bcdc876fe63985c893f613e86ab3ef4f287af0c3850b57`）；`shared/scripts/channel-regions.js` 使用带版本参数的本地图片地址并移除原英文说明节点；`shared/styles/channel-foundation.css` 将入口设为底部背景容器 `100%` 宽（展开态约 208px、约 60px 高），不再设置额外圆角，同时把 `.ant-pro-layout .ant-pro-sider-footer.gaip-sidebar-hub` 的 `padding-block-end` 统一覆盖为 `0`。
- 缓存：16 个根入口继续使用全局框架 CSS `20260904-4`，区域标记脚本更新为 `20260904-3`，图片自身缓存参数更新为 `20260904-3`。
- 验证：共享侧栏 DOM、最新附件资源哈希、无额外圆角、全宽与零底部内边距回归通过；附件与项目图片逐字节一致，本地 Hash 导航、16/16 入口缓存版本、JS 语法及 `git diff --check` 均通过。应用内浏览器的 URL 安全策略阻止自动读取 `file://` 页面，最终视觉需刷新现有本地标签页人工确认。

## 新任务追加格式

## 2026-09-07 正式内容同步

- 状态：待发布。
- 目标：将本地已完成的弹窗基础组件、弹窗用途目录、确认弹窗统一、配置中心组织树/管理员确认和全局侧栏横版入口同步到 GitHub；同时发布本地领先 GitHub 的公告管理与配置中心既有提交。
- 范围：本文件中除“登录页视频英雄图测试入口”“AI Agent 试验素材”“财富值概览候选图”“资讯中心参考分包”之外的正式改动；正式新增资源为 `features/config-center/assets/folder-open.svg`、`shared/assets/sidebar-hub-wide.20260904.png`、`shared/scripts/global-modal*.js` 与 `shared/styles/global-modal*.css`。
- 验证：弹窗索引生成与时效检查通过；弹窗预览为 52 项盘点、35 项可预览、17 项排除；配置中心、公告管理、可展开主导航及 Hash 导航回归全部通过，`git diff --check` 通过。
- 排除：登录视频测试入口/素材、creative-8/9 与备份视频、7 张财富候选图、资讯参考站分包继续保留本地，不加入提交。

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
