---
type: current-changes
updated: 2026-08-31
---

# 当前未发布变更

> 这是协作清单，不代替 `git status` 或 `git diff`。已发布内容见 [[2026-08-26-频道与全局组件更新]]。

## 登录页视频英雄图测试入口

- 状态：待发布（用户明确本次不同步）
- 目标：不修改原 `index.html` 和 `登录.html`，用独立测试入口预览登录页左侧视频英雄图。
- 文件：`index-login-video-test.html`、`features/login-video-test/login-video-test.css`、`features/login-video-test/login-video-test.js`、`assets/login-hero-test/login-hero-preview.mp4`。
- 验证：JS 语法、导航检查及本地预览已通过；等待用户另行确认是否发布。

## 本地试验与参考文件

- 状态：暂停，不发布
- AI Agent：`creative-8/9` 试验图片与纹理、`AI Agent/素材/备份.mp4`。
- 资讯中心参考站资源：`web/p__news__index.f64a0a2e.async.js`。
- 原因：这些文件不是当前正式页面运行依赖，保留本地用于比较或回退。

## 工作台资讯中心线上元素补齐

- 状态：已完成，待发布
- 目标：按线上工作台补齐资讯中心来源说明文案与文章“分享”按钮，并复用全局文章海报分享组件。
- 范围：`features/workspace/workspace-update.js`、`features/workspace/workspace-update.css`、15 个根入口 HTML 的工作台资源版本号、`knowledge/页面/工作台总览.md`、`PROJECT_STATE.md`。
- 根入口：`index.html`、`index-login-video-test.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`。
- 验证：JS 语法和 `verify-local-navigation.sh` 通过；本地直达页面已核对文案与 90×32 分享按钮；点击分享可打开全局海报弹窗并传入当前文章；从产品中心入口切换到工作台后仍显示最新版。

## 全局侧栏百宝箱卡片同步

- 状态：已完成，待发布
- 目标：将所有频道左侧栏底部的“GLORY 百宝箱”入口同步为线上现用图片与尺寸，并继续保持全局共享实现。
- 范围：`shared/scripts/channel-regions.js`、`shared/styles/channel-foundation.css`、`shared/assets/sidebar-hub.7ade03a1.png`、15 个根入口 HTML 的共享资源版本号、`knowledge/公共模块/全局框架与样式.md`、`PROJECT_STATE.md`。
- 验证：`channel-regions.js` 语法和 `verify-local-navigation.sh` 均通过；工作台中的图片原始尺寸为 376×292，显示尺寸为 176×136.68、圆角 11px，与线上一致；客户中心360、保单列表、学习中心均已抽查，切换频道后仍使用同一共享图片和样式。

## 工作台视觉规格整理

- 状态：已完成，待发布
- 目标：统一工作台总览的字号层级、模块间距、文字与边框颜色、按钮高度与内边距，使信息密度更规整，同时保持现有布局与品牌风格。
- 范围：`features/workspace/workspace-update.css`，以及 15 个根入口 HTML 的工作台 CSS 缓存版本号；不修改其他频道的专属样式。
- 规格：指标数字统一为 36px；模块标题区统一为 88px；主视觉按钮统一为 144×44；文章操作按钮统一为 88×34；正文颜色收敛为主文字、辅助文字、品牌绿三层。
- 验证：本地工作台视觉检查通过；分享按钮可正常打开海报弹窗；从产品中心入口切回工作台后仍加载 `workspace-update.css?v=20260827-2`，布局与按钮规格保持一致；`verify-local-navigation.sh` 与 `git diff --check` 均通过，控制台无本次修改引入的错误。

## 工作台横向适配与字重层级

- 状态：已完成，待发布
- 目标：仅解决工作台在常见桌面宽度下的横向滚动，并补齐标题、指标、标签和按钮的 500/600 字重层级。
- 范围：`features/workspace/workspace-update.css` 与 15 个根入口 HTML 的工作台 CSS 缓存版本号；不改内容、模块结构或其他频道样式。
- 实现：工作台布局从固定 1440px 改为视口宽度，侧栏保持 212px、主内容占据剩余空间；取消焦点列表和漏斗图的固定最小宽度；使用本地 HarmonyOS Sans SC Medium/Bold 字体，为标题设置 600、指标与按钮设置 500。
- 验证：1280px 视口下页面与主体宽度均为 1280px、非行情动画节点越界数为 0；Medium/Bold 字体均加载成功，标题、指标、按钮计算字重分别为 600/500/500；切换到产品中心时工作台作用域自动解除，从产品中心 HTML 入口返回工作台后自动恢复并加载 `workspace-update.css?v=20260827-3`；`verify-local-navigation.sh` 与 `git diff --check` 均通过，控制台无本次修改引入的错误。1280px 为本轮桌面检查的最窄宽度，1326px 与 1440px 使用同一自适应规则。

## 客户中心视觉规格整理

- 状态：已撤回
- 目标：参考工作台总览的收口方式，规整客户中心 360 的字号层级、间距、色值、按钮尺寸，并整理后续可继续优化点。
- 结果：用户确认“还是之前的好”，已撤回本轮视觉规格覆盖、客户频道 CSS 版本号和根入口 `channels.js` 缓存号变更；保留上一轮已经确认的 `已关联保单` 标签位置同步。
- 后续优化点：若之后还要微调客户中心，建议只做很小的局部修正，例如标签位置、单个按钮对齐、选中态颜色这类明确问题，避免整体字号和间距再大幅改动。

## 全局操作日志

- 状态：已实现，待手工验证，未发布
- 目标：全局顶栏日期后、用户头像前增加操作日志入口，打开大弹窗；不切换 Hash、不刷新页面、不新增 HTML。
- 新增文件：`shared/styles/global-operation-log.css`、`shared/scripts/global-operation-log.js`、`shared/data/operation-log-mock.js`、`shared/scripts/operation-log-xlsx.js`、`scripts/test-operation-log.cjs`。
- 入口引用：`index.html`、`index-login-video-test.html`、`工作台.html`、`产品中心.html`、`保单列表.html`、`双击打开预览.html`、`学习中心.html`、`客户中心360.html`、`方案中心.html`、`活动中心.html`、`登录.html`、`线索中心.html`、`薄荷入职指引.html`、`财富值中心.html`、`资讯中心.html`，每个入口只增加 1 个 CSS 和 3 个有序脚本引用。
- 文档：本记录、[[../公共模块/全局框架与样式|全局框架与样式]]、`PROJECT_STATE.md`。原有未提交工作保留，不改 `web/`、频道配置、导航脚本、业务页面专属代码或全局字体。
- 交互：模块/类型/日期/姓名账号筛选、重置、分页、长内容展开、导出筛选结果为 Excel；明确标注本地模拟数据。
- 实现：28 条明确标注的模拟记录；原生大弹窗、关闭/Esc、筛选状态保留；不采集真实操作。Excel 为有效 OOXML `.xlsx`，导出全部筛选结果和完整长文本。
- 已验证：JS 语法、`verify-local-navigation.sh`、`git diff --check`；jsdom 代码级测试通过，覆盖顶栏插入位置、唯一挂载、模块/类型/日期/账号筛选、日期反序、空结果、分页/每页条数、展开收起、关闭生命周期、保留原 DOM/输入、模拟 Hash/顶栏重建/登录隐藏及全部 15 个入口引用；Excel ZIP CRC/XML、8 列内容及全量筛选记录检查通过。
- 测试命令：`NODE_PATH=/tmp/gaip-operation-log-tests-20260831/node_modules node scripts/test-operation-log.cjs`。`jsdom@26.1.0` 仅安装在临时测试目录，项目运行时没有 npm 依赖或服务器要求。
- 补充验证：73 条 CSS 规则可解析；独立 openpyxl 读取导出的 XLSX 成功，校验了 ZIP 完整性、8 列、中文/换行、冻结表头及以等号开头文本不被解释为公式。
- 未验证：浏览器工具禁止访问目标 `file://` URL，未绕过。jsdom 不验证像素布局、原生 dialog 顶层/焦点循环或真实 Umi 行为；需手工双击工作台，打开日志检查，关闭后依次切换客户中心/保单/学习中心，再从另一个 HTML 切回工作台。筛选、输入和页面滚动应保持，导航不能整页刷新。

## 操作日志入口视觉微调

- 状态：已实现，待视觉确认
- 目标：仅改善顶部操作日志按钮的视觉，改为中性色轻量工具入口；保留文字、位置及所有交互。
- 范围：`shared/styles/global-operation-log.css` 的入口规则、`shared/scripts/global-operation-log.js` 的图标路径、新增 `shared/assets/operation-log-history.svg`（Lucide history，含 ISC 授权）；15 个根入口的日志 CSS/JS 缓存号更新为 `20260831-2`；同步本记录、全局框架笔记和 `PROJECT_STATE.md`。不调整弹窗、数据或导航逻辑。
- 规格：32px 高、8px 圆角、13px 文字、15px 线性历史图标、6px 图文间距；默认浅灰底与中性灰文字，悬停/展开时浅绿底，保留键盘焦点框与减少动效支持。
- 验证：日志代码级回归、导航检查和 diff 检查通过；CSS 尺寸、SVG 结构和 15 个入口缓存版本检查通过。未重新尝试被策略阻止的 file:// 浏览器操作，视觉效果待用户确认。

## 操作日志按钮标注调整与日期换位

- 状态：已完成代码调整，待视觉确认
- 目标：按钮字号 14px、圆角 32px、桌面左右内边距 12px；全局顶栏顺序改为操作日志 → 日期 → 用户区。
- 文件：`shared/styles/global-operation-log.css`、`shared/scripts/global-operation-log.js`、`scripts/test-operation-log.cjs`；上文列出的 15 个根入口仅将日志 CSS/JS 缓存号更新为 `20260831-3`；同步本记录、全局框架笔记及 `PROJECT_STATE.md`。保留 1000px 以下左右 6px、600px 以下仅图标的规则，不改弹窗与导航。
- 实现：按钮 DOM 顺序在日期前，日期缺失时回退到用户区前；重绘或外部挪动按钮后自动校正，复用原按钮、不重复绑定。
- 验证：日志代码级回归、导航检查和 diff 检查通过；新增顺序/重绘/校正断言，字号 14px、圆角 32px、左右 12px、高度仍为 32px 的 CSS 检查与 15 个入口版本检查通过。浏览器 file:// 策略限制仍在，未绕过，实际视觉与 Umi 切换仍待手工验证。

## 操作日志附件图标与顶栏对齐

- 状态：已实现，待视觉确认
- 目标：使用用户提供的操作日志 SVG，图标 16×16px、图标与按钮文字 #2F3640；操作日志/日期/用户区统一高度、居中和组间距。
- 文件：新增 `shared/assets/operation-log.svg`（原附件完整 SVG），移除旧 `shared/assets/operation-log-history.svg`；修改 `shared/styles/global-operation-log.css`、`shared/scripts/global-operation-log.js` 图标路径、`shared/styles/channel-foundation.css` 顶栏规则、`scripts/test-operation-log.cjs`。上文列出的 15 个根入口更新日志资源版本为 `20260831-4`、框架 CSS 版本为 `20260831-1`；同步本记录、全局框架笔记及 `PROJECT_STATE.md`。原下载附件不改动。
- 规格：图标 16×16px、opacity 1；图标/按钮文字 #2F3640，悬停也不变色。三组统一 32px 高、垂直居中，文本行高 20px，组间距桌面 20px/1000px 以下 12px；用户组内间距仍为 8px。保留现有顺序、日期颜色、头像尺寸、弹窗与导航。
- 验证：日志代码级回归（含 SVG/颜色/尺寸/高度/组间距新断言）、导航静态检查和 diff 检查通过。浏览器 file:// 限制未绕过，实际像素对齐和 Umi 切换仍待手工确认。

## 用户区左侧分割线

- 状态：已实现，待视觉确认
- 目标：仅在全局顶栏用户区左侧增加细分割线，保持现有组间距、对齐与交互。
- 范围：`shared/styles/channel-foundation.css`、15 个根入口的框架 CSS 缓存号、代码级测试；同步本记录、[[../公共模块/全局框架与样式|全局框架与样式]] 和 `PROJECT_STATE.md`。不改导航或业务页面。
- 实现：用户区左侧 1×18px 浅灰竖线，垂直居中，位于已有间距中点；桌面偏移 -10px，1000px 以下偏移 -6px。伪元素不占布局空间、不接收点击；框架 CSS 版本更新为 `20260831-2`。
- 验证：日志代码级回归（含分割线 CSS 规则断言）和导航静态检查通过；真实浏览器视觉仍待手工确认，未绕过此前 file:// 访问限制。

## 用户区分割线通高与留白

- 状态：已实现，待视觉确认
- 目标：分割线贯穿顶栏，扩大线两侧的等距留白。
- 范围：共享框架 CSS、15 个根入口框架 CSS 缓存号、日志样式测试；同步状态与框架笔记。不改导航、按钮或业务内容。
- 文件：`shared/styles/channel-foundation.css`、`scripts/test-operation-log.cjs`、上文列出的 15 个根入口（框架 CSS 版本 `20260831-3`）、本记录、`PROJECT_STATE.md`、[[../公共模块/全局框架与样式|全局框架与样式]]。
- 实现：线宽 1px、高度沿用顶栏 token（56px），两侧留白桌面各约 24px、1000px 以下各约 16px；日期与日志原有间距、用户内部间距不变。
- 验证：日志代码级回归和导航静态检查通过；分割线高度 token、外边距与窄屏规则断言通过。浏览器视觉及真实 Umi 切换仍待手工确认，未绕过此前 file:// 限制。

## 2026-08-31 正式内容合并发布

- 状态：待发布。
- 目标：核对本地实际差异，同步所有已修改的正式网站内容到 GitHub，排除登录视频测试入口及素材、AI Agent 试验素材、资讯参考站分包。
- 范围：上文工作台、百宝箱、操作日志与顶栏更新；补充纳入全局海报分享组件及模板、资讯中心分享接入、全局组件预览和已有产品详情 Mock/样式变更。
- 发布补齐：统一 14 个正式入口的 `local-preview.js` 缓存版本；产品详情跨入口样式缓存；同步资讯与产品页面笔记、全局框架笔记和项目状态。
- 边界：现有 `web/p__dashboard__product__index.48332667.chunk.css` 为用户本轮已修改内容，本次同步保留、不继续修改该构建产物。
- 验证：日志代码级回归与导航检查通过；浏览器确认工作台、日志弹窗、工作台/资讯海报分享与产品详情；工作台 → 资讯 → 产品、客户入口 → 工作台 → 资讯 → 保单 → 学习的主文档 loaderId 不变，日志按钮唯一。原生 Esc 和焦点循环未获可靠自动化结论，保留手工确认项。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
