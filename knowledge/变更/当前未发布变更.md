---
type: change-log
status: active
updated: 2026-09-16
---

# 当前未发布变更

## 学习中心直播横卡与开播状态（2026-09-17）

- 直播标签已改为黑底，文字与视频图标统一为用户指定的 `#EFCE88`；仅修改项目内SVG副本填色，下载目录附件与图形路径不变。标签尺寸、间距和卡片内容不变。独立浏览器配色断言及布局回归、导航检查通过，缓存/索引与视觉记录已同步。

- 已完成：直播标签接入用户“形状.svg”（独立素材 `assets/learning/live-video-label.svg`，与附件 SHA-256 一致），横卡封面占比提升至46%，仍按750:320完整显示；第一场公开标题为“GLORY产品及服务说明会预告：全球视野 · 传承洞察”，简介为“每周四早八点不见不散”。新数据直接使用新文案，旧示例仅一次性更新已知默认文案，保留自定义内容、时间、图片和历史日志。单场半宽、切换控件、已有标题字号及课程数据不变。迁移/导航/语法及独立浏览器320–1920px检查通过，已查看桌面新图标和完整标题；视觉台账、缓存与弹窗索引已同步，完整file页面待刷新验收。

- 已完成共享轮播切换：新增 `shared/scripts/global-carousel-controls.js` / `shared/styles/global-carousel-controls.css`，活动与直播同工厂接入，活动原生 Slick 指示器及原轮播行为保留。直播使用圆形箭头与浅底短条指示点、手动循环按组切换，单组隐藏；卡片素材、布局及数据不变。组件目录新增同源“轮播切换”预览，接入说明 `docs/carousel-controls-spec.md`，视觉参数 `design-changes/shared-carousel-controls.json`。频道登记与所有实际缓存消费者同步。
- 本轮验证：独立 Chromium 覆盖活动原箭头计算样式一致、原生指示器无重复、键盘/循环/销毁/重挂载/窄屏/减弱动画及真实目录预览；直播覆盖控件点击与焦点、单/双/空/末组、权限与数据保留、320–1920px。已查看末组与窄屏、组件目录截图。导航、组件目录 DOM、表单来源回归、弹窗索引及语法检查通过；完整 file/Umi 页面仍待刷新验收，未发布。

- 第二个直播示例封面已替换为用户附件“2个嘉宾直播750X320banner.jpg”（750×320原图），保留第一张已替换图片、课程原图和文字/时间配置。原占位图一次性迁移，用户上传图片与删除记录不受影响；素材 SHA-256 一致，Mock 迁移、独立浏览器及导航检查通过。

- 已将首个直播示例封面替换为用户附件“产品服务说明会banner.jpg”（750×320），新增独立素材 `assets/learning/live-product-service-20260917.jpg`，与附件 SHA-256 一致，不改课程共用原图。一次性迁移仅替换仍使用旧占位图的目标记录，保留用户自行上传、删除结果、其余字段及日志。Mock 迁移/独立浏览器/导航检查通过，已查看新图加载截图；完整file页面待刷新验收。

- 本轮已完成：直播卡片 hover / focus-visible 与课程卡片共用 `learning-center.css` 规则，移除直播独有的绿色边框反馈；同为白底、上浮 2px、相同阴影，封面放大 1.018 倍及相同过渡时长。键盘聚焦共用青色内描边，布局及业务逻辑不变。独立 Chromium 对比两类卡片 hover/focus 计算样式相等通过，已查看悬停截图；缓存、索引、导航及视觉记录同步。

- 本轮已调整：按用户反馈取消单场通栏，单场与双场共用相同卡片宽度及内部布局，靠左保留右侧空白；开播状态、权限和数据逻辑不变。独立浏览器确认单场桌面半宽/左对齐及窄屏自适应通过，已查看最新单场截图；缓存及视觉记录同步。

- 已完成本地实现：内容区直播横卡单场半宽靠左、双场并排、更多按组切换、无直播整体隐藏；桌面固定头部高度及课程数据不变，窄屏标题/管理按钮换行。独立公开标题、简介及开播时间复用原共享编辑弹窗；倒计时原位替换“直播中”，不使用广告上架时间推断，未配置时显示待定。
- 范围：Documents 原项目学习中心直播模块、首页挂载、专项验证及视觉台账；不发布、不重置用户数据。
- 文件：`features/learning-center/learning-app.js`、`learning-live.js/css`、`learning-live-data.js`、频道登记及其缓存消费者；自动弹窗索引已重新生成。展示迁移只补三条已知 Mock 的缺失字段，保留原数据和删除结果。
- 验证：领域/Mock/课程管理 DOM 回归、弹窗索引及预览、导航保护和语法检查通过；独立 Chromium 验证单/双/无直播、分页、开播状态边界与焦点、权限、数据保留、320–1920px容器排版及共享表单保存。已查看单场、双场、窄屏及编辑器截图；完整 file/Umi 页面仍待用户现场验收，未接入真实直播状态或 ID 播放地址。
- 视觉参数：`design-changes/learning-live.json` 新增横卡相关记录；旧标题区试版仍保持撤回，不恢复原轮播视觉。

> 本次薄荷入职引导与共享专家库已同步GitHub，详见 [[2026-09-16-薄荷入职引导与共享专家库更新]]；下列其他任务和候选文件仍未发布。

> 2026-09-15 此前的正式功能和样式更新已发布，详见 [[2026-09-15-学习中心与共享组件更新]]、[[2026-09-15-配置中心Tab与产品卡片更新]]。本页记录后续本地修改及用户明确排除的候选文件。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：只有用户后续明确要求时才可单独提交，不得混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：
  - `AI Agent/素材/creative-8.2ab723e3.png`
  - `AI Agent/素材/creative-8.70d73cd4.webp`
  - `AI Agent/素材/creative-9.1a6c0a5a.webp`
  - `AI Agent/素材/creative-9.e00adc24.png`
  - `AI Agent/素材/备份.mp4`

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面仍只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`，发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 共享折叠多选与筛选器样式统一

- 状态：已完成，仅本地修改，未提交或推送。
- 目标：共享多选的边框状态、箭头、选项视觉与筛选器一致；不改选择逻辑、页面布局或数据。
- 范围：共享多选 CSS、资源缓存版本及相关测试；视觉参数记录于 `design-changes/shared-multi-select.json`。
- 实际文件：`shared/styles/global-multi-select.css`、`shared/config/channels.js`、`全局组件/index.html`、`scripts/test-multi-select-style-browser.cjs`；筛选栏原有 CSS 与业务 JS 未改。
- 验证：Chromium 独立多选/筛选多选的默认、hover、展开、focus、选项与箭头计算样式对照通过；选择和 Esc 关闭通过。原筛选栏 Chromium 交互与 320–2200px 容器回归、筛选 DOM、组件目录 DOM、导航静态检查及 diff 检查通过。
- 边界：未对完整业务页逐页视觉验收。关联 [[../公共模块/全局框架与样式|全局框架与样式]]。

## 所属组织选择树默认收起

- 状态：已完成，仅本地修改，未提交或推送。
- 目标：首次打开只展示顶级组织并保持父节点收起；已有下级选中值时仅展开其祖先路径；搜索仍自动展开匹配路径。
- 实际文件：`shared/scripts/organization-tree.js`、`shared/config/channels.js`、`全局组件/index.html`、`docs/filter-bar-spec.md`、`knowledge/公共模块/全局框架与样式.md`及组织树/学情专项测试。
- 验证：共享树 DOM 回归确认空值只显示 6 个顶级组织、父节点默认收起、下级已选值只展开祖先且自身仍收起；真实学习中心 Chromium 回归确认所属组织首次打开状态、搜索、选择、清除、键盘与学情筛选链路正常；配置中心组织管理、全局筛选器、频道导航回归通过。
- 边界：不改组织数据、选择结果、配置中心组织架构管理树及其他筛选器交互。

## 下划线 Tab 数量后缀使用常规字重

- 频道同步：已完成。产品中心复用原React数量外层span，仅设置后缀常规字重并保留源非换行空格；组织架构与学情统计继续使用同一共享皮肤。三个频道和组件库 CSS 缓存版本统一为 `20260915-tabs-channels-6`。
- 频道验证：产品源结构已对照实际构建代码更新专项夹具，原数量外层span及计数节点不重建；独立Chromium字重/键盘/静态下划线通过。组织架构与学情统计真实控制器浏览器回归通过。Chrome119按既有边界跳过嵌套组织popover；完整入口手工浏览未替代为夹具结论。

- 补充：标签与数量后缀使用明确间距，避免 flex 子项边缘的文本空格不显示；参数并入 `design-changes/shared-tabs.json`。

- 状态：已完成，仅本地修改，未提交或推送。
- 目标：纠正此前理解；“保险”标签选中时加粗，“(24)”括号及数字始终常规字重，包含共享组件及现有产品分类。非当前项、颜色与静态下划线不变。
- 范围：共享 Tab 工厂为数量后缀生成独立 span、对应 CSS、引用缓存、专项回归和 `design-changes/shared-tabs.json`；撤销上一轮整项取消加粗的规则，原产品 React 节点不拆改。
- 验证：`test-tabs-browser.cjs` 独立 Chromium 实际计算字重、选中/切换/hover、原节点事件、静态下划线、窄屏及键盘通过；导航静态保护通过。完整业务页未逐页视觉验收。规范见 [[../../docs/tabs-spec|下划线 Tab]]。

## Tab 组件预览展示分离

- 状态：已完成，仅本地修改，未提交或推送。
- 原因：两组 Tab 与未设置专属间距的说明段落直接混放在同一白卡内。
- 目标：两组真实组件独立白卡，状态与操作说明置于卡片外；不修改业务 Tab 样式与行为。
- 实际：修改目录展示 JS/CSS 与缓存，补充目录 DOM 回归；参数见 `design-changes/component-catalog.json`。
- 验证：目录 DOM、导航静态、弹窗登记与预览检查通过；独立 Chromium 加载真实目录源码，1440/480px 两卡与说明无重叠、无横向溢出；已查看桌面截图。字重规则随后按数量后缀专项纠正，用户当前标签页需刷新。

## 下划线 Tab 新增薄荷入职引导及方案中心

- 状态：已完成，仅本地修改，未提交或推送。
- 目标：入职顶部章节与方案中心视图切换同源；目录增加两处使用位置。入职胶囊小节、进度、锁定点击反馈和方案业务内容保留。
- 范围：共享Tab适配器/皮肤、频道资源登记、方案数量后缀结构、组件目录与专项测试。
- 验证：新增原页面结构浏览器夹具通过（共享样式、旧CSS晚加载、数量字重、章节锁定/解锁、鼠标与键盘、原节点身份及胶囊不接管）。此前三频道Tab浏览器、目录DOM、JS语法及导航静态检查通过。非完整业务页面验收。
- 参数：`design-changes/shared-tabs.json`；关联 [[../页面/薄荷入职指引]]、[[../页面/方案中心]] 与 [[../../docs/tabs-spec|Tab规范]]。

## 新任务追加格式

## 学习中心 V1.2 直播 Banner（2026-09-16）

- 最新要求已完成：首页恢复加入直播前的课程布局，撤下直播广告、直播管理入口和试版分区/标题布局；保留默认超级管理员及隐藏 Mock 栏，不删除课程、学习记录或直播示例数据。`learning-app.js` 恢复原列表结构，`learning-center.js`/`learning-live.css` 撤去顶部试版覆盖，更新实际消费者缓存与记录。独立浏览器验证原头部尺寸、学员权限、保留 8 条直播示例且首页不展示；课程管理 DOM 回归通过。下列首页直播描述为撤回前历史，不是当前入口状态。
- 本轮完成：首页明确直播/课程分区，管理按钮移至标题下，删除 Mock 身份栏渲染并在本地页面首次挂载时默认超级管理员；保留实际权限判断与课程数据。`learning-app.js`、`learning-live.css` 及实际消费者缓存同步；独立浏览器验证分区标题、按钮对齐、无直播隐藏、默认身份与多宽度边界，参数合并至 `design-changes/learning-live.json`。
- 紧凑布局：直播轮播并入标题右侧，管理入口按权限显示，无权限时不占位，窄容器换行。顶部按可见宽度适配，课程列表原有桌面最小宽度不变。保留素材比例和直播数据，视觉参数合并到 `design-changes/learning-live.json`。独立 Chromium 覆盖学员/管理员位置一致、320/480/900/1440px、宽画布下顶部边界及无直播状态；完整 file 业务页仍待验收。
- 追加 Mock：8 条直播示例（3 已上架、3 草稿含排期/待完善、2 已下架）及 15 条模拟日志。`learning-live-data.js` 使用一次性增量批次，保留用户已有记录、编辑、删除及课程数据；图片沿用本地素材占位。新增批次回归，并同步登记/入口缓存；无视觉样式修改。
- Mock 验证：增量批次/既有领域测试、独立 Chromium 示例数据（轮播图片加载、8 条列表、模拟日志）、登记和导航静态检查通过；未直接修改用户浏览器存储，刷新原项目后自动补入。
- 状态：本地实现与专项回归完成，仅 Documents 原项目；桌面重构版不改、不发布。完整业务页视觉与实际接口仍待验收。
- 用户确认：保存草稿不自动上架；确认上架后才进入定时流程。Banner 750:320，名称仅后台使用；不接真实上传、诺学堂或真实播放。
- 范围：首页独立 Banner、直播管理、共享筛选/表格/弹窗与合并日志、本地状态机及回归；保留课程和学习记录。
- 已知接口缺口：直播 ID 到播放地址的转换规则未提供，本地仅提示未接入，不编造播放地址；本地排期只在页面运行/重新打开时执行，不能冒充服务端任务。
- 实际文件：新增 `learning-live-data.js`、`learning-live.js`、`learning-live.css`；`learning-app.js` 接入入口、生命周期和合并日志；共享表格增加可选不分页。更新频道登记/消费者缓存、5 个弹窗源登记与自动索引，保留既有用户改动。
- 验证：V1.2 领域/独立 Chromium 专项、V1.1/课程管理、课程日志浏览器、弹窗预览与登记、导航静态检查通过；31 条不分页及默认分页同时覆盖。未验证完整 Umi/file 业务页。
- 交接：[本地实现与边界](../../docs/learning-center-v12-local.md)；视觉记录 `design-changes/learning-live.json`。

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
