---
type: current-changes
updated: 2026-09-10
---

# 当前未发布变更

## 组件库筛选器直接搜索演示

- 范围：将共享组织树组合输入框从筛选器演示的“更多筛选”移到默认可见区域，补充使用说明与显隐开关；不复制控件、不改变业务筛选配置或视觉参数。
- 完成：`全局组件/components-preview.js`默认展示所属组织，新增配置显隐开关；登记描述与入口缓存同步。Chromium真实目录源码专项验证直接输入搜索、选中回填稳定ID、隐藏清空及恢复展示通过；筛选DOM、语法、导航及diff检查通过。完整file页面仍待刷新验收，未发布；原有旧Chrome弹窗兼容缺口不在本次变更范围。

## 组织筛选输入框直接搜索

- 目标：共享treeSelect改为输入框直接输入关键词并展开组织树，移除面板内部搜索；保留节点组件、选择范围、重置及弹窗兼容，验证键盘/输入法/未确认搜索恢复。
- 已实现：`shared/scripts/global-filter-bar.js`使用可编辑combobox；`shared/styles/global-filter-bar.css`沿用同源40px控件，清除与箭头留独立空间。学情主页面/课程详情/组件库共享实现；注册表、16个根入口、组件库资源及真实弹窗索引同步缓存版本。
- 验证：筛选DOM、组织迁移/权限、当前Playwright Chromium学情主页面/两类详情/日志和组件库筛选器专项通过；直接输入、IME、键盘选择、未确认恢复、清除/重置、弹窗隔离均覆盖；导航、JS语法、索引和diff检查通过。完整file入口仍待现场刷新验收；`test-popup-preview.cjs:108`仍为既有配置中心版本断言失败，不修改无关断言。
- 样式参数见`design-changes/shared-filter-bar.json`，交互契约见`docs/filter-bar-spec.md`；仅本地修改，未部署。
- 额外兼容缺口：本机Chrome 119在课程详情弹窗内从组织输入框按Escape会出现renderer crash；当前Playwright Chromium同场景通过。未将未验证的临时兼容方案写入生产代码，旧内核仍需单独排查；不能将当前Chromium结果宣称为全浏览器通过。

## 弹窗筛选器重复增强修复

- 范围：排查两类学情详情筛选栏重复下拉、搜索图标重叠及控件错行；明确全局筛选器自己拥有控件，避免被弹窗自动增强器再次接管。补齐正式弹窗增强脚本共同加载的回归，不改筛选业务和设计参数。
- 原因已复现：原学情独立测试漏加载`modal-controls.js/css`，实际页面会对筛选器的原生select再生成代理，对搜索输入再加图标。补齐脚本后新增断言先失败（出现2个二次增强节点），修复后通过。
- 完成：`global-filter-bar.js`使用既有控件/字段排除契约，只保护自身form；未关闭全局增强器，未改CSS参数。两类详情、组织树弹出搜索与重置保持原行为；组件目录、业务与真实源预览同步缓存`20260910-modal-filter-1`。
- 验证：包含正式弹窗增强层的学情浏览器专项、40px控件/重置同排、无重复代理/搜索背景、组织树和Esc层级及筛选DOM回归通过；两类截图已复核。索引/语法/导航通过；预览全套仍停在原配置中心版本断言108行。完整file入口待刷新验收，本地未发布。本次是接入逻辑修复，视觉参数未变，不新建视觉差异项。

## 学情详情改为共享信息弹窗

- 范围：学员/课程学习统计的“详情”由右侧抽屉改为居中信息展示弹窗，复用共享外壳、标题、关闭、遮罩和定位；保留字段、筛选、分页、统计与权限，同步真实源预览及视觉记录。
- 完成：两类详情共用`openStudyDetail`，接入`.gaip-modal`与共享关闭/定位；保留全局筛选及表格，关闭恢复触发焦点和主表状态。原抽屉登记迁为`learning-study-detail`、`learning-course-study-detail`两条信息预览，真实源和组件库同入口。原生backdrop未继承root变量导致透明的问题由共享遮罩加原规范值兜底修复，未改配色规范；缓存同步所有实际入口及预览。
- 验证：真实控制器Chromium专项、两类预览入口、筛选/分页、关闭重开/Esc/焦点、480–1600px居中安全边距及截图通过；V1.1业务DOM、确认场景DOM、索引生成/检查、语法及导航保护通过。目录新增条目检查通过后，全套预览仍停在配置中心旧版本断言（108行），未放宽。完整file入口仍待现场验收，本地未发布。视觉记录：`design-changes/learning-center.json`和`shared-modal-mask.json`。

## Tab 切换动画接入修复

- 范围：学情管理和组织架构切换时保留共享 Tab 实例，仅更新下方内容；检查产品中心原有节点更新方式。不改变共享样式、动画参数、业务筛选规则或数据。验证实际过渡中间帧及连续切换。
- 完成：`learning-app.js`和`config-center.js`保留已连接的头部/Tab节点，按原语义更新内容，组织头部按钮防重复；产品源码稳定key/class更新，无需改写只读基线。两个脚本、入口注册表与弹窗源缓存同步为`20260910-tabs-motion-1`，共享CSS/JS未变。
- 验证：三组独立Chromium测试通过（真实学情/组织控制器，产品原结构夹具），实测过渡中间帧、稳定实例、快速反向切换；学情完整专项、组织共享节点DOM、语法、导航保护、弹窗索引检查通过。`test-config-center.cjs`仍在未修改的表单CSS断言失败（要求`padding:0 24px 24px`，214行），未放宽断言或改无关CSS，不宣称全套通过。完整file入口仍待用户现场刷新验收；仅本地，未发布。

## 两类学情统计与详情展示统一

- 目标：同时整理学员/课程统计表格及详情抽屉的对象信息、筛选状态、长文本、数值列和标签；复用全局表格/筛选，不删字段、不改统计口径、权限、Mock与导出。保留详情抽屉形式和已统一的顶部Tab/日志。
- 完成：`learning-app.js`复用文本/身份/标签渲染，两类主表与详情采用共享表格的顶部对齐和数值列右对齐；名称、组织和群组两行省略并保留全文title。详情顶部明确学员账号/组织或课程属性；学习状态并入共享筛选栏，重置同时清空搜索、组织与状态。`learning-v11.css`统一信息区、表格间距及响应式抽屉，不改全局组件默认样式。主表/详情字段、统计计算、导出与权限条件未删改。
- 验证：真实控制器独立Chromium回归通过，覆盖两类详情、状态筛选/清空、空态、原数据不变、主表分页/筛选保留、组织范围、导出、焦点恢复、Esc及480–1920px边界；两类详情截图已查看。语法、导航保护、索引生成/检查及diff通过。全站弹窗预览测试停在原配置中心版本断言（仍要求20260908-inline-validation-1，现有配置源为20260910-org-1），本次未改配置源码或放宽测试，不宣称全套通过。完整file入口仍受访问策略限制待刷新验收。
- 参数已合并`design-changes/learning-center.json`；更新频道及16个HTML消费者缓存、学习弹窗源资源版本和生成索引。本地未发布。改动前源码/CSS/测试/台账副本保存在`/tmp/gaip-study-before.Igx6cy/`，用于本轮对照回退，不覆盖其他任务修改。

## 三频道Tab统一组件

- 目标：以组织架构/学情现有下划线Tab为基准，将产品中心、组织架构、学情统计接入共享组件并登记真实组件预览。保留页面布局位置、产品数量和业务切换，不重建React拥有的产品节点；统一字号、文字色、间距、指示条与动画，支持键盘及减少动态效果偏好。
- 完成：新增`shared/scripts/global-tabs.js` / `shared/styles/global-tabs.css`，新实例mount、现有节点enhance；三个适配器保留业务处理器，观察重建并清理旧实例/恢复键盘焦点。学习页移除独立Tab皮肤，组织保留不滚动的工具栏，产品保留装饰与数量。频道注册表、16个实际HTML消费者缓存及目录引用同步；目录新增“下划线 Tab”真实示例（普通、数量、禁用）。未改只读web构建包。
- 验证：独立浏览器三处真实CSS计算样式、加载顺序、原产品节点/点击、键盘、禁用、重建、减少动态效果及窄屏通过；真实学情控制器浏览器回归、真实组织控制器渠道切换/焦点/数据DOM回归、目录真实挂载与切换检查通过。目录测试限定导航Tab选择器，避免将新预览内Tab错误计入目录导航。完整file页面仍受访问策略限制待现场视觉确认；本地未发布。
- 规范见`docs/tabs-spec.md`，参数见`design-changes/shared-tabs.json`；学习页已有台账保留首次本地基准、更新为共享来源。项目状态和三页/公共模块笔记已同步。

## 虚拟频道统一清理旧导航选中态

- 目标：将学习中心单频道保护统一为注册表内虚拟频道的公共状态保护，覆盖资讯、财富值/配置全部子页；保留真正hover、键盘active、父项展开、路由和业务逻辑。
- 完成：`shared/scripts/learning-nav.js`用注册表的virtual标记及直属`data-gaip-channel`菜单确定当前归属，替代上一轮学习专用class观察。清除归属外的旧选中态，叶子同步选中及ARIA；组内子项/展开/aria-current仍由真实控制器管理，未知或未挂载频道不接管。不改CSS与导航事件，16个HTML引用同步缓存。
- 验证：`test-virtual-nav-state.cjs`加载真实资讯/财富/配置导航控制器，在登录与财富值两个内存DOM入口覆盖8个虚拟频道/子页、共216次旧菜单class重写、全部8个原生频道返回、父项纯开合及观察器静止；学习原回归、可展开主导航/键盘回归、导航静态保护和语法/diff检查通过。原可展开导航测试补载项目新引入的organization-store/tree依赖，断言未放宽。仅DOM与源码核验，真实鼠标/像素尚待验收；未提交、未推送。

## 学情顶部Tab与组织树筛选

- 用户已确认同步关联Mock组织，现已完成：共享`organization-store.js/organization-tree.js`由配置中心主树和学情`treeSelect`共用；原主树管理菜单及排版保留，下拉紧凑展示并支持节点搜索、祖先展开、空态、长名路径提示、键盘/关闭/清空。主学情及课程详情抽屉接入；组件目录增加真实示例。按稳定ID迁移已知Mock旧归属与管理范围，保留旧路径、账号、课程和学习记录；未知自定义归属不猜测。配置改名通知同源消费者，引用节点阻止删除。
- 目标：统计维度改为返回栏中间的下划线Tab；组织筛选期望搜索节点与树形展开并复用组织架构。源码核对发现两模块Mock组织尚未关联，已询问是否同步迁移组织归属；在确认前不改人员组织和学习记录。
- 本轮完成Tab：复用配置中心下划线Tab视觉参数，移至返回栏中间；窄屏另行居中，保留日志/导出，支持左右/Home/End键，重复点击当前Tab不清空筛选。修改learning-app.js/learning-v11.css及频道缓存，视觉记录合并至learning-center.json。
- 核验：独立控制器1920–692px居中/无重叠、Tab键盘、组织树搜索/父子选择/空态/Escape/面板边界及原筛选/表格/日志/导出回归通过；共享目录浏览器回归通过。新增迁移测试验证记录不变、重复加载、范围权限及改名；配置主树针对性DOM测试验证折叠、改名与共享选择器。原配置中心大套件停在既有表单CSS旧断言（期望padding0 24px 24px），本轮未改该CSS，不宣称全套通过。领域、筛选DOM、目录/预览、导航和语法检查通过。完整file入口受策略限制待现场验收，本地未部署。
- 组织树视觉参数新增`design-changes/shared-organization-tree.json`；控件外框仍继承共享筛选栏。源码/频道缓存、组件目录和弹窗预览依赖同步；配置中心节点仍延续会话内存模式，并未改为服务端或永久组织存储。

## 学情管理接入课程管理同源组件

- 目标：统一返回栏操作、白底、筛选及统计/详情/日志表格；日志改信息弹窗，保留原字段、搜索、统计口径、范围权限和导出。课程管理业务不改。
- 已完成：返回栏右侧放日志/导出，白底及上分割线沿用课程管理；两维度表格和详情抽屉均挂载全局表格/筛选栏。即时筛选只更新结果，切维度销毁旧实例；必修保留全部/是/否，群组保持原单选语义。学情日志与课程日志共用信息弹窗工厂，原六字段/搜索及导出日志权限不变，关闭保留底页状态；长详情复用四行展开。新增真实源登记46号。
- 核验：V1.1领域/DOM、课程管理DOM、学情浏览器（筛选/分页保留/详情/导出/权限/空态/1920–692px/销毁）、课程日志浏览器、目录/预览、导航及语法检查通过，独立截图已检查；课程DOM旧日志表头断言同步此前已确认的字段名。完整file入口被浏览器安全策略阻止，未通过其他方式绕过，待用户刷新现场验收。本地未部署。
- 修改前快照：项目集`本地回退备份/学情管理组件统一-20260910-K1BhDb/`。视觉参数仅记`design-changes/learning-center.json`本次study记录；源码为learning-app.js/learning-v11.css、频道缓存及生成目录，未改学习数据层或全局组件样式。

## 日志文本左对齐与行顶部对齐

- 已完成：展开/收起按钮padding归零；全局表格新增可选rowVerticalAlign，默认middle，课程日志显式top，仅影响正文td，不改表头。其他实例默认样式不变，文档与频道缓存同步。
- 核验：实际Mock展开状态下按钮与正文左边缘一致、日志全部单元格top、底页课程表仍middle通过；截图已查看。全局表格浏览器回归（1440/1100/800宽度）、目录/预览、导航、语法和diff检查通过；旧分页测试课程补齐required:false以满足现有必填校验，未改业务规则。视觉记录更新按钮padding，垂直对齐配置作为组件行为记入table-spec；本地未部署。

## 日志展开收起演示数据

- 已追加6条“展开演示”Mock，涵盖双侧长文本、短变长、长变短、多段换行、课节排序、学习群组及短字段。首次加载用当前时间列入近期日志，批次标记与固定ID去重；不改变课程、旧日志或交互样式，数据脚本缓存同步。
- 核验：从旧Mock增量迁移不改课程与旧日志，重载不重复；使用实际交付Mock的Chromium检查首屏入口、独立展开/收起、四行折叠、多段换行和短长对照通过，截图已检查。数据回归/语法/diff/导航保护通过；纯数据修改无新视觉参数，本地未部署。

## 课程日志标签跟随组件规范

- 已完成：撤掉分类标签非颜色的局部覆盖，直接继承全局表格标签布局和尺寸，仅保留已确认的分类颜色。不是重新复制组件参数；以后共享标签样式调整将直接作用于该列。
- 验证：独立浏览器与默认tag的11项非颜色计算样式一致，七种颜色及日志长值/分页/生命周期回归通过，截图已核对。目录/预览及导航检查通过，CSS缓存和原视觉记录已同步；本地未部署。

## 课程日志分类标签

- 已完成：分类列调用全局表格tag接口生成静态标签，课程日志局部样式采用配置日志的12px/4px圆角和浅底深字色值；新增/上架/上架课节绿色，编辑蓝色，删除/下架/下架课节红色。其他未知类型沿用neutral，分类原因保留。只接入课程日志，全局表格API及配置中心不改。
- 验证：独立浏览器七类文字/色值、字号/圆角/内边距及截图通过，原长值展开、分页、关闭和窄屏回归通过；目录/预览、导航保护和语法/diff通过。频道缓存与视觉记录同步，本地未部署。

## 课程日志前后值四行展开

- 完成：仅课程日志变更前/变更后采用四行折叠、展开全部/收起全部交互，两格独立，保留换行；按实际溢出判定显示操作，短内容直显。操作对象和其他列、配置中心源码、分页与数据不改；属于页面单元格渲染，未扩展全局表格API。
- 浏览器通过4行高度、独立展开收起、短值无操作、换行/转义、分页后测量、窄屏、关闭与无数据写入回归，已检查截图。目录/预览、语法/diff通过；频道缓存与视觉记录同步，本地未发布。

## 课程日志八列与Mock补充

- 已完成：课程日志无搜索，列名顺序为分类/操作对象/变更字段/变更前/变更后/操作人/操作时间/IP；复用全局表格。信息弹窗用双类选择器保护1200px宽度变量，防止公共默认480px后加载覆盖；窄屏沿用视口约束和表格横向滚动。
- 增量追加24条独立演示日志，涵盖新增/编辑/上下架/课节/删除，操作人为模拟账号，IP为TEST-NET示例地址；批次标记与固定ID双重去重，不覆盖已有记录、不改变课程。当前操作产生的日志仍标本地Mock，不访问真实接口。
- 验证：数据增量/重载不重复、领域规则与旧记录只读通过；浏览器八列/无搜索/公共CSS后加载宽度保护、长值展开、分页、480/900/1440px、Esc/关闭/重开及底页保留通过，已检查截图。弹窗目录/预览回归通过，频道缓存同步，视觉台账已更新；未部署。

## 放弃修改按钮警示线框

- 已完成：未保存确认中的“放弃修改并离开”显式接入共享警示线框按钮，复用操作确认危险色，保持白底和原尺寸；保存按钮及关闭/保存/放弃行为不变，其他次按钮不受影响。频道缓存同步，视觉记录见learning-center.json。
- 核验：8种业务/预览同源场景、弹窗索引与预览回归通过；Chromium共享工厂检查默认/悬停/按下色、保存按钮不变及Esc/关闭/重开，截图已检查。未对完整Umi入口现场复验，本地未部署。

## 上传成功提示图标与文字色

- 已完成：内容/讲义成功提示添加用户“完成.svg”，保留附件青色；文字统一“上传成功，待保存”并使用项目主文字黑色。上传与保存逻辑不变，沿用16px图标与8px间距。四类上传浏览器回归通过，成功图标尺寸/色值/文案和间距断言通过，已检查截图；语法/JSON及导航保护通过，视觉记录同源更新。仅本地未发布。

## 课节上传区状态整理

- 本轮授权：统一文件信息、状态、操作层级，去除重复文件说明；上传/更换使用用户提供的 SVG 并跟随文字色，演示操作收纳。保留本地文件链接、失败保留旧文件、独立保存与锁定逻辑。
- 修改前快照：`../本地回退备份/上传区状态整理-20260910-0ISPPf`；仅本地修改，未部署。
- 完成：课节上传区固定信息/状态/操作层级，去除重复原文件与点击说明；长文件名单行省略并保留完整标题，失败仍显示唯一旧文件链接。上传/更换内联使用用户 SVG 原路径，跟随文字色；演示操作收纳，青色进度和独立保存规则不变。CSS/JS频道缓存同步，视觉参数见`design-changes/learning-center.json`。
- 验证：四类文件真实所选字节链接、新标签打开、失败保留、重试/取消、状态高度一致、图标16px/同文字色、480–1440px边界通过；已检查上传中/成功/失败截图。5个编辑/新增浏览器回归以及V1.1领域、导航保护通过。浏览器为独立本地夹具，未对完整Umi入口现场复验；音视频未验收真实解码，仍为Mock流程。

## 课程预览入口上移与说明精简

- 本轮目标：配置页“预览已保存内容”改名“预览课程”，移到返回栏右侧；移除必修和精选的辅助说明。保留原草稿预览权限、已保存内容及新窗口逻辑，同步首次保存后的顶部入口。仅复用现有工具栏样式，不新增视觉参数。
- 已完成：`learning-app.js`复用现有返回/管理工具栏，编辑重绘同步顶部节点；频道JS缓存同步。浏览器夹具1920/1327/760px核验返回与预览同行、右边界、无重叠，首次保存前不显示/保存后出现，草稿新窗口与数据隔离回归通过；已查看截图。V1.1、课程管理DOM、导航保护和语法/diff通过。本地未发布，快照`../本地回退备份/课程预览入口上移-20260910-xpFtyA`。

## 课节文件预览与编辑细节

- 目标：上传进度使用主色青色、群组提示改为面向用户的说明、移除重复必填说明；修复首次保存后底栏残留新增布局。第5项按用户最新要求改为上传文件名链接，不保留内联预览。
- 回退快照：`../本地回退备份/课节预览与编辑细节-20260910-SyiBrp`。保留上传、保存、锁定和原页面滚动规则；不接真实媒体或线上服务。
- 完成：取消内联示例预览；模拟上传成功后文件名链接打开实际选择文件的Blob新标签页（noopener），不向服务器上传、不嵌入播放器、不调用学习session。Blob/File仅存在当前页内存，保存元数据用独立引用ID；刷新后提示重新选择，不用示例冒充原文。失败/取消保留原文件访问，删除/切换/放弃时清理无引用Blob。首次保存底栏同步class恢复右对齐；上传进度青色，群组文案易读、重复必填说明移除。样式见`design-changes/learning-center.json`。
- 核验完成：Chromium夹具四类上传/重试/文件链接字节长度与前缀/MIME、新标签打开、保存与返回后链接保留、失败保留旧链接、无Blob地址持久化、内存引用丢失后的提示、窄屏边界通过；文件名链接截图已检查。首次保存底栏右边界、V1.1领域/DOM、课程管理与导航保护通过。PDF验证实际PDF字节与新标签创建，未对浏览器内置PDF阅读器逐页验收；音视频测试使用占位字节，不宣称媒体解码已验收。内联预览试验已撤销；本地未发布。

## 新增课程说明对齐薄荷底栏

- 本轮仅将新增课程草稿说明对齐薄荷入职引导底栏的实测字号/颜色/行高及左侧位置；保存按钮保持右侧、逻辑不变，编辑已有课程底栏不变。薄荷源只读。快照：`../本地回退备份/底部说明对齐-20260910-AIxu0c`。
- 完成：使用薄荷底栏实际源码与Chromium计算样式，说明改为左侧垂直居中、按钮右侧；窄屏自动换行保护。已验证字体/颜色/行高、底栏高度及左边距、与按钮垂直中心对齐，并回归新增两种保存/预览/统计链路。视觉参数合并既有说明记录并补底栏差异，未改薄荷源或保存业务，未部署。

## 学习中心切换后旧导航状态残留

- 后续：此单频道保护已由本文件“虚拟频道统一清理旧导航选中态”替换，下述为首次修复记录，不再是当前覆盖范围。

- 目标：修复进入学习中心后，旧频道菜单在悬停重绘时恢复选中态的问题；只修复导航状态同步，不改菜单样式、尺寸、路由和课程业务。验证跨入口与反向切换。
- 原因与修复：学习中心沿用底层Umi路由；旧菜单在Ant悬停/焦点重绘时可能重新写入`ant-menu-item-selected`，原`learning-nav.js`只监听childList，漏掉class变化。共享导航新增仅在学习频道生效的菜单class观察，幂等校正选中态及ARIA，不清除真实hover/键盘active、不修改CSS、不改Hash。所有16个实际引用入口同步脚本缓存；试验入口只改同一引用，不纳入发布授权。
- 验证：新增`scripts/test-learning-nav-state.cjs`在修复前稳定失败（旧选中态残留），修复后通过；覆盖登录/财富值入口的内存DOM场景、各原生频道及资讯→学习、悬停class重写、反向导航、其他虚拟频道不接管及观察器静止。导航父项/键盘回归、学习V1.1业务DOM、导航静态保护、语法和diff检查通过。真实页面鼠标悬停视觉尚未核验；未提交、未推送。

## 课节上传状态演示

- 本轮仅调整课节内容/讲义更换按钮黑底白字，并加入视频/音频/PDF/讲义的本地上传中、成功、失败、重试与取消。保留原文件到更换成功，不读真实媒体、不请求服务器；封面上传沿用原逻辑。使用已有文件区、共享控件和校验样式，补生命周期清理与浏览器回归。
- 完成：临时状态不写入领域数据；默认模拟上传约2.5秒，上传期间“模拟失败”可演示失败，格式/大小失败要求重新选择，模拟失败可重试。成功才替换编辑副本，保存本课节/课程后落盘；上传中阻止保存。切换类型、删除课节、取消、放弃离开及销毁清理定时器，失败/取消不覆盖原文件，原“使用示例”保留快速填充。
- 验证：新`test-learning-upload-browser.cjs`覆盖四类完整过程、取消/原文件保留、并行上传、类型变更/退出/销毁、保存阻止、黑底白字及窄屏边界；既有编辑、滚动拖拽删除、V1.1回归通过。已检查失败截图；视觉参数记录在learning-center.json，未接真实上传、未部署。回退文件：`../本地回退备份/课节上传状态-20260910-yJPBIv`。

## 新增课程流程及9月9日规则落地

- 追加授权：草稿说明移至保存按钮下方；封面建议16:9；学习设置三列辅助文案对齐；新增课节类型默认空选，选择后显示内容上传，编辑已有类型保持不变。此增量沿用同一快照范围。
- 追加已完成：底栏说明在按钮下方；封面仅建议比例，不强制拒绝其他比例；三列说明浏览器实测顶部对齐。新课节类型null，选择前不展示内容上传按钮，校验同源且已有课节不重置。新增流程、类型反馈及布局回归通过；完整file草稿新窗口与模拟播放器入口也已通过，图片/视频仍使用本地Mock，未访问外部HTTP。

- 用户已批准：首次保存前隐藏课节区，两种保存按钮明确草稿/下一步；草稿仅管理列表及配置页新窗口预览；禁止已上架课程最后课节下架；学情及导出排除下架课节，保留历史记录。复用本地 Mock、共享表单及确认组件，不接真实媒体/data ATM，不部署。
- 修改前快照：`../本地回退备份/新增课程流程-20260910-MkpWQM`。保留本地其他未提交变更；本项覆盖下方“待实施”说明，完成后补验收。
- 完成：首次新建只有基本信息，底栏取消/保存草稿并返回/保存并添加课节及轻量说明；成功保存后才显示课节区，继续添加自动定位首课节。用户追加要求的必修单选默认空选，保存前明确选择，校验复用全局字段反馈；已有值不变。管理列表/配置页仅草稿提供同一新窗口预览，保存失败不跳转，未保存输入不进入预览。
- 草稿从学习列表完全排除；新窗口保留当前入口以兼容file存储，noopener且校验本地权限。last-lesson-offline改为中性阻止提示，领域层原子拒绝最后课节下架，不改状态/日志；目录已生成。学情表格/详情/导出用上架课节数据，底层记录保留并可重新参与，未接data ATM。
- 验证：V1.1、管理列表、日志、Mock迁移、8种确认及目录检查通过；独立Chromium覆盖两种保存、必修空选与校验、真实新窗口/无进度写入、统计及导出排除、下架原子保护；编辑器表单、反馈、布局、滚动/拖拽/删除回归及导航保护通过。完整file入口新增页面已检查，视觉参数追加到learning-center.json。测试快照在隔离浏览器，不影响用户日常Mock数据；未部署。

## 移除课程顶部重复保存

- 本轮授权：移除新增/编辑课程标题旁重复保存，保留底部保存及课节独立保存。图示20260909三项规则仅核对差异，未授权在本轮改动相关业务。
- 完成：renderEditor 删除标题右侧 save-course 入口；新增/编辑仍共用底部保存，不改保存规则。浏览器断言两种页面顶部为零个保存、底部为唯一课程保存，并回归上传、独立课节保存及排序；V1.1 DOM、导航保护和 diff 检查通过。仅删除重复入口，无新增视觉参数，未改 CSS。
- 9月9日新图示待实施差异：草稿从学习列表移除，管理列表及配置页增加新窗口预览；新课程首次保存前隐藏课节区，改为“保存，返回课程列表”与“保存，新增课节”，已有课程仍独立保存课节；阻止已上架课程最后一个课节下架（当前会自动下架课程）；统计时长/最近学习等 history 汇总仍含下架课节，需与当前仅上架课节的完成度统计统一，保留底层记录但不计学情展示并补说明。本地没有 data ATM 接口，同步排除须在真实接口侧落实，不能声称本地已同步。
- 回退快照：`../本地回退备份/移除顶部保存-20260910-9wVof8`；未部署。

## 编辑底栏与字段错误反馈

- 本轮仅将底栏按钮恢复为内容区最右对齐（卡片继续居中），并复用全局弹窗的 createFieldFeedback 为新增/编辑课程提供字段错误反馈；保留校验规则、数据与保存权限。
- 完成：移除 footer-inner 的居中最大宽度约束；数据校验错误增加 field/lessonId 定位信息（不改规则、顺序与消息），页面复用共享反馈控制器的 aria-invalid、描述关联、提示清理。校验失败只滚动编辑容器到目标字段，输入修正/重新渲染清理旧反馈；非字段错误仍保留轻量摘要。上传类型/大小错误也落在对应控件下。
- 验证：独立 Chromium 检查最右按钮边距、错误文本与边框颜色/字号/行高/实际间隔、所有基础字段及课节名称/内容、错误清理、重复保存和新增后编辑；编辑、拖拽/滚动/删除回归与 V1.1、课程管理 DOM、导航检查通过。截图已核对，未宣称完整入口通过。
- 视觉参数合并到 learning-center.json；回退快照在 `../本地回退备份/课程编辑反馈-20260910-d5pdZj`，仅用于本次增量回退。

## 课程编辑页面统一

- 范围：新增/编辑课程的课节状态展示、独立底部操作栏、居中内容列、顶部边线及全局表单样式接入；保留 Mock 数据、上下架权限、滚动锚点、拖拽与删除确认。不修改线上环境。
- 完成（2026-09-10）：新增课节按是否已保存、当前状态及 locked 发布历史分别展示未保存/未上架/已上架/已下架，不改领域数据。底栏移到编辑滚动容器外，内容列共享居中轴；顶部线在返回工具栏上方。完整重绘仍保留原 main，并恢复底栏自己的焦点。
- 表单接入显式共享页面适配层 `shared/styles/global-page-form.css`，引用全局表单 tokens 与原多选组件；移除编辑器覆盖输入框/原生单选/多选选中态的旧样式。页面没有添加 modal 容器或观察器，文本域保留仅纵向调整及计数避让；不修改其他频道布局。
- 已通过：课程编辑、交互浏览器回归，新增布局浏览器回归（含四类标签、底栏边界/焦点、不同宽度居中、控件计算样式）；V1.1、课程管理 DOM 检查及导航保护。独立控制器截图已检查；完整 Umi/file 页面因预览工具超时未复验，截图中本地图片/图标不作为资源加载验收。
- 视觉台账：`design-changes/learning-center.json`合并既有属性，补共享适配层记录。回退快照在来源外 `../本地回退备份/课程编辑统一-20260910-sCigvk`，按本次增量回退，不覆盖其他任务变更。

## 课节编辑交互补齐

- 完成（2026-09-10）：按钮明确为保存课程/保存草稿、保存本课节、保存排序及按类型上传/更换，讲义操作明确对象。`renderEditor`保留原滚动容器，`keepEditorView`恢复内外滚动与焦点；类型/示例/讲义只更新当前课节，上下移动复用原卡片并维持视口锚点。
- 拖拽改为Pointer Events：手柄捕获指针，超过移动阈值后显示不可交互整卡副本、原位占位与插入线，支持编辑容器边缘滚动；松手提交本地顺序，Esc/失焦/指针取消/列表外释放清理且不改顺序，保存排序仍独立。视觉参数在`design-changes/learning-center.json`，不引入动画依赖或改变学员页面。
- 删除课节调用同一`confirmScene`及全局危险确认外壳，显示课节名称、取消/删除课节，确认仅移除编辑列表，保存课程才落盘；已有发布课程的课节仍禁止删除。源登记追加`learning-lesson-delete-confirm`（45），目录现为63/45/0/18；索引与学习资源缓存同步。保留并行任务的“删除草稿”及未保存离开双按钮结构。
- 验证：V1.1/课程管理DOM、8个确认场景业务与预览一致性、目录、索引、导航及语法检查通过。独立Chromium验证真实内层滚动与焦点、整卡拖动/落点/取消/边缘滚动/独立保存、删除取消/X/Esc/名称转义/保存生效/发布保护，课节交互及480–1440px表单回归通过；已检查拖动和删除截图。完整Umi/file入口未现场复验。测试`scripts/test-learning-editor-interactions-browser.cjs`，未部署。
- 修改前快照：`../本地回退备份/课节交互修复-20260909-PwIRyy`。仅回退本任务增量，不覆盖后续并行修改。

## 学习中心确认按钮文案与危险操作

- 追加：41主按钮由“删除”改为“删除草稿”，警示色与回调不变；业务/预览7场景DOM回归、弹窗目录与自动索引检查通过。本次仅文案变更，未新增浏览器视觉验收。
- 目标：38/42立即上架与再想想；40/43/44确认下架与再想想；41删除草稿与再想想，下架/删除复用共享危险按钮配色。39移除底部取消，仅保留放弃修改并离开/保存并离开，X/Esc不变。业务与预览同源修改，不动其他弹窗。
- 完成：`learning-app.js`由场景参数向共享工厂传按钮标签与danger语义；39直接使用原次按钮作为放弃操作，不保留隐藏取消、空位或额外克隆按钮。共享CSS、弹窗标题和正文未改，脚本缓存和自动索引同步；视觉参数见[[design-changes/learning-center.json]]。
- 验证：7场景业务/预览文案与按钮数量、危险语义及预览无数据写入通过；V1.1（含未保存关闭保留与放弃离开）、目录、索引、导航和语法回归通过。独立Chromium在375/1280核验所有底栏均2按钮、文字一致且不越界，下架/删除默认/hover/active颜色均命中共享危险token；关闭重开与Esc通过，已检查警示/未保存截图。完整业务整页未视觉复验；未提交、未发布。

## 课节管理表单重排

- 完成：`learning-app.js`提取`courseLessons/courseLessonEditor/lessonFilePicker`，课节标题与排序操作集中于顶部，名称与三类单选同组，必填内容和选填讲义分区，保存与上下架放在底部；空列表提供添加提示。文件按钮补键盘操作，标题实时同步，类型切换仍清除旧内容。所有原字段、本地示例、保存/排序及已发布限制保留，确认场景沿用并行任务现有实现。
- `learning-v11.css`仅新增课节编辑区样式，沿用基本信息的单选与输入风格，小宽度分组换行，面板宽度沿用上限。注册表同步资源缓存；视觉参数见`design-changes/learning-center.json`。修改前快照：`../本地回退备份/课节管理排版-20260909-grOMW2`。
- 验证：V1.1/课程管理DOM、确认场景与导航保护通过；扩展`test-learning-editor-browser.cjs`，独立Chromium验证三类切换/文件清空、讲义添加移除、单节保存、增加删除、排序独立保存、12课节及锁定限制，480–1440px区域无横向溢出。已检查课节面板截图；完整Umi/file入口未现场验收。未提交、未部署。

## 学习中心确认场景完整登记

- 目标：把通用示例改为真实课程/课节确认场景，业务与预览共用文案和确认工厂；补齐未保存离开真实提示，抽屉继续排除。不改视觉、不在预览执行数据操作，保留同时进行的课程编辑改动。
- 完成：`learning-app.js`集中维护七种确认文案，业务分支与无写入回调的预览共用`confirmScene`和全局确认工厂。38保留ID改为课程上架确认、39未保存离开沿用真实提示，40–44追加课程下架/删除草稿/课节上架/课节下架/最后一个上架课节下架确认；统一type为confirm。仅修改对应逻辑与登记，课程编辑基本信息保留；频道脚本缓存同步，索引按源重新生成。没有新增独立CSS或样式参数变更。
- 验证：新增`test-learning-confirm-scenes.cjs`逐一通过真实业务点击与预览的文案/按钮比对，预览全部底部按钮不改数据；V1.1、目录/44卡片、索引、导航保护、语法与diff检查通过。独立Chromium在640/1280宽度下核验7个真实入口的可见边界、X/Esc/确认关闭、重开、焦点恢复与数据隔离，检查未保存确认截图；不等同于完整学习中心页面视觉验收。未提交、未发布。

## 课程编辑基本信息重排

- 完成：新增/编辑基本信息重排为课程内容（名称/描述与封面预览）和学习设置（共享折叠多选、必修单选、共享开关皮肤）。群组“所有人”互斥、连续选择保持展开，重绘/离开销毁实例；表单输入计数内置，必填标记前置，封面支持键盘上传，示例入口保留但降为文本动作。新增时使用“保存草稿”，编辑使用“保存修改”，普通说明改为非警告样式。
- 所有字段、Mock、保存/未保存确认与课节业务保留；课节只同步面板最大宽度对齐。缓存同步至`editor-basics-1`。CSS参数记录在学习中心视觉台账，修改前副本位于项目外 `../本地回退备份/课程编辑基本信息-20260909-GCeCiO/`；回退需保留后续修改，不整份覆盖共享注册表。
- 验证：课程管理/V1.1 DOM、弹窗目录/预览、导航保护通过；`test-learning-editor-browser.cjs`在独立真实控制器检查多选互斥/展开、单选/开关、封面上传、计数、保存/重新编辑、未保存取消及480–1440px基本信息布局。截图已检查，完整原入口未本轮验收。未部署。

## 课程管理工具栏与筛选区间距收紧

- 完成：仅减小课程管理内容区顶部内边距，保留工具栏、筛选组件内部间距、白底与上方分割线；不影响其他管理页。CSS缓存同步，参数合并至学习中心视觉台账。独立Chromium480/1327/1920px核验筛选区距工具栏底部8px、其他admin仍24px；完整入口未截图验收，未部署。

## 课程管理内容区白底与顶部分割线

- 用户纠正分割线位置：应在返回/操作日志/新增课程工具栏上方，不在筛选内容区上方。本轮移至`.lc-manage-header`，白底保持。

- 完成：课程管理 `.lc-admin.lc-manage` 保持白底，分割线改在 `.lc-manage-header` 工具栏上方；内容区原误加边线移除。不改变其他页面背景和功能。CSS缓存同步，参数合并至 `design-changes/learning-center.json`。独立Chromium核验480/1327/1920px工具栏上方边线及无重复边线；完整入口未截图验收，未部署。

## 课程操作日志字段与记录规则对齐

- 完成：`learning-data.js` 新日志保存结构化字段差异，新增/编辑/删除/课程上下架/课节上下架/课节顺序均记录实际前后值；一次操作多字段作为同一事件保存、表格逐字段一行展示。草稿编辑、重复上下架和无变化保存不新增记录；直接下架课程仅一条课程日志，最后一课节下架联动课程时注明自动原因。文件仅展示文件名，封面上传补存名称。
- `learning-app.js` 信息弹窗使用共享表格八列，默认10条、倒序、搜索扩展至对象/字段/前后值/账号，长文本可展开。旧日志只读适配，能从快照还原则拆分，缺失前值注明“历史未记录”，不覆盖原始日志。学情日志和权限不变；无课程手工展示排序入口，不因排序/筛选列表伪造操作日志。
- 验证：新增日志领域测试，V1.1、课程管理、Mock增量迁移、目录/预览、语法和导航检查通过；独立Chromium验证八列表头、真实保存产生的日志、转义长文/展开、搜索/分页、480–1440px边界和关闭后底页状态。完整原入口尚未本轮验收。更新频道缓存和视觉列宽台账，数据仍为本地Mock，未部署。

## 课程管理操作日志信息弹窗

- 完成：课程管理日志改为信息展示类原生弹窗，复用共享表格、搜索、关闭图标、定位与遮罩；保留现有六列/详情/分页，关闭后不重建课程列表。学情日志入口不改。共享表格的每页条数面板兼容原生弹窗顶层；登记真实预览入口并更新缓存。
- 验证：课程管理DOM、V1.1、弹窗目录生成检查及预览、共享表格浏览器和导航保护通过；独立Chromium验证日志搜索/分页/详情、480–1440px边界、Escape/关闭/重开/销毁、底页状态保留和无数据写入。完整原入口视觉待验收。目录57项，39可预览。未部署。
- 后续用户已授权补齐日志字段/记录规则，见上方“课程操作日志字段与记录规则对齐”。此条保留弹窗接入时的六列阶段记录，当前运行展示八列。

## 课程管理四项筛选与群组多选

- 已在 `learning-app.js` 配置搜索、学习群组多选、课程状态、只看必修课；移除创建日期和对应筛选逻辑，无高级字段自动隐藏更多按钮。群组按OR匹配原课程配置，“全部用户”选项仍对应all配置。共享日期能力和表格创建时间列保留。
- 课程管理测试替换为四字段/多选OR/重复勾选不关闭/分页和重置检查，课程管理DOM、共享筛选DOM及导航保护通过；同步脚本缓存和规范。不改Mock数据，未部署。
- 独立Chromium实测发现容器查询形成层叠上下文，导致展开选项被表头挡住；共享筛选栏仅在多选展开时提升层级，不改视觉参数。修复后 `test-learning-group-browser.cjs` 验证三个群组连续点击、面板保持展开、Escape及重置通过。完整原入口未做本轮视觉验收。

## 共享筛选重置按钮有效状态

- 目标：只要当前筛选值偏离重置后的默认条件，“重置”按钮就使用 `#2F3640` 底色和白色文字；恢复默认后回到普通按钮。覆盖搜索、单选、多选、开关、日期和数字范围，不改筛选提交模式、默认值或业务查询。
- 完成：共享筛选器按可见字段的归一化有效值与 `defaultValue`/空值基准比较，搜索忽略首尾空格；初始默认条件不误点亮。有可重置变化时按钮使用 `#2F3640` 边框/底色和白字，重置后即时复原；缓存标识及规范已同步。
- 验证：共享筛选 DOM、课程管理 DOM和导航保护通过；全局组件目录的真实筛选器已在 Chromium 核对深色为 `rgb(47,54,64)`、文字为白色且重置可恢复。完整课程管理页未重复视觉验收，未发布；参数并入 [[design-changes/shared-filter-bar.json]]。

## 全局筛选字段宽度与容器适配

- 已在 `shared/styles/global-filter-bar.css` 按控件类型限制最大宽度，开关不拉伸，使用容器查询适配窄区域；日期/数字区间保持两端等宽。课程管理与组件目录缓存同步，不改筛选逻辑、显隐、日期面板或分页。
- 参数合并至 `design-changes/shared-filter-bar.json`，规范见 `docs/filter-bar-spec.md`；`test-filter-bar-browser.cjs` 增加320–2200px容器及单独日期区间检查。独立Chromium、筛选DOM、课程管理DOM和导航保护全部通过；完整课程管理入口视觉未验收，未部署。

## 共享多选已选标签文字色收口

- 完成：共享多选器已选标签文字从旧 Ant 黑色 `rgba(0,0,0,.88)` 改为项目正文色 `#2F3640`；只改文字色，不改标签背景、边框、尺寸、下拉选中色或交互。课程管理与全局组件目录同源生效，缓存标识已同步。源码、筛选 DOM、导航保护及组件目录实际计算色核验通过；完整课程管理页未重复视觉验收，未发布。参数记录于 [[design-changes/shared-filter-bar.json]]。

## 全页面本地字体两档统一

- 目标：用户已验收操作确认试点，扩展到全部正式入口与同源组件；全局仅登记本地Regular/Bold，合并确认/标题/分组的字体别名，收口工作台旧Medium引用与有效中间字重。保留现有字号、颜色、布局、滚动条；原站web和第三方备份只读。不包含发布。
- 完成：`global-font.css`成为唯一业务字体面登记源；`global-modal.css`移除试点/标题/分组别名，工作台移除局部字体面，配置表头中间字重收口，海报Canvas映射到两档；16个根入口、3个组件文档、源登记与缓存版本同步，自动索引重新生成。字体资产未修改，保留用户已有删除改动。参数见[[design-changes/global-font.json]]及原共享标题、确认、内部组件台账。
- 验证：`test-project-font-browser.cjs`使用真实共享CSS/确认工厂，核验全部入口引用以及640/1280两宽度、两种CSS顺序下的计算字体、实际本地Regular/Bold渲染和确认关闭重开；已检查独立夹具截图。`test-modal-form-primitives.cjs`的16个表单真实源/旧适配契约、目录索引、预览、导航保护、资源版本和JSON检查通过。整页的临界换行/完整业务视觉未逐页验收；本地未提交、未发布。

## 操作确认字体局部隔离

> 本阶段已由用户验收；临时隔离名称随后在“全页面本地字体两档统一”中合并为全局家族，下面保留试点过程。

- 目标：按用户确认，仅操作确认使用隔离字体家族、本地Regular/Bold两档真实字体与通用sans-serif兜底；主标题700，正文/辅助/按钮400，不改字号颜色间距，不改全局字体定义及表单/页面。
- 完成：`global-modal.css`登记组件隔离家族并在`.gaip-modal`内应用；主标题沿用共享标题字重，图标字体排除。不修改`global-font.css`或字体资产，保留工作区其他字体删除改动；来源/预览CSS缓存及源登记版本同步，自动索引按现有登记生成。参数分别合并于[[design-changes/shared-modal-title.json]]与[[design-changes/shared-modal-confirm.json]]。
- 验证：`test-confirm-font-browser.cjs`通过真实共享工厂的两种CSS加载顺序、实际字体渲染（CDP读取本地Bold/Regular）、400/700、字体合成关闭、外部页面/表单样式不变、确认元素尺寸/颜色/行高不变及关闭重开；已检查截图。目录/预览与导航保护通过。配置中心旧测试在未修改的正文CSS上仍要求`scrollbar-gutter:stable`而失败，未放宽断言；业务来源整页尚未视觉复验。本地未发布。

## 课程管理普通字段颜色收口

- 已移除 `learning-v11.css` 中时间的局部灰色，课程管理表格更新人继承正文色；状态标签、禁用操作、其他页面保持不变。不改布局及功能。缓存标识在 `shared/config/channels.js` 更新，参数合并到 `design-changes/learning-center.json`。
- 课程管理DOM回归及导航保护通过；独立Chromium核对文字继承、禁用颜色与其他页面隔离，完整入口视觉未验收。本地未发布。

## 组件统一项目全局字体

- 目标：组件目录及海报分享移除独立字体，接入项目`global-font.css`；保留共享标题字重和现有交互，不修改项目字体定义或原站`web/`。海报导出文字也需读取同一字体变量。
- 完成：目录品牌/代码说明取消字体覆盖；海报分享移除独立字体面并引用同源全局字体，Canvas导出读取计算字体且等待加载，原绘制字号/字重不变。共享标题字体规则未改，图片内已烘焙的文字不处理；同步目录CSS缓存。
- 验证：目录8标签DOM、弹窗索引/预览、导航保护通过；独立源码检查确认5个代表入口同源字体路径、本地字体资产存在、内联脚本语法正确、Canvas随计算字体变化。真实浏览器排版和PNG观感待验收；未提交、未发布。参数记录在[[design-changes/component-catalog.json]]。

## 组件预览展示区与说明分离

- 目标：移除组件目录重复的三个概览指标；白色卡片只承载组件展示，标题、说明、演示配置、调用信息和来源链接置于卡片外。保留已有组件登记、导航选择、真实工厂与业务交互，不改业务页面。
- 完成：仅修改`全局组件/index.html`、`components-preview.js/css`及独立弹窗预览的外部展示壳；同步首页预览资源缓存。8个标签页、38个真实弹窗登记及懒加载保留；筛选、日期、表格、多选继续调用原工厂，海报分享真实组件及业务源码不改。
- 验证：新增`test-component-preview-layout.cjs`覆盖8标签切换、查询参数恢复/键盘、白卡边界与真实组件挂载、表格场景、筛选配置、表单关闭/重新展示及弹窗按钮委托；源码语法、弹窗索引/预览回归和导航保护通过。真实浏览器视觉与点击命中尚未验收，未提交、未发布。
- 参数：[[design-changes/component-catalog.json]]，首次本地值与未知交付基准分开记录；说明位置调整仅记本节，不回填为视觉参数。

## 清除图标实际尺寸统一

- 完成：筛选搜索的14px、多选的12px和独立多选文字×改为与弹窗同源的16px SVG，点击区域统一24px；独立多选显式抑制文字×叠加，保持原清除、显隐行为。不修改其他控件、分页或弹窗源码。
- 修改：`global-filter-bar.css`、`global-multi-select.css`，频道登记与组件目录CSS缓存同步；参数合并到 `design-changes/shared-filter-bar.json`。
- 验证：`test-clear-icon-browser.cjs`在真实共享CSS及学习中心覆盖样式下测得四类清除控件均16px图形/24px热区，无文字叠加，三类清除操作通过；共享筛选DOM与导航检查通过。独立机制检查不等于来源整页验收，未部署。快照位于项目集 `本地回退备份/清除图标尺寸-20260909-Iy8smP/`。

## 全局可配置筛选栏

- 目标：共享容器及单选/折叠多选/开关/搜索/重置，补日期与数字范围、更多筛选、显隐规则。仅课程管理首接，业务字段及筛选含义不减；其他页面不自动替换。
- 完成：全局脚本/CSS按配置渲染7种控件，支持即时或查询提交模式、清空/重置/更多、显隐/禁用及范围校验；隐藏清值、更多折叠保留且提示数量。复用原折叠多选与共享图标/颜色/尺寸变量，不修改原多选组件行为。
- 首接：课程管理四项保留，必修改二态开关，群组仍单选；只刷新结果区以保留搜索焦点，销毁时清理延迟/监听。全局组件目录新增同源演示及显隐开关，原条目保留；其他业务页未批量替换。
- 资源：`shared/scripts/global-filter-bar.js`、`shared/styles/global-filter-bar.css`，学习频道统一登记；目录登记及真实源预览更新。规范见[[docs/filter-bar-spec]]，视觉当前台账 `design-changes/shared-filter-bar.json`；旧7条课程筛选记录移至archive作组件迁移追溯，不冒充恢复线上。
- 验证：共享组件、课程列表和原V1.1 DOM回归通过，覆盖中文输入/延时取消、显隐重置、多选键盘、范围错误、提交模式及目录同源；语法/导航保护/弹窗登记检查通过。原生单选/日期面板及实际多选裁切/响应式视觉待本地验收，未通过HTTP绕过file限制。
- 回退点：项目集 `本地回退备份/全局筛选栏-20260909-tVXgLR/`。仅本地，未发布，不重置Mock。显隐当前由页面配置，真实用户自选筛选项未新增。

## 课程管理列表排列试版（可单独回退）

- 顶部续调完成：移除独立标题，日志/新增移至返回同行右侧，页面身份由共享面包屑显示；新增/返回/日志功能不变。顶部按钮使用共享默认高度/品牌色变量，未宣称其他表格控件已完成全站样式对齐。
- 面包屑：`learning-app.js` 同步当前管理身份，`learning-center.js` 复用该同步而不覆盖；返回学习中心、进入其他视图及卸载时清除。原共享面包屑、Hash 与其他频道源码不改。
- 本轮回退点：`/Users/hhp/Documents/GAIP项目集/本地回退备份/课程管理顶部合并-20260909-TkLXi7`，保留此前排列试版；原列表/领域测试、真实共享面包屑与频道生命周期的 DOM 回归及导航保护通过，真实视觉待本地刷新验收。
- 目标：仅调整课程管理列表的筛选分组、字段顺序及操作列布局；保留所有字段、按钮、筛选与业务规则，学情管理、新增/编辑页及权限不改。
- 修改前快照：项目集外置备份目录 `本地回退备份/课程管理排列试版-20260909-9mzqen/before/`，包含本轮源码与记录起点；回退只撤销本轮增量，不重置仓库或 Mock 数据。
- 完成：名称筛选前置并补固定标签，表格按名称/状态/群组/必修/课节数/时间/操作排列；课程名称与操作列固定，三项行操作等宽且不折叠，禁用删除有原因；新增仍为主按钮，日志仍可直接进入。筛选保持原即时生效，不新增提交步骤。
- 范围：只改 `features/learning-center/learning-app.js` 的 `renderManage` 与 `learning-v11.css` 的课程列表专属规则；新增 `scripts/test-learning-course-manage.cjs`。课程业务规则、共享表格、学情和编辑页原函数不变，用户 Mock 数据未重置。
- 验证：列表字段/操作保留、筛选组合/空态/分页、确认取消、编辑与学情隔离的 DOM/CSS 回归通过；原学习 V1.1 回归、JS语法、导航保护通过。file 访问限制仍在，真实页面视觉及横向固定列效果待用户本地刷新验收。
- 交付：试版待用户确认，未发布；视觉参数并入 `design-changes/learning-center.json` 的 `learning.manage-trial.*`。回退说明和源码逆向补丁保存在上述外置备份目录，不覆盖此前 V1.1 改动或其他任务。

## 学习中心测试环境基线与 V1.1 本地同步

- 目标：保留已确认本地视觉，补齐课程管理、学情管理与学习流程，落实 2026-09-08 V1.1；仅使用本地 Mock，不提交线上数据，不加载真实音视频。
- 已确认口径：课程完成度只计算当前上架课节；下架课节保留历史，重新上架再参与计算。
- 完成：学习中心拆分领域状态、页面交互与原频道生命周期；补课程管理、编辑/独立课节保存/排序、草稿/课程/课节上下架、学情双维度/抽屉/导出/日志、身份范围、视频/音频模拟、PDF连续阅读与水印；原六套素材、卡片/返回/左右布局保留。新增两种共享确认源登记，原编号不变，学情抽屉不进入普通弹窗预览。
- 文件：`features/learning-center/{learning-data,learning-app,learning-center}.js`、`learning-v11.css`，已有本地 PDF 渲染的四张阅读页；`channels.js`登记及16个实际HTML消费者的学习引用/共享XLSX缓存同步。共享XLSX增加可选表名和机密页眉页脚，不改变其他调用方默认参数。未触碰线上数据，未部署/提交。
- 验证：学习 V1.1 新增领域/DOM/XLSX/频道生命周期回归、原导航保护与主导航测试、弹窗自动登记/预览、原全局操作日志回归通过，JS语法与差异检查通过；PDF四页已查看。浏览器工具明确拒绝 file 页面，未绕过，实际布局/点击/全屏/历史栈现场验收仍待完成。
- 交接：[[docs/learning-center-v11-local|实现、Mock边界、演示口径及验收路径]]；视觉参数唯一台账为 `design-changes/learning-center.json`。产品文档中尚未定稿的口径单列，不冒充线上已实现事实。

## 真实弹窗预览下的表单框架子级

- 内容范围目标：用淡底色标出示例的内容放置范围，替代只有文字的占位；仅修改目录示例，不改变真实弹窗背景或内容内边距。
- 内容范围完成：单个框架的内容槽显示淡灰底色及“内容区域（示意）”，保留有副标题状态和原内边距；背景记录合并到目录视觉台账。独立浏览器夹具验证底色、单一区域、副标题保留及宽窄屏边界，目录/预览/导航检查通过；不代表业务页面重新验收。
- 本轮目标：同一个框架示例展示可选副标题，使用共享副标题语义与样式，不新增第二个示例。
- 本轮完成：标题下加入可选副标题示例，经`adoptForm`接入现有`gaip-modal__subtitle`；只修改示例内容和缓存，不改共享视觉参数。DOM确认唯一框架、标题后位置和共享类接入，语法/目录/导航检查通过；本轮未单独浏览器截图验收。
- 目标：在真实弹窗预览下仅增加一个“表单弹窗框架”子级，展示单个共享表单外壳；不恢复25项部件清单，不改业务弹窗样式。
- 完成：登记一个子级，沿用切换展示；仅放标题、空内容区域、关闭与取消/保存按钮的框架，通过`adoptForm`引用现有共享样式及关闭图标。示例只关闭/重新展示，不提交数据；目录布局参数见[[design-changes/component-catalog.json]]。
- 验证：独立Chromium目录夹具确认仅一个子级和框架、共享语义/关闭图标/按钮接入、关闭与重新展示、三档宽度无溢出，已检查截图；语法、目录登记、预览与导航保护通过。未改业务来源，本地未发布。

## 原四项组件目录切换展示

- 目标：仅将原四项目录的平铺锚点改为导航切换，一次显示对应内容；不增加分类、部件或说明，不改业务共享组件。
- 完成：原导航改为四个切换按钮，右侧仅对应面板显示；保留原正文、示例与技术说明，使用查询参数记忆选择，支持返回和键盘方向键/Home/End，失效旧锚点默认首项。离开多选示例时关闭下拉；窄屏延续原横向导航并修正容器撑开。
- 验证：独立Chromium目录夹具通过四项切换、单面板可见、旧链接回退、浏览器返回、键盘和1326/768/390宽度检查，已看截图；弹窗目录/登记与主导航检查通过。未改业务共享组件，目录布局参数见[[design-changes/component-catalog.json]]，本地未发布。

## 全局组件目录恢复原始内容

- 目标：用户明确要求恢复加入任何本轮内容之前的目录；撤销25项部件、阅读说明、参数面板与配套展示脚本，保留原四项全局组件入口。不回退业务弹窗、共享组件或其他任务修改。
- 完成：目录CSS、JS与登记恢复至新增展示内容之前；HTML恢复原始内容，仅保留当前共享资源版本并刷新目录缓存。移除试版示例脚本和专用测试，视觉台账标记撤回；以下两节试版记录均已失效。
- 验证：三份目录源文件与原版本一致；JSDOM验证四项入口正常渲染、无新增部件或说明区；语法、弹窗目录登记和导航保护检查通过。未改业务源码，未发布；本次未做浏览器视觉复验。

## 全局组件目录单项浏览

- 撤回目标：按用户要求撤销本轮单项浏览排版，恢复此前分组铺开与锚点导航；不回退共享组件和业务弹窗，也不影响其他工作区修改。
- 当前结果：单项浏览试版已撤回，恢复25项铺开、状态对照、使用编号与原目录跳转，移除本轮增加的选择栏和状态折叠；以下单项版完成记录仅供追溯，已不生效。独立Chromium148恢复版通过共享控件交互、确认开关、锚点跳转和三档响应式测试，已检查截图；目录登记、导航及语法检查通过。共享控件与业务来源未改，本地未发布。
- 目标：按用户反馈将长清单改为左侧选组件、右侧只显示当前项；正常示例优先，状态对照和技术信息折叠。保持25项部件和36项业务弹窗入口，不改共享控件、业务页面或新增HTML。
- 完成：目录分类支持折叠，选中部件在右侧独立展示；默认只展开一个正常示例，其他状态、使用编号、技术接入与共享参数按需展开。窄屏改用组件选择栏；支持链接定位与浏览器返回。只改目录展示结构和布局，所有原登记入口保留。
- 验证：独立Chromium148目录夹具通过25项逐项切换、状态折叠、历史返回、移动选择及1326/768/390宽度检查；共享表单交互与确认弹窗开关回归通过，已检查截图。自动弹窗目录、预览登记与主导航检查通过；业务来源页面未重验。视觉参数合并于[[design-changes/component-catalog.json]]，本地未发布。

## 全局组件首页弹窗部件设计对照

- 目标：不新增HTML页面，在现有全局组件首页铺开弹窗内部部件；用于设计师向前端交付，优先呈现名称、状态、使用位置，技术接入折叠展示。引用共享样式与适配器，不改业务弹窗或复制业务页面，保留完整弹窗预览入口。
- 范围：组件首页、目录登记与展示脚本/布局；示例仅提供本地数据和交互，明确区分共享工厂、共享控件适配和组合样式。页面、抽屉和Agent主面板不纳入。
- 完成：原首页增加四组25项部件、状态对照、使用编号、侧栏定位及实时读取的共享视觉参数；前端接入说明默认折叠，保留原四项全局入口与36个业务弹窗目录。新增`modal-component-specimens.js`只提供示例数据/组合结构，调用原共享工厂、语义接入器和控件事件；未新增HTML、未改共享/业务样式。目录缓存单独更新，布局视觉记录见[[design-changes/component-catalog.json]]。
- 验证：独立Chromium148展示夹具通过25项渲染、初次加载无焦点跳转、共享hover/focus/active、清除、单多选下拉、日期时间、计数手柄、单选、节点搜索、分页、错误修正、真实确认开关/Esc/焦点返回及1326/768/390宽度无横向溢出；检查截图。实际业务来源页面未重验。弹窗自动目录与36项预览登记、导航保护通过；本地未发布。

## 弹窗必填星号统一前置

- 目标：弹窗必填标记统一到标题/字段文字前，复用共享错误色与标记间距；包括Ant、07报名、20终态、16节点、33/34公告分组。只调整呈现，不移动React节点或改变必填条件、分组校验及字段事件。
- 完成：只在`global-modal.css`收口前置伪元素与字面星号顺序，新增线索的后置伪元素关闭；真实星号节点通过CSS排序前置，保留源DOM顺序与标签关联，不改可选/必填条件。来源和预览CSS及登记缓存同步；视觉参数见[[design-changes/shared-modal-fields.json]]。
- 验证：独立Chromium148检查前置、无重复、共享色与间距、长标签换行、两种CSS顺序、页面字段排除、可选标签不新增星号、原标签聚焦及required属性保留；共享表单DOM、公告业务与20真实React终态校验、弹窗目录/导航、视觉JSON及diff检查通过。来源整页视觉仍待验收，未发布。

## 弹窗滚动条最终选择原生加淡色

- 目标：按最新确认采用默认原生宽度，仅覆盖淡色；撤销上一轮常驻滑块的宽度、圆角及悬停绘制。显隐交给浏览器/系统，不再将常态常驻作为验收标准，不改系统设置或正文布局。
- 完成：删除`global-modal.css`的WebKit绘制分支，保留共享宿主的颜色变量；来源与预览同步缓存。独立测试改为对比无样式原生基线与颜色覆盖后的宽度，保留溢出、滚动、关闭重开检查；参数仍合并于[[design-changes/modal-form-scroll.json]]，不保留相互冲突的两套验收标准。
- 验证：独立Chromium148的原生基线宽度对照、颜色继承、三个共享宿主、两种CSS加载顺序、长短内容/文本域、滚动与关闭重开通过；共享表单DOM、目录/导航、JSON及diff检查通过。来源页面视觉未验收，未发布。

## 弹窗常态滚动条恢复

- 后续：用户明确选择“默认原生宽度＋仅淡色”，本节实现已由[[#弹窗滚动条最终选择原生加淡色]]替代，不再是当前规范。

- 目标：按最新确认恢复普通弹窗内容溢出时的可见滑块；只在共享弹窗范围恢复旧WebKit绘制参数，并让两套绘制规则互斥。不修改系统设置、来源DOM、overflow或内容padding；独立浏览器机制测试与来源页面视觉验收分别记录。
- 完成：`global-modal.css`在支持WebKit部件时将弹窗标准颜色设为auto，采用共享原生部件绘制；不恢复全页面WebKit规则或局部重复规则。默认色不变，恢复原悬停绘制；滚动条自身占据布局宽度，未额外补正文留白。来源和预览CSS及登记缓存同步，参数合并于[[design-changes/modal-form-scroll.json]]。
- 验证：独立Chromium 148对照复现标准规则静止隐藏；恢复规则的静止截图、悬停加深、长短内容/文本域、两种CSS顺序、三个共享宿主、滚轮和关闭重开通过。Chromium119也核对恢复滑块；没有访问受限来源页面，不能把独立测试当成项目全弹窗视觉验收，也未确定最初回归发生于哪次改动。新增可复跑机制测试，共享表单DOM、弹窗目录/导航、脚本语法、视觉JSON及diff检查通过；测试浏览器已关闭，未发布。

## 滚动条统一标准原生规则

- 后续：普通弹窗已按最新确认由[[#弹窗常态滚动条恢复]]覆盖；本节保留过程记录，不再作为弹窗最终显示规范。

- 目标：按用户选择撤掉共享滚动条的WebKit像素宽度与绘制规则，统一由标准原生属性着色；清理弹窗自身的重复规则，保留当前默认淡色和滚动容器。宽度、显隐与滑块悬停由浏览器/系统管理，不修改系统设置。
- 完成：`global-font.css`移除共享WebKit绘制路径，保留标准原生属性；`global-modal.css`与成员弹窗正文去除独立列表/正文绘制规则。页面原有默认色及侧栏专属规则不改；弹窗淡色继续在共享宿主继承，旧源/非弹窗局部样式未逐个重写。来源与预览资源缓存同步，参数见[[design-changes/modal-form-scroll.json]]。
- 验证：共享原生规则契约、标题/表单/滚动容器DOM回归及导航检查通过；目录测试已同步新的资源版本，浏览器视觉仍待验收。不是“常态隐藏已修复”，而是按用户新选择交由浏览器/系统显示，未发布。

## 10与20字段报错接入补齐

- 目标：新增成员的必填/重复账号由浏览器气泡改为共享内联反馈；线索终态必填错误分别归属原因、说明及截图字段，复用预留行、修正清除及首错定位。保留真实源与提交逻辑，不改操作确认或只读web基线。
- 完成：共享组件新增`createFieldFeedback`负责原字段的提示节点、显隐及ARIA关联；10真实业务校验直接调用，20通过控件接入器在原提交前检查必填，合格后委托原React上传/提交。上传预览变化自动更新凭证错误，关闭/卸载清理；不改源上传格式/大小检查及服务端错误处理。共享脚本、样式、频道与预览缓存同步，视觉接入差异见[[design-changes/shared-modal-fields.json]]。
- 验证：配置中心真实流程的必填、空格、重复账号、修正和首错焦点通过；新增20实际React源DOM测试覆盖转化/关闭/原因选项为空、上传满额/删除/再上传、正常业务提交和卸载。共享表单/控件/观察器、目录及导航通过。测试隔离后端，不调用真实业务；未进行浏览器像素验收，未发布。

## 表单反馈区预留一行

- 目标：表单操作的内联校验预留一行，单行错误不再改变字段高度，多行自然扩展；合并原字段间隔，保留错误节点、校验和可访问性。操作确认不改，预览与业务使用共享来源。
- 完成：共享`adoptFeedback`在原Ant控制区及本地真实错误父节点接入占位，不新增或移动React节点；空态由无文本伪元素占位，原错误出现后替换该空间。保留hidden、aria及业务校验，永久帮助文字另计，多行自然扩展；取消Ant负间距抵消并合并字段原间隔。参数见[[design-changes/shared-modal-fields.json]]与[[design-changes/modal-interiors.json]]，来源与预览缓存同步。
- 验证：07六字段与单选组、15/16错误区、公告分组、24真实React/Ant输入校验与修正、节点身份、控件/手柄及观察器回归通过；目录与导航检查通过。仅源码/DOM验证，浏览器真实高度与窄屏视觉待验收；没有内联节点的原生气泡校验保持原行为，未发布。

## 07报名内联错误与提示间距

- 目标：07用共享字段下方错误说明替代浏览器气泡，保留原字段约束与模拟提交；只在提交失败后显示并随修正清除。核对提示上距，消除表单错误外壳与内层重复间距，不改操作确认。
- 完成：07真实工厂使用novalidate并读取现有Validity约束，每个字段创建一个共享help/error节点，单选组共用一条；aria-describedby/aria-invalid与错误显隐同步。提交失败调用共享首错定位，修正只更新对应字段；正常模拟提交不变。共享CSS去除Ant内层错误重复上距，来源与预览缓存同步；参数见[[design-changes/shared-modal-fields.json]]。
- 验证：07真实源首次无错、空提交、逐字段修正、数值上下限、单选组、首错焦点、有效提交及重新创建无旧错误通过；16表单适配、控件/手柄/错误定位、目录与导航回归通过。源码/DOM验证，不代表浏览器实际像素间距验收；未发布。

## 表单底部边界检查与首错定位

- 目标：从真实来源检查正文/底栏边界，修正原生字段首错焦点争抢及公告错误过早清空；不改变操作确认、不扩大重写滚动布局，08保存问题继续跳过。
- 完成：共享控件增加`revealInvalidField`与原生invalid事件首错合并；只滚动所属表单祖先，保留原校验与节点，关闭/销毁取消延迟聚焦。成员新增只报告第一个空字段，空格值也提供错误；公告首次提交后按当前值更新错误，修正一个分组不清空其他分组错误。业务与预览JS缓存同步。
- 边界检查：10成员编辑、15批量导入、23/24客户编辑及33/34公告的正文可收缩滚动与外置底栏契约通过，保留现有高度与布局，没有新增视觉参数。
- 验证：16表单源/适配、共享控件（含模拟滚动几何）、配置中心、公告、目录、导航、JS语法与diff检查通过。浏览器实际裁切/长短视口仍未验收；原站Ant校验逻辑保留，不声称所有自定义异步校验均已接入共享首错API，未发布。

## 表单内容文字层级与间距收口

- 目标：只规范表单操作内容区的分组标题、辅助文字及字段/分组间距；达标项保持不变，操作确认、图标、按钮、滚动条与定位不调整。
- 完成：共享CSS添加表单专属层级规则，复用本地Medium字体；补齐10说明、15目标路径及上传标题留白，07/10双列与15/33/34分组间距按规则收口。已有正文/标签/输入、占位/禁用/错误与计数规则保留；窄屏字段纵向紧凑间距保留。参数合并于[[design-changes/modal-interiors.json]]，来源与预览资源同步。
- 验证：16表单真实源/旧源适配、嵌套确认排除、共享标题与按钮、字段事件/错误/禁用/手柄及观察器回归通过；弹窗目录与导航检查通过。只验证源码/DOM，浏览器视觉及实际行距观感待确认，未发布。

## 弹窗正文滚动条默认色调淡

- 续调目标：默认色再调淡，并扩展到普通弹窗全部内部滚动层及所属Portal；继续保留悬停和几何，不包含页面、抽屉与Agent主面板。
- 目标：仅调淡共享弹窗正文的原生滑块默认色，保留悬停色、滚动方式、宽度及正文间距；内部输入、列表与下拉滚动条不变。
- 最新完成：`shared/styles/global-modal.css`改为在普通弹窗宿主、共享组件及所属Portal统一覆盖默认滑块变量，替换上一轮仅正文的限定规则。所有内部滚动层继承同一颜色；来源和预览缓存同步，未改全局悬停token。参数合并到[[design-changes/modal-form-scroll.json]]。
- 验证：正文/文本域/列表/表格/所属Portal覆盖、页面/抽屉/Agent排除、原悬停规则保留的CSS契约检查及弹窗目录、导航检查通过；未做浏览器视觉验收，未发布。

## 07无计数备注接入共享手柄

- 目标：修正此前仅带计数输入框接入手柄的遗漏；07活动报名备注复用同一手柄，不新增计数，不改变原字段限制、校验与提交。共享规则支持明确声明的无计数多行外壳。
- 完成：`activity-sync.js`真实工厂声明`resizable-textarea`外壳；共享`global-modal.js`/`modal-controls.js`与`global-modal.css`复用已有手柄控制器和视觉，原生手柄被抑制。共享资源、活动脚本与来源登记缓存同步为`20260908-activity-resize-1`；参数见[[design-changes/modal-interiors.json]]。
- 验证：07真实工厂的唯一手柄、无计数、拖动计算、FormData备注、原必填/长度限制及禁用通过；共享表单/控件、目录、导航与diff检查通过。未做浏览器视觉验收，未发布。

## 缩放手柄去除聚焦方框

- 目标：按用户截图与确认，去除手柄四周聚焦描边，保持透明底色、斜线悬停/聚焦变色；不改输入外框状态、计数行、手柄位置或拖动逻辑。
- 完成：只修改`shared/styles/global-modal.css`手柄描边规则，实际来源与预览的CSS引用及目录缓存同步为`20260908-resize-no-ring-1`，JS行为未改。参数见[[design-changes/modal-interiors.json]]。
- 验证：控件DOM/CSS回归、弹窗目录、导航与diff检查通过；增加聚焦后无描边/底色/阴影且斜线状态色保留的断言。未做真实浏览器视觉验收，未发布。

## 多行输入组件来源核验与接入规则

- 目标：按用户确认检查23/24真实来源、多行输入控件的同类漏接和可复用规则；08跳过，不通过弹窗预览页替代来源验收。保留已确定外框手柄与独立计数行，不再另换布局方案。
- 验收分层：检查来源模块和实际共享资源；可执行DOM测试与真实浏览器视觉分开记录。当前没有浏览器控制/截图工具，不能声明第1步视觉验收完成。
- 实现：补齐fieldset禁用、动态字段排除后的手柄清理；新来源可用`counted-textarea`/`count`语义标记接入，不再依赖Ant私有类名。不改变本轮视觉参数、不修改只读web基线；共享脚本及引用缓存更新为`20260908-textarea-contract-1`。
- 规则与盘点：[[docs/modal-textarea-acceptance]]。新增`test-customer-modal-source.cjs`运行原客户React/Ant组件，接入标准`test:modal-forms`入口；模型与后端在内存隔离，不复制预览弹窗。
- 已通过：来源23/24实际组件的空/短/上限正文与原计数更新、拖动计算、原关闭回调、保存中禁用及卸载重开；共享表单、公告、目录与导航检查通过。源码盘点中的18/33/34自动增高、07/22无计数变体保留；没有把不同业务形态统一成强制拖拽。视觉仍待来源浏览器核验，未发布。

## 计数文本域外框缩放手柄

- 目标：计数继续独立占行，手柄统一在整体外框右下角；共用手柄调节原textarea高度，不移动原节点，不触发数据变更。自动增高/禁止缩放字段不新增手柄；保留禁用只读、关闭清理及缩放后的原值。
- 实现：共享控件接入器添加外框手柄，CSS抑制内部原生手柄；指针拖动考虑预览缩放比例，支持键盘调整，关闭/卸载/失焦/取消拖动时清理监听。禁用只读不允许缩放。参数见[[design-changes/modal-interiors.json]]；引用缓存统一更新为`20260908-count-resize-1`，未发布。
- 验证边界：DOM与CSS规则回归覆盖拖动、原节点与数据不变、计数独立行、关闭重开和清理；不等于真实浏览器视觉与命中测试。08保存逻辑继续跳过。
- 已通过：`test:modal-forms`、`check:modals`、`check:navigation`、`test:announcements`及`git diff --check`。预览登记数量未变，真实浏览器视觉仍待验证。

## 计数与滚动正文分离

- 目标：修复带计数文本域滚动时正文穿过固定计数的问题。计数使用原节点、在外壳内独立占行，移出textarea滚动视口；范围为已接入弹窗的计数控件，不改数据、maxlength、输入事件或08保存逻辑。
- 完成：共享CSS把计数由绝对定位改为正常流行，文本域恢复普通内边距，避免滚动中段正文穿过计数。原生手柄因此偏高的问题已由上方“计数文本域外框缩放手柄”续修，不再回退计数布局。24追加修正正文/表单/底栏横向留白的抵消关系，按钮与字段只保留一层公共边距。参数见[[design-changes/modal-interiors.json]]与[[design-changes/modal-form-scroll.json]]。
- 验证：共享表单/控件/观察器回归通过；新增断言要求计数必须正常流且在textarea之外，而非仅检查底部padding，保留18/23/24/33/34计数节点和原输入事件测试。此前padding测试不能证明滚动不重叠，本次不再作此推断；真实浏览器视觉尚未验证。
- 引用：来源与预览共享CSS、频道注册表和目录缓存同步为`20260908-count-row-1`；没有新增预览副本，未发布。

## 23与24正文独立滚动

- 目标：23客户资料介绍和24沟通纪要使用受视口约束的可收缩布局，仅正文滚动；24不搬动React管理的标题、表单或底栏，保留原保存/校验/关闭事件。不改宽度、按钮、字体、计数规则或08。
- 完成：共享CSS只对这两个源类名约束白色表面高度并补齐flex收缩链；23原Ant body滚动，24的body承载固定头尾、原form单独滚动。24将横向留白归属从外body移至标题/form/底栏以保持内容对齐和滚动条贴边，不复制DOM或预览样式。视觉参数见[[design-changes/modal-form-scroll.json]]。
- 引用：共享CSS、频道注册表和预览目录消费者缓存更新为`20260908-customer-scroll-1`；生成目录按源注释重新生成，弹窗数量不变。
- 验证：共享表单/控件/观察器DOM回归通过；新增23/24真实结构对应的收缩链、滚动拥有者、留白和节点/值保留断言，保留23代理事件、24保存禁用态测试。浏览器几何、短视口滚动和选择面板裁切尚未验证，未发布。

## 07与08正文滚动及33与34重复内边距

- 目标：33/34只保留共享正文的横向留白；07/08保留自然高度和业务表单，补齐从外框到正文的可收缩布局链，标题与底部不参与正文滚动。不改15或其他弹窗的宽度、定位及业务行为。
- 完成：33/34内层form横向padding归零；07外框、form与body均参与可收缩布局，08外框限高、正文滚动且字段不压缩。所有修改均在真实来源CSS，预览不复制DOM或样式。视觉参数唯一记录见[[design-changes/modal-form-scroll.json]]。
- 引用补齐：活动增强资源原仅由活动中心根HTML直引，现登记到公共频道资源表并去掉重复直引；增加重复加载保护。任意根入口的Hash切换使用同一报名弹窗来源。修改资源、公共频道注册表及预览目录消费者更新到`20260908-form-scroll-1`。
- 验证：共享表单/控件/观察器、公告新建编辑删除、弹窗目录及Hash导航源码/DOM检查通过；07中间form收缩、08字段不压缩及33/34默认/窄屏内边距均有针对性断言。公告测试时钟固定在示例有效期，消除随真实日期失效的问题，不改业务数据或时间判断。浏览器实际滚动、几何与按钮可达性尚待验收，未发布。

## 15目标范围布局与可见面板居中

- 目标：将当前目标移至目标节点标题同行，统一缩小渠道/节点标签下距，拉开折叠与单选间距并增加树可见高度；消除底部预览控件预留导致的白色面板偏上。只改真实来源，保持选择/搜索/三步流程及原宽度。
- 完成：`config-center.js`调整真实标题行结构，完整路径title随选择同步；`config-center-content.css`通过局部密度变量复用共享标签样式，树列表增加可见高度，叶节点占位同步。白色面板与原生dialog等高，打开前显式接入共享居中；结果切换器留在面板外，不再参与中心计算。源/频道/预览引用缓存为`20260908-bulk-scope-1`。
- 验证：配置中心真实三步流程、路径迁移/更新、搜索/折叠/选择和返回确认DOM回归通过；目录与导航检查通过。视觉参数见[[design-changes/config-bulk-import.json]]；真实浏览器居中、滚动与面板外切换器点击尚待现场确认，未发布。

## 批量导入返回确认接入公共组件

- 目标：把「返回重新选择？」迁移为共享确认组件生成的原生dialog；业务只提供文案与回调，取消/关闭/Esc保留第二步，确认才清空并返回第一步。增加独立真实源预览登记，不加载父导入页面，保留既有编号；删除旧确认卡片私有样式。
- 完成：共享`global-modal.js`新增`createConfirm()`外壳工厂并复用`setConfirmState()`；业务只传文案、绑定取消/确认和焦点恢复，不复制标题/关闭/按钮结构。旧私有卡片CSS已删除，确认框改为原生顶层dialog，父窗关闭或导航时一并清理。
- 登记：36「批量导入返回重新选择确认」加入“操作确认”，通过同一`showBulkImportReturnConfirm()`真实工厂打开；独立预览不创建或隐藏父导入页面，既有01—35编号不变。目录为36 ready / 0 pending / 17 excluded，分类总览数量改为读取目录。
- 验证：取消/X/Esc不清空校验、确认才返回第一步、重复调用不重复弹窗、原生close及父窗关闭清理、焦点返回、独立预览关闭再开均通过DOM回归；共享表单/控件/观察器、目录与Hash导航检查通过。实际原生顶层和视觉仍需浏览器验收，未发布。参数见[[design-changes/config-bulk-import.json]]；来源JS/CSS/目录缓存为`20260908-bulk-return-1`，共享JS为`20260908-confirm-factory-1`。

## 15校验步骤目标路径与整行状态色

- 目标：第二步目标节点独占完整一行，统计与说明放下方；补完整路径原生悬停提示。可导入/失败状态分别整行着浅绿/浅红底，保留文字状态、失败原因、表格滚动与导入逻辑。不改弹窗外框或其他弹窗；预览直接调用真实来源。
- 完成：`config-center-content.css`按弹窗自身需要始终分行，不再依赖整页宽度断点；原路径节点在`config-center.js`中保留完整文本并增加转义后的title原生提示。状态背景覆盖整行所有单元格，hover沿用对应色相，原状态文字及失败原因保留。CSS/JS、频道注册表、真实源登记与预览索引缓存同步为`20260908-bulk-validation-1`，未复制预览样式。
- 验证：完整导入流程、正式入口短路径、真实无背景预览深层路径及关闭重开后的提示更新、两类状态全部单元格选择器覆盖、hover声明、宽度/滚动约束、目录与Hash导航、JS语法及diff检查通过；DOM和源码不代替浏览器视觉，实际悬停提示、色块观感待刷新验收。参数见[[design-changes/config-bulk-import.json]]，未发布。
- 附加核对时发现第二步「返回重新选择？」缺独立登记和完整共享确认接入；用户随后要求接入，现已处理，见[[#批量导入返回确认接入公共组件]]。原生文件窗口不计入业务弹窗。

## 15目标节点单选标记与弹窗收窄

- 续调完成：根据用户最新反馈，15真实来源外框宽度已与16「调整节点」一致；只调整15宽度上限，保持三个步骤同宽、既有响应式边距及内部表格滚动，不改16或单选交互。最终参数更新在原视觉记录中，不累加中间方案。
- 续调验证：配置中心真实流程DOM回归、15/16宽度约束相等及滚动边界源码断言、弹窗目录/预览登记、Hash导航与diff检查通过；未做浏览器视觉验收，需刷新确认实际观感。
- 目标：15「批量导入成员」将尾部勾号改为与10/16共用的单选圆点，保留文件夹、独立展开箭头及现有单选事件；适当收窄三步弹窗，不改16交互、渠道默认值或其他弹窗宽度。
- 完成：`config-center.js`在原节点选择按钮内加入装饰圆点并移除尾部勾号；`modal-controls.css`把15接入10/16同一组Radio声明，未复制样式；`config-center-content.css`仅收窄15外框并删除废弃勾号规则。展开、选择、默认渠道目标及16行为不变；三步同宽，校验表格保留自身双向滚动。
- 引用与记录：续调外框CSS、频道注册表和源/自动索引消费者缓存同步为`20260908-bulk-width-3`；单选CSS与业务JS仍使用`20260908-bulk-radio-1`，逻辑未改。自动索引按生成脚本更新。视觉参数见[[design-changes/config-bulk-import.json]]和[[design-changes/modal-controls.json]]，范围见[[docs/modal-control-coverage]]。
- 验证：真实流程、单一选中、圆点点击沿用原事件、展开不改选择、渠道重绘、关闭重开、15/16命中同一Radio声明以及16当前节点禁选均通过源码/DOM检查；目录与Hash导航检查通过。JSDOM对嵌套:is/:not的匹配限制在测试中展开外层分支处理，不改生产选择器或删掉测试条件。浏览器尺寸观感与图标对齐仍待刷新验收，未提交或推送。

## 弹窗日期时间图标按用途统一

- 目标：使用用户提供的日历/时间SVG替换共享资产；日期和日期范围统一日历，纯时间统一时钟，24原自定义日期下箭头不再保留为箭头。普通Select不变，来源与预览共用，保留原节点、选择/清除/校验和交互态。
- 完成：附件原文副本位于`shared/assets/modal-calendar.svg`与`modal-clock.svg`，由`shared/styles/modal-controls.css`按控件用途直接绘制。共享Ant日期/范围（包括24旧自定义下箭头）使用日历，clock-circle语义的纯时间使用时钟；原生date/datetime-local与time同样区分。原节点、业务事件与交互状态不变，仅弹窗内，不扩展到页面、抽屉或AI Agent主面板。
- 引用：15个正式根入口及2个组件预览入口共用`modal-controls.css?v=20260908-datetime-icons-1`；没有另建预览样式或修改原站web构建产物。参数唯一记录见[[design-changes/modal-controls.json]]；接入范围见[[docs/modal-control-coverage]]。
- 验证：两份资产与附件逐字比较一致（忽略文件末尾换行）。24真实后缀宿主、Ant日期/范围/纯时间、原生date/time/datetime-local、普通Select不误替换和页面排除的源码/DOM断言通过；弹窗表单及生命周期、公告业务、目录、Hash导航和diff检查通过。实际浏览器图标对齐与显示仍待刷新验收，未提交或推送。

## 32系统警告布局接入共享确认规则

- 目标：32「检测到账号已切换」标题到正文间距使用共享确认参数，正文不再受警告图标列挤压，长账号按共享正文规则换行。保留警告图标、不可取消及原立即刷新事件，不移动或复制React节点，不改预览单独样式。
- 完成：仅`global-modal.css`调整确认正文换行与32内部布局盒；原Ant paragraph使用`display:contents`参与共享网格，图标/标题同排、正文占完整第二行，React节点不搬动。普通确认标题与32共用同一间距token。来源/预览CSS与登记缓存统一为`20260908-warning-layout-1`，JS业务处理不变。
- 验证：补齐真实Ant body-wrapper/paragraph嵌套结构，不再以简化的相邻标题正文代替；校验同一间距token、正文跨列、长账号原文保留、唯一刷新事件、重复接入/重绘/重新挂载。表单/控件与异步生命周期、资源目录、Hash导航、语法及diff检查均通过。视觉参数记录于[[design-changes/modal-interiors.json]]，浏览器现场视觉仍待刷新验收，未提交或推送。

## 计数重叠与24下拉图标漏接修复

- 目标：修复计数文本域底部留白被普通内层字段规则覆盖的问题；核对24沟通纪要及其他弹窗选择器图标，补齐自定义日期下拉箭头的共享引用。仅修改公共弹窗样式与回归测试，不复制预览、不改业务字段/事件。
- 完成：`global-modal.css` 的普通内层文本域规则排除计数变体，保留原文本域/计数节点及事件；`modal-controls.css` 补齐24两个日期字段自定义后缀的共享下拉图标。普通Select、原生和多选已有共享入口；日历、时钟、树展开图标不替换。来源及预览CSS、登记缓存统一更新为 `20260908-counter-icon-1`。
- 验证：18/23/24/33/34实际匹配padding的优先级及顺序检查、公告三语言输入上限计数、24真实源三处suffixIcon与宿主匹配、标准日历/时钟及页面排除测试通过；恢复旧CSS的内存对照运行能触发新增回归失败。完整弹窗字段/控件与观察器、公告业务、目录及Hash导航检查通过。浏览器现场视觉未验证，需刷新确认；未提交或推送。
- 参数与覆盖记录：[[design-changes/shared-modal-fields.json]]、[[design-changes/modal-controls.json]]、[[docs/modal-control-coverage]]。不据源码检查宣称任意无语义自定义控件均已自动统一。
- 后续：24日期字段不再使用下箭头，已按用户新的用途规范统一为日历，见[[#弹窗日期时间图标按用途统一]]；本节保留上次修复依据。

## 15批量导入模板区域裁切修复

- 目标：第一步内容超出剩余高度时允许正文纵向滚动，完整访问下载模板区域；保留固定头部、步骤与底部按钮，以及校验步骤的独立表格滚动。不改样式参数或复制预览DOM。
- 完成：`config-center-content.css` 为非校验步骤正文增加纵向滚动和滚动链限制；原 `overflow:hidden` 仍负责校验步骤，后者由表格容器滚动。模板保持在真实正文内，不移动到按钮栏，不改变字号、尺寸、间距或视觉参数。来源及目录资源版本更新为 `20260908-bulk-scroll-1`。
- 验证：配置中心真实流程及无背景预览控制器通过；新增第一/二/三步滚动规则命中、头尾同级、模板归属断言，目录及导航检查通过。基于用户截图和源码定位，未进行受限的浏览器现场操作，实际滚动视觉待刷新验收。未提交或推送。

## 操作确认加载循环修复

- 目标：复现并修复32系统警告接入后的class观察循环；测试真实异步观察器下的挂载、重绘与再次打开，不改分类、样式或刷新行为。
- 原因与修复：上次新增的 `adoptSystemWarning()` 在每次扫描中无条件 `classList.add()`，相同值也产生class变更记录，触发同一观察器再次扫描。改用现有带存在性检查的 `mark()`；相同状态重复接入的class写入由1次变为0次。来源与预览统一加载 `global-modal.js?v=20260908-observer-fix-1`，CSS及分类不改。
- 验证：新增 `scripts/test-modal-observer-lifecycle.cjs` 并加入 `test:modal-forms`，验证无写入契约、真实MutationObserver下的挂载/类名重绘/卸载再开、单按钮原事件。修复前复现循环超时；修复后专项及16表单、控件、预览目录、导航、语法检查通过。未进行浏览器现场验收；旧页如已卡住须关闭该预览标签后重新打开。未提交或推送。

## 弹窗五项一致性续修

- 目标：修复24的Ant关闭伪元素显示；计数区按有无resize手柄分开留白；15/16节点搜索改通栏组合布局；32归入操作确认并以非破坏方式接入系统警告变体；表单标题到内容由单一间距拥有者控制。保留单按钮警告不可关闭和原刷新逻辑、节点选择权限及所有真实DOM事件。
- 完成：以上五项接入共享源码，来源及预览资源统一版本 `20260908-five-fixes-1`；32保留原DOM及唯一刷新按钮。设计参数见 `design-changes/modal-interiors.json` 与 `design-changes/shared-modal-fields.json`，不复制预览样式。
- 验证：16个表单契约、五项专项DOM/规则断言、控件交互、目录与Hash导航检查通过。浏览器访问仍受限制，未绕开，实际图标、间距及滚动视觉仍需刷新人工验收；DOM测试不等于浏览器验收。未提交或推送。

## 23与24共享标准底栏

- 目标：23 代理取消/保存按钮创建时即完成共享标记并支持重新接入；23/24 同用标准底栏的对齐、间距和分割线，不改其他弹窗底栏、原 React 字段或业务处理器。来源与预览使用同一实现。
- 完成：`global-modal.js` 优先识别23代理底栏，创建时标记公共按钮；23/24 显式接入 `global-modal.css` 的 standard 底栏变体，24 不搬动原节点。共享 CSS/JS 实际消费者统一版本 `20260908-footer-23-24-1`，自动索引更新，未发布。参数见 `design-changes/modal-interiors.json`。
- 验证：首次按钮标记、样式规则匹配、类名替换后重新接入、无重复代理底栏、取消/保存单次委托、24只保留保存及原父节点均通过；16个表单契约、控件、目录/预览、Hash导航、JS语法及diff检查通过。23现场失效的完整运行时链路尚未实测；本轮加固了接入路径，实际按钮/分割线视觉需刷新验收，不将DOM结果视作浏览器通过。

## 计数文本域右下角错位修复

- 续修目标：移除共享 Ant 多行外壳继承的 `::before` 基线占位，消除内部块级 textarea 前的空白行；只改伪元素，不再次调整正文内距、计数空间或尺寸。
- 续修完成：共享多行外壳取消占位伪元素，单行外壳不匹配；CSS 消费者更新为 `20260908-textarea-strut-1`。补充 18/23/24 外壳匹配及单行排除断言，16 个表单契约、控件交互、目录/预览、Hash 导航及 diff 检查通过。JSDOM 不支持实际伪元素排版，本轮只确认源规则/作用域与事件，视觉待刷新验收；参数记录于 `design-changes/shared-modal-fields.json`。
- 目标：用户截图的 24 沟通纪要中，外壳独立计数留白使内部原生拖拽手柄悬在框底之上；修复共享计数布局并核对 18/23/33/34，不改预览副本、原值/限制/事件或手柄功能。参数更新到现有文本域台账，视觉以用户截图为问题证据。
- 完成：仅修改 `shared/styles/global-modal.css` 的计数文本域规则；取消外壳底部留白，计数空间由内部文本域提供，字数原节点避开拖拽热区并取消旧位移，补齐矮文本域最小容纳高度。实际消费者统一使用 `20260908-counter-corner-1` CSS；JS/业务入口保持原版本，未发布。最终参数见 `design-changes/shared-modal-fields.json` 和既有内部组件记录。
- 验证：新增 23/24 原 Ant rows/showCount/resize 结构回归，检查计数及字段身份、输入事件、字数限制和拖拽声明保留；18 和 33/34 核对同一规则。表单/控件、公告业务、目录/预览、Hash 导航和 diff 检查通过。未做真实浏览器视觉或拖拽命中验收，需刷新现场确认。

## 确认与表单内部组合组件统一

- 续检目标：核对两类弹窗的间距/字号/颜色遗漏，处理 10 管理员标签、13 成员文字、22 按钮内距叠加、33/34 公告分组间距和辅助文案；补同一 token 与源节点契约回归。源码层执行，不以浏览器视觉验收名义交付。
- 续检完成：10 横排管理员标签、13 成员及账号/计数、15 导入字段、16 目标标签、22 内嵌按钮组、33/34 公告分组与辅助说明已接入共享字号/行高/间距/颜色参数；确认正文也使用同一正文行高。修正结构分组与末字段间距叠加，必填颜色收口，禁用文本域外壳与禁用选项保持语义状态。具体初值与最终参数仅记录在 `design-changes/modal-interiors.json`。
- 续检验证：16 个表单源/旧组件契约、共享控件交互、15 导入流程、预览登记（35 ready / 0 pending / 17 excluded）、目录校验、Hash 导航、JS 语法及 diff 检查通过。测试新增真实源节点与共享规则匹配断言；不把 JSDOM 当完整 CSS 层叠/视觉引擎。浏览器视觉尚未验收。
- 目标：按用户确认，统一两类弹窗的圆角层级、提示/空态、列表、选项卡片、字段布局、计数文本域，以及批量导入步骤/附件/校验表格/摘要；复用共享按钮和操作栏。保留业务结构、事件、列数、宽高约束与滚动拥有者，不涉及信息展示、抽屉或 Agent 主面板。
- 范围：共享 `global-modal.js/css`、控件 token、实际消费者与回归；参数记录在 `design-changes/modal-interiors.json`，原参数基线未知的保持空值，不覆盖其他任务修改。状态：代码已接入，来源/预览资源版本统一为 `20260908-spacing-type-1`，未发布。
- 接入方式：`adoptInteriors` 为真实业务节点增加共享语义类，不重建、移动或复制字段；支持 `data-gaip-modal-part` 显式接入。保留树层级、字段身份、隐藏提示、计数/自动高度、表格列宽和导入状态机。圆形标记不改成圆角方框。
- 验证：`test:modal-forms`（16 个源/旧组件契约及控件状态）、真实 15 上传→校验→结果→重试、配置中心与公告业务回归通过；目录生成校验、预览登记、导航、JS 语法及 `git diff --check` 通过。预览维持 35 ready / 0 pending / 17 excluded。浏览器 file 访问受限，未绕过；实际圆角、排版、图标、滚动和各状态视觉待人工刷新验收。

## 18 编辑对话名称输入组件接入

- 目标：带计数的 Ant TextArea 外壳接入共享输入框规则，保留真实节点、100 字限制、自动增高和保存事件；仅修改共享接入层，不改 Agent 主面板或复制预览组件。
- 状态：共享 `adoptFields` 接入 Ant 文本域 affix 外壳，复用输入框边框/状态/文字规则，内部不再叠加边框；单行和多行高度规则分离，不覆盖 React 自动高度。来源/预览统一使用 `20260908-textarea-1`，自动索引已生成，参数更新 `design-changes/shared-modal-fields.json`，未发布。
- 验证：补充 18 带计数外壳结构回归，检查原文本域/计数节点、100 字限制、输入事件和动态高度保留；表单与共享控件 DOM、索引及导航检查通过。实际视觉仍待刷新确认，不把 DOM 模拟视作浏览器验收。

## 15 批量导入成员下拉点击修复

- 目标：修复共享原生下拉面板继承 Ant 弹窗外层 `pointer-events:none` 导致选项不能点击；只恢复面板命中，不改变外壳、遮罩、图标与视觉参数。补真实批量导入源的选择、重绘、再次展开和关闭回归。
- 状态：共享 `modal-controls.css` 已只对 `.gaip-native-popup` 设置 `pointer-events:auto`，17 个入口/预览引用同一新版 `20260908-popup-click-1`；不改 Ant 外壳的点击穿透，不重写业务选择事件，未发布。
- 验证：新增回归先复现面板继承 `none` 失败，修复后通过；使用真实批量导入工厂，验证连续切换三个渠道、目标同步、重绘后再次展开、Escape 和关闭清理。共享控件 DOM/CSS 检查通过；浏览器真实点击命中仍待刷新现场确认。

## 弹窗清除图标本地显示修复

- 续修：针对用户反馈的上下疑似裁切，共享热区/图形补齐 min/max 尺寸与强制居中、不收缩约束；hover/active 保持默认底色，仅图形加深，不再出现灰色方底。保留选择器清除按钮的白色底以遮住同槽箭头、键盘提示、源事件与显隐逻辑；全部消费者 CSS 版本更新为 `20260908-clear-box-1`。参数同步 `design-changes/modal-controls.json`；真实裁切效果仍需刷新现场确认。
- 目标：修复单行输入/搜索及已有选择器清除图标的外部 mask 显示风险，保留显隐与原清除事件。用户截图中的“保存的信息”为浏览器自动填充 UI，不修改浏览器设置或项目自动填充行为。
- 状态：本地完成，未发布；清除伪元素改为本地 SVG 背景图，文件使用明确颜色，透明度由共享 CSS 保留原默认/hover 语义。SVG/CSS 消费者缓存同步，不修改字段和事件。
- 验证：单行清除真实事件、原节点身份、空值/禁用/只读、重复接入、直接图像与无mask规则，以及表单/控件 DOM、索引/预览、导航、diff 检查通过。未做浏览器视觉验收，实际需刷新后对有值字段悬停/聚焦确认。

## 弹窗下拉箭头本地显示修复

- 附件替换完成：共享 `modal-down.svg` 使用用户提供的 `Downloads/ICON/通用/下拉.svg` 原文，保留显示尺寸与定位；SVG 与 CSS 消费者缓存均更新。附件一致性、控件 DOM、索引/预览、导航及 diff 检查通过，真实视觉待刷新确认，未发布。
- 目标：修复用户截图中 15 批量导入成员的下拉箭头缺失，并同步相同实现的 Ant/多选箭头；保留几何、定位和交互，仅将外部 SVG mask 改为本地图像直接绘制。
- 状态：本地完成，未发布；`modal-controls.css` 的原生代理与 Ant/多选箭头直接绘制同一 `modal-down.svg`，保留原几何并将原蒙版色写入 SVG。实际消费者版本统一为 `20260908-arrow-1`，不改选择数据或展开逻辑。
- 验证：表单/控件 DOM、两个箭头渲染规则及无外部mask契约、弹窗索引/预览、导航与 diff 检查通过；真实浏览器视觉仍需刷新确认。参数见 `design-changes/modal-controls.json`。

## 04 与 07 共享副标题

- 目标：将两处标题区上下文说明接入共享副标题，统一文字与主副标题间距；04 单行省略、07 换行及背景保留。不重建源 DOM，不复制预览样式。
- 状态：本地完成，未发布；真实源节点由 `adoptForm` 添加共享副标题标记，`global-modal.css` 统一文字与间距，参数记录于 `design-changes/shared-modal-subtitle.json`。只识别明确副标题类或 `data-gaip-modal-subtitle`，不自动猜测其他标题文字。
- 实现：共享 JS/CSS 及全部消费者版本更新为 `20260908-subtitle-1`，自动索引由生成器更新；04 截断、07 换行与原背景规则不改。
- 验证：04/07 真实源副标题接入、重复接入幂等、文字/父节点保留、两种 CSS 顺序及内嵌活动名文字规则、表单/控件 DOM、索引/预览与导航检查通过；未做真实浏览器视觉验收。

## 弹窗表单交互色与无光圈

- 状态可见性续修完成：选择器的控件类留在 `:where` 外，状态规则与默认边框同为 (0,4,0)，按默认→hover→focus→active 顺序覆盖；合并展开聚焦规则，移除后置高权重 expanded 覆盖。错误/禁用继续更高优先级。颜色与无光圈目标不变，缓存更新 `20260908-state-cascade-1`。新增真实失效机制的规则结构/顺序检查，表单/控件 DOM、索引/预览及导航通过；未做浏览器视觉验收。
- Active 续调完成：按下使用独立稍深青色，松开仍沿用原 Hover/Focus；错误/禁用及无光圈保持。更新同一共享源、消费者版本与视觉台账；表单/控件 DOM、索引/预览及导航检查通过，浏览器视觉待确认。
- 目标：仅弹窗表单 hover/focus/active 边框统一为用户指定的青色，取消聚焦光圈；保留禁用与错误语义、底部按钮和页面表单。修改共享源及消费者版本，预览不另写样式。
- 状态：本地完成，未发布；共享 `--gaip-form-interaction` 收口文字字段、搜索、选择器及日期/时间的状态边框，普通与错误聚焦均不显示光圈。键盘保留边框/底色或下划线提示，底部按钮与弹窗本体阴影不改。
- 文件：`shared/styles/global-modal.css`、`shared/styles/modal-controls.css`；所有消费者/源登记同步版本，自动索引由生成器更新。参数合并至 `design-changes/shared-modal-fields.json` 和 `design-changes/modal-controls.json`。
- 验证：16 项表单契约、控件状态 DOM/源码、无光圈规则、登记/预览与导航检查通过；真实浏览器视觉待刷新确认。

## 04 客户归属选择列表内嵌边框

- 目标：参照 19 分配线索的圆角边框列表，将 04 客户选择区内嵌到弹窗正文，滚动保留在框内；保留客户信息、选择态和业务逻辑，仅修改真实来源样式及资源版本。
- 状态：本地完成，未发布；真实 `.gaip-owner-list` 增加内嵌留白、边框和圆角，保留 grid 弹性高度与框内滚动，不新增预览副本。参数记录：`design-changes/proposal-owner-list.json`。
- 实现：`features/proposal-center/proposal-center.css`；频道配置与预览源登记统一 CSS 缓存 `20260908-owner-frame-1`。未改 19、客户信息、选择标记或业务事件。
- 验证：表单/控件 DOM、弹窗索引/预览、导航保护、列表容器规则与版本一致性、diff 检查通过；真实浏览器视觉仍需刷新确认。

## 弹窗预览精简分类入口

- 目标：去掉“全部”筛选入口，仅保留信息展示、表单操作、操作确认；默认显示信息展示。原弹窗编号、来源、分类总览及业务样式不变。
- 状态：本地完成，未发布；仅预览导航行为变更，不新增视觉参数记录。
- 实现：`全局组件/弹窗预览.html` 不再生成“全部”按钮，默认选中信息展示；点击上方总览名称时只打开对应分类，不再隐式切回全部。保留三个按钮原样式、35 项真实源及延迟加载。
- 验证：`scripts/test-popup-preview.cjs` 新增真实父页脚本 DOM 检查，覆盖三个筛选、默认选中、总览跨类定位、35 卡片保留与无提前 iframe；弹窗索引、预览、导航保护及 diff 检查通过。未做本轮真实浏览器视觉验收。

## 表单控件一致性续修

- 单选指示器统一完成：以 10 配置中心成员编辑的 Ant Radio 为来源，16 调整节点及弹窗内 Ant/原生单选使用共享几何和选中态，消除 16 原白色内环及 Ant 旧圆点负 margin；只改 `modal-controls.css` 和消费者缓存 `20260908-7`，不修改选择数据、回调或文字布局。参数见视觉台账；控件/表单 DOM、配置中心业务、索引/预览、导航检查通过。未做浏览器视觉验收，未发布。
- 附件图标替换完成：仅弹窗共享搜索采用用户附件 `Downloads/ICON/通用/搜索.svg`，本地保存为 `shared/assets/modal-search.svg`；保留尺寸、留白、搜索与清除行为，不覆盖普通页面的旧搜索资源。共享 CSS 与全部消费者更新至 `20260908-6`；附件内容一致性、控件源码/DOM、索引/预览、导航与 diff 检查通过，未做浏览器视觉验收、未发布。
- 搜索图标缺失续修：04/15/16 共享前缀从外部 SVG mask 改为直接引用同一本地 `control-search.svg` 背景图，避免 file 模式下蒙版加载限制；保留原尺寸与图文留白。仅 CSS 消费者版本更新为 `20260908-5`，不改清除/表单事件。新增直接资源引用与无外部蒙版契约检查；未进行真实浏览器视觉验收，需刷新确认。
- 目标：共享搜索图标/留白，统一可编辑单行文本与搜索清除规则，补齐下拉及日期时间面板状态与选项尺寸；仅弹窗内部，保留字段、校验、树层级、多行计数与业务布局。
- 状态：本地实现及源码/DOM 验证完成，未发布；复用 `modal-controls.js/css`，参数合并至 `design-changes/modal-controls.json`，不另建预览样式。
- 实现：表单搜索共用前缀槽；单行清除优先复用真实源，无原按钮则添加兄弟按钮，保留输入节点/父级、校验和源事件；禁用/只读不清除，补充键盘与旧清除显示同步。选择器保留各自业务结构，统一选项密度和状态。正式入口及预览更新相同资源版本 `20260908-4`。
- 检查：04/07/08/10/11/15/16 的真实来源清除、幂等、原字段身份、禁用/只读、原生选择空值状态、16 项表单接入，以及配置中心/公告业务回归通过。10/11 测试改用完整业务 open 入口，不用未绑定事件的纯标记工厂冒充真实清除。浏览器 file 访问受限，实际布局/hover仍需刷新复核；详细边界见 [[../../docs/modal-control-coverage|覆盖清单]]。

## 确认弹窗按钮间距收口

- 目标：统一清除公共确认组件底部按钮自身的旧外边距，解决 12 删除部门确认与 17 调整管理员节点确认的间距叠加；不改按钮尺寸、颜色及业务事件。
- 状态：本地修复完成，未发布；参数记录在 `design-changes/shared-modal-confirm.json`。来源和预览共用同一 CSS，不按编号另写补丁。
- 实现：`shared/styles/global-modal.css` 对公共确认 footer 的直接按钮统一清零 margin，并以 important 阻止配置中心 scoped Ant 普通外边距叠加。只更新实际消费者的 CSS 缓存版本及对应频道/源登记，自动索引由生成器更新。
- 验证：12、17 真实源按钮均匹配同一清零规则；重要性与 footer gap 契约、`test:modal-forms`、`check:modals`、`check:navigation`、diff 检查通过。浏览器 file 访问受限，本轮为源码/DOM 验证，实际视觉需刷新复核。

## 弹窗控件错位续修

- 目标：修复弹窗内选择面板、清除/下拉图标，以及 22 新增线索的重复位移；单选、多选使用用户指定选中色。仅真实共享源，页面表单、抽屉和 Agent 主面板不改。
- 状态：本地源码修复及 DOM/静态检查完成，未发布；视觉参数已合并到 `design-changes/modal-controls.json`，保留首次基准与其他任务的区域语义变更。浏览器现场对齐仍需验收。
- 实现：`modal-controls.js/css` 修正原生展开面板坐标、图标居中/占位、单选文字与搜索输入高度，隔离日历内部输入；`global-modal-position.css` 仅取消 22 新增线索旧 transform 的二次上移。所有正式入口与预览引用更新为相同资源版本 `20260908-2`，不修改原站 `web/`、业务保存逻辑及共享区域语义 JS。
- 检查：`test:modal-forms`、`check:modals`、`check:navigation` 通过；新增包含块平移/缩放与顶层分支模拟、图标/隐藏规则、22 定位覆盖及选择色检查。详见 [[../../docs/modal-control-coverage|覆盖与验收缺口]]，模拟通过不代表实际视觉通过。

## 确认与表单弹窗区域划分

- 状态：源码与 DOM 检查完成，未发布；本轮未进行真实浏览器视觉验收。
- 目标：仅为“操作确认”和“表单操作”建立标题区、内容区、底部操作区及可选扩展区的真实节点语义；信息展示分类不动。
- 边界：不复制预览 DOM/CSS，不重排 React 节点，不改变尺寸、滚动、业务状态或事件。区域标记不代表内部布局已全部标准化。
- 实现：`shared/scripts/global-modal.js` 新增 `getRegions` / `adoptRegions`，复用原确认/表单接入流程；共用 JS 缓存标识 `20260908-regions-1`。区域、外壳、关闭控件及旧嵌套关系使用 data 属性标记；所有消费者仍引用真实共享源。其余 HTML/频道/源登记修改仅更新此 JS 引用；自动索引由生成器更新。
- 职责及编号对照：[[../公共模块/全局框架与样式#确认与表单弹窗区域职责]]。既有 CSS、普通字段与另一任务正在维护的控件共享层未在本轮改动，因此不新增视觉参数记录。
- 验证通过：`node --check shared/scripts/global-modal.js`、`node scripts/test-modal-form-primitives.cjs`（真实工厂、旧结构、动态底部、区域幂等、原节点/父级保留、信息展示排除）、`node scripts/test-config-center.cjs`、`node scripts/test-announcement-management.cjs`、`node scripts/generate-modal-catalog.cjs --check`、`node scripts/test-popup-preview.cjs`、`./scripts/verify-local-navigation.sh`、`git diff --check`。源码登记数量仍为 52 / 35 ready / 17 excluded；这些结果不等于全弹窗浏览器视觉通过。

## 弹窗内部控件完整共享层

- 状态：本地源码接入及 DOM 检查完成，未发布；实际视觉仍待验收。仅普通弹窗及其所属展开面板，排除页面表单、抽屉、AI Agent 主面板。
- 范围：清除/展开/日期时间图标、选择器及面板、单选/多选/标签、数字输入、树勾选、表格选择与分页、上传区、状态色；保留业务字段、数据、验证、回调和个性化内容布局。原生 select/date/time 使用统一弹窗内选择面板，原表单控件保留作为数据及验证源。
- 共享来源：`shared/scripts/modal-controls.js`、`shared/styles/modal-controls.css`；旧普通输入仍复用 global-modal；参数与首次本地依据保存于 `design-changes/modal-controls.json`。
- 状态范围：未输入/占位、已输入、hover、按下、焦点/键盘焦点、选中/半选、禁用、只读及现有错误；日期和时间面板、下拉空态也在范围。清除图标复用原站几何，新增本地共享 SVG 只作为原素材的统一入口，不改原素材。
- 接入：所有正式根入口及真实源预览增加同一控件资源；普通输入样式未复制，仍由 `global-modal.css` 拥有，作用域扩至普通弹窗的明确控件宿主。Ant 外置面板通过 aria 关系或实际弹窗触发匹配归属；原生面板保留值、required/min/max/step 及业务 input/change。日志原生 showPicker 仅在共享面板已处理点击时让行，内联日志页面不变。
- 验证：16 项表单源/旧源接入契约、配置中心、公告、日志、登记及导航检查通过；新增 `scripts/test-modal-controls.cjs` 验证页面/抽屉/Agent 排除、单多选、日期范围、时间步长、空/已填、只读、禁用、Esc、父弹窗关闭清理、外置面板归属和控件移出弹窗恢复原生 UI。测试已加入 `test:modal-forms`。
- 覆盖与剩余项见 [[../../docs/modal-control-coverage|弹窗内部控件覆盖清单]]：保留业务图表、附件和内容布局，开关只提供兼容主题不冒称实例验收；真实浏览器图标对齐、定位、响应式以及各状态颜色仍需验收，DOM 检查不能证明全部 35 项实际视觉一致。

## 表单项与普通输入框共享样式

- 状态：本地源码及 DOM 检查完成，未发布；真实浏览器视觉待确认。
- 本轮范围：10 配置中心成员编辑、11 部门名称编辑、18 编辑对话名称作为代表，将已接入表单弹窗的普通输入、文本域、字段标签及现有帮助/错误提示接到共享样式；保留真实节点、校验、清除事件和文本域自动增高。选择器、树、单选卡片、上传及复杂布局不在本轮范围。
- 参数唯一记录：`design-changes/shared-modal-fields.json`；只修改真实共享源，不在预览另写样式。
- 实现：`shared/scripts/global-modal.js` 的 `adoptFields` 随现有 `adoptForm` 非破坏式接入；`shared/styles/global-modal.css` 拥有标签、普通输入和原有帮助/错误提示的共享视觉。保留带清除按钮的原节点，边框只由外壳绘制；公告的字数占位和所有文本域自动增高仍由源组件拥有。React 更新校验 className 后会幂等补回共享类，不更改字段值和校验状态。
- 消费者资源版本同步为 `20260907-4`；入口、频道配置、真实源登记及自动索引引用同一文件。没有新增预览专属字段样式，也没有修改 `web/` 原站基线。
- 验证：共享表单 DOM 契约（16 项接入、真实工厂字段及旧源结构）、原清除/输入事件、选择范围、禁用/只读、校验 className 更新、文本域约束、字数提示及专用控件排除检查通过；配置中心、公告、登记/预览、导航保护和 diff 检查通过。预览测试的固定缓存版本断言同步至本轮新版本，未放宽检查。
- 限制：工具拒绝本地 `file://` 浏览器访问；未绕过限制，尚未验收真实字体、尺寸、焦点环与响应式排版。不把 DOM 模拟写成视觉通过。

## 视觉样式增量记录规则

- 状态：规则配置完成，未发布。
- 范围：项目 `AGENTS.md` 增加入口与完成条件；详细方法见 [[../../docs/visual-change-workflow|视觉增量记录流程]]，数据入口为 `design-changes/README.md` 和空 `_template.json`；维护流程、知识索引、PROJECT_STATE 同步链接与职责。本次不改业务页面或标注运行时。
- 验证：规则入口、相对链接、空 JSON 格式与限定文件的 `git diff --check` 通过；已检查重复试调、恢复基准、只改位置、基准未知、共享样式和抽离只读的规则衔接。属于文档/格式检查，不代表自动监听、历史回填、标注系统或业务视觉验收已完成。

## 表单弹窗复用确认弹窗基础组件

- 状态：本地代码及 DOM 检查完成，未发布；真实浏览器视觉待验收。
- 本轮续修完成：两类主标题字体契约及预览资源解析；首次本地规则和最终目标记录于 `design-changes/shared-modal-title.json`。全局字体重置排除共享标题及文字子节点，复用项目已有 Bold 文件；主标题只有一条公共文字规则。正文、字段、关闭及按钮视觉不在本轮修改范围。
- 预览：改变 `<base>` 之前固定已有资源的绝对 URL，避免页头共享 CSS/JS 指向项目外目录；字体和组件样式的实际消费者缓存版本同步更新。并未把业务背景页重新加载到直接源预览。
- 本轮验证：两种 CSS 顺序下，两类标题及嵌套文字均选中同一规则（JSDOM）；本地字体 OS/2 字重元数据、资源 URL 不变性、16 项接入、配置中心、公告、导航保护和登记/预览静态检查通过。没有以模拟环境替代字体渲染验收。
- 范围：共享关闭、标题、外壳、操作按钮与间距；16 项表单保持字段、宽度、滚动、事件和校验。本轮只纠正原确认标题与表单标题的文字冲突，其他确认视觉不变，预览引用真实共享源。
- 实现：`shared/scripts/global-modal.js` / `shared/styles/global-modal.css` v1.2.0；共享 `adoptClose`、`adoptForm` 和非破坏式旧弹窗接入器，保留 React 节点与业务事件。正式入口与预览直接加载同一来源（资源版本 `20260907-2`），频道及登记缓存同步更新。
- 特殊兼容：10 的原自定义 footer；19/20/22 的 Umi 自定义按钮；23 保留标题内原操作节点，共享关闭和底部按钮委托原取消/保存；24 保留原自定义关闭事件与导入操作。没有纳入 Agent 主面板、抽屉或信息展示弹窗。
- 上轮接入验证：16 项覆盖（10 个真实源工厂 + 6 个旧 Umi 接入契约，另补特殊 DOM 结构）、动态 footer、节点身份、输入与原事件保留检查通过；配置中心和公告管理 DOM 回归、导航保护、弹窗登记/预览静态检查通过。当时仅证明确认声明未改，未证明跨分类和跨预览的实际字重一致；本轮标题续修补齐了对应规则检查。
- 边界：未做真实浏览器像素/hover/原生顶层验证；DOM 通过不等于视觉验收。新增回归命令 `npm run test:modal-forms` 已加入 DOM 测试入口。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
## 筛选控件溢出与展开面板修复

- 目标：修复标签溢出和多选误收起，单选/日期采用共享筛选自绘面板，日期复用弹窗图标与中文占位；不改变业务条件与配置显隐。
- 完成：共享多选改为保留选项节点、标签内层省略；共享筛选自绘单选/日期面板、日期中文占位和同源日历图标；目录演示增加内边距与正常复选框显示，有效条件收进可展开调试区。业务字段、显隐配置与数据不改。
- 文件：`shared/scripts/global-filter-bar.js`、`global-multi-select.js` 及对应CSS；`全局组件/components-preview.js/css`、目录入口和频道资源版本；规范与 `design-changes/shared-filter-bar.json` 同步。
- 验证：共享筛选、课程管理与V1.1 DOM测试、导航保护通过；新增独立 Chromium 同源组件/目录测试通过，覆盖连续勾选、单选、日期、窄屏与面板边界；不是来源课程管理整页验收。未部署。
- 回退快照：项目集 `本地回退备份/筛选控件修复-20260909-Nr973Y/`；不覆盖之前试版备份，不触碰Mock存储。


## 全局表格与课程管理首接

- 已完成：共享 `global-table.js/css` 统一表头、行操作、工具栏与分页；全局目录 `table-preview.js` 同源展示少量/长列表/宽表及空态/加载/失败。课程管理接入并清理原页面的重复表格样式，保留业务字段、权限和筛选；其他页面未批量接入。
- 资源统一登记，根入口同步 channels 缓存版本；静态预览运行时为原生 DOM，正式 React/Ant 接入边界见 `docs/table-spec.md`。视觉参数：`design-changes/global-table.json`。
- 验证：课程管理、学习 V1.1、筛选 DOM 回归，导航保护、弹窗目录检查通过；真实 file 组件目录及隔离课程控制器通过 Chromium 高度/表头/固定列/分页/筛选收拢与 1440/1100/800 视口检查；完整学习中心 file 入口成功加载同一实例，8 条记录的分页底边为 976px（1000px 视口）。仅本地完成，未发布。
- 修改前本地快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格-20260909-140946`。


## 全局表格简化与固定列阴影

- 按用户反馈移除内置列表标题/数量行及排序；核对全局滚动条继承，固定列竖线改为随横向遮挡变化的阴影，并参考官方文档总结分页规范。已完成。
- 已移除表格内部标题、摘要和排序；课程管理和目录同源更新。分页外观暂未改，建议见 docs/table-spec.md。
- Chromium148已验证全局原生滚动条颜色继承、左右阴影端点与无溢出时消失，以及高度和分页回归；课程管理DOM、导航保护通过。旧Chrome119不支持标准滚动条颜色时原生回退。视觉记录已同步，未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格简化-20260909-144024`。


## 全局表格正文滚动与精简分页

- 已完成：表格局部滑块32%改18%，原生宽度不变；可见表头移出竖向滚动区并同步横向位置，语义表头仍保留给辅助技术。分页去掉跳转，普通页码无边框、当前页白底绿框，单页只留总数和条数。
- Chromium148验证表头/正文列对齐、滚动区顶边、局部滚动条颜色、固定阴影、分页/筛选收拢；课程管理DOM与导航保护通过。共享源码、缓存、规范和视觉记录已同步，未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格正文滚动-20260909-150059`。


## 表头通栏与分页自定义下拉

- 已完成：表头通栏底色覆盖竖向滚动条上方留白，内部列保持对齐；单页保留白底绿框1。条数选择改为自定义面板并复用control-down.svg，支持上下避让、选中、键盘、Esc/外点关闭与卸载清理。
- Chromium148验证通过，课程管理DOM与导航保护通过；共享缓存、规范与视觉台账同步，未发布。
- 本轮快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格下拉-20260909-151609`。


## 全局日期选择器统一

- 已新增 `shared/scripts/global-date-picker.js` / `shared/styles/global-date-picker.css`：筛选栏和原生弹窗共用渲染器，弹窗内真实 Ant 面板使用同一视觉规范；保留原选择条、字段事件和校验。清除复用输入条尾部图标。
- 组件目录新增“日期选择器”及同源单日期/日期范围预览；课程管理新增“更多筛选 → 创建日期”，含当天和单边范围，重置清空。资源同步根入口、直接源弹窗预览和频道注册表。
- 实际增量包括全局日期源、`global-filter-bar`、`modal-controls`、`learning-app.js`、组件目录及依赖引用；不代表其他任务的未提交变更属于本任务。
- 导航保护、弹窗目录、表单/实际源、筛选栏、课程管理与V1.1回归通过；Chromium验证真实file目录、原生dialog、原客户Ant日期组件、完整学习file入口日期操作，以及480/800/1100/1440px面板边界。完整学习入口原WebGL跨域图像报错保留，未纳入日期修改。
- 规范：[[../../docs/date-picker-spec]]；视觉参数：`design-changes/shared-date-picker.json`。本地完成，未提交、未发布。


## 表格无外框与统一字号

- 已完成：表格外圈无边框；表头高度56px、上下内边距16px；表头、正文、状态标签、时间、行操作和分页文字统一14px。当前页悬停保留绿色文字与边框，仅背景变为#edf6f3。
- 验证：Chromium148组件目录与课程控制器全部场景通过；完整学习中心入口实测56px表头、14px文字、无外框和独立hover，正文/表头衔接且分页底边976px（1000px视口）。导航保护检查通过。修正浏览器测试外点目标为当前可见组件标题；未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格视觉-20260909-153354`。


## 日期选择器改用项目配色

- 用户指出上一轮沿用Ant蓝色；已在 `global-date-picker.css` 接回项目品牌/交互/文字/边框色，覆盖自绘与真实Ant日期面板，保留结构及选择条。所有样式引用版本同步；视觉台账合并更新，保留首次基准。
- Chromium核验目录的今天/选中/当天描边、原Ant Today及学习file日期操作通过；导航和弹窗目录检查通过。本地未发布。

## 课程管理追加示例数据

- 范围：仅扩充学习中心本地 Mock，追加24门课程，覆盖发布状态、学习群组、必修属性和课节数；不改表格、筛选器和分页器样式，不连接测试环境。
- 已在 `features/learning-center/learning-data.js` 使用稳定ID与一次性批次标记追加24门（12上架、6草稿、6下架），全新本地数据共32门；原数据数量不同则在其基础上追加。保留已有课程编辑及学习历史，避免重复追加和已删除草稿复活；复用本地封面、媒体占位和PDF逻辑，不生成虚假操作日志。
- `shared/config/channels.js` 更新数据脚本缓存标识；`scripts/test-learning-mock-batch.cjs` 覆盖新数据、旧数据迁移、ID冲突、刷新及删除安全。迁移、课程管理及V1.1 DOM回归通过，未执行本轮浏览器视觉验收；本地未部署。

## 全局表格配色调整

- 已完成：共享表格正文悬停改淡灰，表头使用指定底色和项目主文字色；通栏底色、固定列与组件预览同步。参数合并于 `design-changes/global-table.json`。
- Chromium148已验证完整课程管理入口的表头、通栏与固定列悬停计算颜色；组件浏览器回归与导航保护通过。本地未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/全局表格配色-20260909-160113`。

## 表格悬停继续减淡

- 已完成：正文行悬停进一步减淡，参数已合并到 `design-changes/global-table.json`；Chromium148核验整行及固定列悬停计算颜色通过，导航保护通过，本地未发布。修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/表格悬停减淡-20260909-162034`。

## 表格内标签组件

- 已完成：共享 `global-table.js/css` 提供表格专用标签，课程管理状态/精选与目录示例共用；尺寸和文字排版参考组织架构，颜色保留课程管理。卡片、详情和组织架构未改。接口见[[../../docs/table-spec#表格内标签]]，视觉参数合并于 `design-changes/global-table.json`。
- Chromium148验证标签尺寸/配色、未知tone回退与文本转义，表格全部场景及课程控制器浏览器回归通过；课程管理/V1.1 DOM回归与导航保护通过。源码和缓存已同步，本地未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/表格标签组件-20260909-162351`。

## 表格标签实际规格纠正

- 原因：上一轮误读未加载的组织架构旧快照，实际 `config-center-content.css` 覆盖为紧凑标签。已按实际规格修正共享表格标签，颜色不变，普通正文保留14px；参数合并于 `design-changes/global-table.json`。
- Chromium148实测组织架构最终计算样式并截图；修正后组件尺寸、原配色与全表格/课程控制器回归通过，导航保护通过。本地未发布。
- 修改前快照：`/Users/hhp/Documents/GAIP项目集/本地回退备份/表格标签实际规格纠正-20260909-163006`。

## 表格标签盘点与五色建议

- 仅整理审阅报告，业务样式未改：`docs/table-tag-audit.html/json` 收录8组实现来源、32套已核实文字/底色组合及逐项五色映射建议。11个主入口与相关二级表格已扫描；保单入口目标等待超时及深层状态覆盖限制在报告中注明。
- 当前值依据Chromium148最终计算样式，排除非表格标签与未用预留色；建议参数尚未写入视觉变更台账或业务组件。
- 报告验证：71项映射条目、来源/文字筛选与1440/800px页面边界检查通过，无脚本错误。

## 保存表格标签五色映射方案

- 按用户要求保存本轮67个标签的完整分类与颜色对应关系，作为可继续调整的待定方案；保留v1快照，不应用到业务样式。
- 已保存 `docs/table-tag-color-mapping-v1.md`，并在表格规范、知识索引和项目状态添加入口。核对67个文案无遗漏、无重复，配色归属与本轮对照表一致。
