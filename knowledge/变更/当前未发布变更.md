---
type: current-changes
updated: 2026-09-02
---

# 当前未发布变更

## 组织架构批量导入成员弹窗流程

- 状态：本地完成，尚未发布。
- 目标：保留原组织架构顶部单行结构与全局“批量导入成员”入口；依据产品文档将原新页面流程改为同一弹窗内的“选择导入范围 → 校验并确认 → 导入结果”三步流程，固定标题和底部操作区，并以内容最多的校验预览步骤统一三步弹窗高度。
- 业务范围：入口属于组织架构全局；目标范围单选渠道与组织节点，模板固定字段为“域账号、用户姓名、香港、新加坡、美国、大陆、百慕大、转介绍人、管理员”，单次最多 100 人。上传后允许部分成功，预览需逐行显示校验结果与失败原因，并支持返回重新选择、下载失败明细、确认导入和继续导入。导入成功成员写入当前会话 Mock；刷新页面后重置。
- 设计边界：不新增独立页面，不修改组织架构独立操作日志和“配置中心 → 操作日志”二级频道，不接真实上传、UA 查询或后端导入接口。本地通过真实 `.xlsx` 模板与确定性示例数据完整演示各状态。
- 弹窗实现：桌面宽 `1100px`、统一高 `840px`（同时保留视口安全边距），标题、三步进度和底部操作区固定。弹窗主体不纵向滚动；第二步表格区独立横向与纵向滚动，表头吸顶，数据增加为 18 行以直接演示长表格状态；切换三步时外框尺寸不变化。第一步使用渠道单选下拉，并提供可搜索、可展开收起的目标节点单选树、完整目标路径、模板下载和拖放上传；长组织树仅在树区内滚动。第二步去掉重复标题，将目标节点作为主信息，并把 18/2/16 人数统计与失败处理说明合并为一条弱提示；底部左侧为文字链接“下载失败明细”，右侧依次为描边按钮“上一步”和主按钮。“上一步”仍保留返回确认；“确认导入”按钮把动作文字与人数分层，右侧以 `26×18px / 11px` 的半透明小胶囊展示 `2人`，完整按钮名称仍保留人数。第三步展示导入节点和结果汇总。确认导入仅写入两条有效本地成员，失败行不写入；失败明细可下载为 `.xls`。
- 模板与预览：新增 `features/config-center/assets/批量人员导入模板.xlsx`，包含固定 9 列、100 行数据区、冻结表头、地区/转介绍人/管理员下拉验证和独立填写说明；弹窗提供“使用示例文件预览完整流程”，用于无需真实接口的设计验收。
- 实现：六大渠道 Tab、搜索成员、批量导入成员和组织架构独立操作日志仍处于原顶部单行，整行由 `72px` 收至 `64px`。批量导入位于右侧全局操作组、紧邻操作日志之前，复用操作日志的文字按钮结构与样式；两个入口统一为 `14px` 文字、品牌绿图标和 `40px` 点击高度。默认文字为 `#2F3640`、图标为 `#025B52`，hover/键盘聚焦时文字转为 `#025B52`，按下时文字与图标统一为 `#01473F`。顶部批量导入入口与弹窗内上传按钮现共用用户提供的 `bulk-import-upload.svg`，不再克隆并旋转页面下载图标；顶部按 `16px` 画布显示并保留品牌绿状态色。搜索继续以弹性宽度吸收可用空间，不为 Tab 创建滚动条。
- 频道操作：当前渠道工具栏的设置管理员、导出Excel、添加成员保留原位置，统一为 `40px` 高、`14px` 文字和 `16px` 图标；设置管理员、导出Excel宽 `112px`，添加成员宽 `120px`。三枚图标的容器、Ant 包裹层和 SVG 本体均固定为 `16px` 并使用零行高居中，避免只改字号后仍受各自原始属性影响，点击逻辑不变。
- 决策：此前尝试过独立全局工具行、独立 `56px` Tab 行和缩小频道操作按钮的三层结构；用户对比后要求回到试排前的单行基准版，只在该基础上增加导入入口，因此整套三层试排已撤回。
- 资源与验证：配置中心外壳 CSS 为 `20260902-8`、主体 CSS 为 `20260902-21`、交互脚本为 `20260902-24`，全部 16 个根入口注册表缓存为 `20260902-34`。配置中心 DOM/交互回归覆盖原顶部单行结构、批量导入真实入口、三步状态、模板约束、18 行长表格、返回确认、2 条部分成功、继续导入、固定头尾、表格内部双向滚动、窄屏适配和焦点恢复；弹窗已以 `config-bulk-import-members` 自登记并进入自动预览目录。

### 当前试排：导入位置选择

- 目标：将第一步改为“渠道下拉单选 → 目标节点树”的连续选择体验，使渠道数量增加时不挤压弹窗内容，并允许用户在节点较多时搜索定位。
- 交互：渠道使用独立单选下拉；目标节点继续使用项目组织数据生成可展开、可收起、可搜索的单选树，默认仅展开到一级。切换渠道时清空原节点并回到新渠道根节点，完整导入路径持续显示在范围卡片右上。
- 当前排版目标：参考用户提供的第一步布局减少重复说明；弹窗标题区只保留标题与关闭按钮，范围卡片右上展示当前目标，上传卡片右上放示例流程入口，模板下载与格式限制合并为卡片底部提示条。渠道、节点树、上传、模板下载和示例流程功能均保留。
- 上传区细化：按最新参考图恢复紧凑的“上传文件”描边按钮，上传图标与文字位于按钮内，`文件大小不得超过10MB` 在同一行显示；选择文件后在下一行以回形针图标加文件名展示附件。模板条继续使用左侧 `32px` 绿色文件图标、右侧上下两行的模板名称与格式说明。
- 图标资产：下载导入模板使用用户提供的 `/Users/hhp/Downloads/编组 5.svg`，上传文件按钮使用用户提供的 `/Users/hhp/Downloads/ICON/通用/上传.svg`；两枚原始 SVG 均复制到配置中心资源目录，供正式页面与弹窗组件预览共同加载，不再用导出 Excel 图标旋转代替上传图标。
- 边界：只调整批量导入第一步的目标范围选择，不修改上传、校验、导入结果和真实数据规则；树区域自身可滚动，三步弹窗外框及主体继续保持无纵向滚动。
- 实现与验证：改动位于 `features/config-center/config-center.js`、`features/config-center/config-center-content.css` 和 `scripts/test-config-center.cjs`；回归已覆盖渠道下拉选项/切换、一级默认收起、树展开、节点选择、完整路径、搜索结果和清除。弹窗自动索引检查、真实源预览回归、配置中心 DOM 回归、Hash 导航验证及 `git diff --check` 均通过；现有应用内浏览器因 `file://` 自动控制策略无法代为刷新，最终视觉需在既有本地页刷新后确认。

## 全局组件目录接入弹窗预览

- 状态：本地完成，尚未发布。
- 目标：在 `全局组件/index.html` 中增加“真实弹窗预览”正式入口，动态展示当前可预览、待接入与不展示数量；点击后打开现有真实源预览页。
- 边界：目录首页只加载轻量登记数据和入口卡片，不内嵌 30 个弹窗 iframe，不复制任何弹窗 DOM、CSS 或打开逻辑。
- 实现：`components-registry.js` 新增 `modal-catalog` 组件项；`components-preview.js` 直接读取 `__GAIP_MODAL_SOURCE_CATALOG__` 显示 30/0/17 数量并输出独立页面链接；`components-preview.css` 只增加目录卡片和数量栏视觉。`index.html` 按注册器、基线、自动索引、组件目录的顺序加载，数量不在组件卡片中另写一份。
- 验证：脚本语法、索引时效、目录加载顺序、组件登记、真实链接及“首页不得出现 iframe”断言通过；弹窗总目录仍为 47/30/0/17。
- 关联：沿用 [[../决策/ADR-004-弹窗源自登记与自动索引]]，数量以同一运行时弹窗目录为准。

## 弹窗源自登记与自动索引

- 状态：本地完成，尚未发布。
- 目标：其他任务在真实源中新增弹窗后，不再同时维护预览页的第二套打开代码；通过源登记块生成索引并自动进入弹窗预览。
- 实现：新增 `shared/scripts/modal-registry.js` 和 `scripts/generate-modal-catalog.cjs`；预览加载既有基线与自动生成索引，并按登记的 `invoke` 通用打开。`features/config-center/config-center.js` 中的组织架构独立操作日志已增加首个 `@gaip-modal` 登记和公开 `openOrganizationLog()` 入口。
- 规则：`AGENTS.md` 已加入跨任务强制契约；新增本地弹窗必须在源 JS 登记并运行生成器，抽屉和明确排除项继续不展示。架构决定见 [[../决策/ADR-004-弹窗源自登记与自动索引]]。
- 验证：自动索引 `--check`、注册表 47/30/0/17 数量、资源、重复 ID、预览通用入口和 JS 语法通过；组织架构日志的真实公开入口已通过隔离 DOM 回归，确认打开 7 列、10 行首屏且没有渲染频道背景页。共享操作日志、可展开主导航与本地 Hash 导航回归通过。浏览器自动化受本机 `file://` 安全策略限制，最终像素观感继续由现有本地预览刷新后人工确认。

## 组织日志“导出”操作状态

- 状态：本地完成，尚未发布。
- 目标：只在组织架构独立日志中增加“导出成员名单”展示状态及对应 Mock，呈现所选节点、导出总人数、空的变更前值和独立操作标签。
- 范围：组织架构右上角独立日志弹窗的单条 Mock、操作标签视觉和回归；不实现真实多节点导出、Excel 新字段、三个新增渠道，也不修改“配置中心 → 操作日志”二级频道。
- 实现：独立日志首行新增“导出成员名单”记录，渠道为 Glory品牌顾问，节点为“Glory品牌顾问 / 销售中心”，来源为“成员导出”，变更前为 `—`，变更后说明包含全部子节点且共导出 36 条。“导出成员名单”复用操作类型列其他标签的同一颜色和 DOM 类，不设置独立色。
- 资源与验证：配置中心主体 CSS 当前为 `20260902-21`、交互脚本当前为 `20260902-24`，全部 16 个正式根入口注册表缓存当前为 `20260902-34`。`scripts/test-config-center.cjs` 覆盖导出记录字段、节点路径、总人数说明及标签类复用。

### 当前修正

- 目标：恢复长表格的独立滚动和固定分页，扩大弹窗桌面宽度；“导出成员名单”标签改为复用操作类型列的统一颜色。
- 边界：只调整组织架构独立日志弹窗，不修改二级共享日志。
- 结果：弹窗宽度改为 `min(100vw - 48px, 1360px)`，高度上限为 `800px`；标题固定，中间表格区独立横向/纵向滚动且表头吸顶，分页作为固定底部区域不参与表格滚动。导出标签已与其他操作类型统一。
- 结果：吸顶表头改为不透明 `#F5F5F5` 底色、独立边框模型和内嵌底部分隔线；正文纵向滚动时不再透出到表头区域，原吸顶、双向滚动和固定分页行为保持不变。

## 组织操作日志需求复核与表格对齐

- 状态：本地完成，尚未发布。
- 目标：依据“组织架构支持多节点导出与新增渠道”产品文档，梳理组织架构独立日志的新增设计范围；修正当前独立日志表格列宽、行内基线、多行内容与表头对齐。
- 范围：仅组织架构右上角独立日志弹窗的表格布局、对应回归和需求结论；不修改“配置中心 → 操作日志”二级频道，不提前实现多节点导出、新增渠道或真实日志写入。
- 文档结论：日志业务只新增一类“导出成员名单”记录，节点路径为当前所选节点完整路径，“变更后”说明包含全部直接/间接子节点的合计导出人数；成员为空、角色语义为成员名单、变更前为空。文档截图采用“操作时间、操作人、组织节点、成员、角色、操作、变更前、变更后”8 列，和当前线上/本地独立日志的 7 列信息架构不同，实施前必须由产品与设计确认最终列模型。文档同时要求导出表首列增加节点路径，以及新增 ARK HK、Olive、其他三个大渠道。设计侧需补完整 8 列方案或明确继续 7 列的字段映射、导出日志长文本行、导出进行/成功/失败/空结果反馈，以及九个渠道下顶部 Tab 与搜索框的窄宽适配；Excel 字段顺序和后端记录口径属于产品/研发规则，不需要另建页面。
- 实现：独立日志表格增加 `colgroup`，固定七列为 `150 / 120 / 112 / 128 / 220 / 96 / 280px`，总最小宽度 `1106px`；表头和正文统一左对齐，正文改为垂直居中与 `22px` 行高，节点路径和变更内容的层级前缀固定占位。表格字段模型仍保持七列，后续经用户明确要求仅追加导出展示状态，记录见上方独立条目。
- 资源与验证：配置中心主体 CSS 当前为 `20260902-21`、交互脚本当前为 `20260902-24`，全部 16 个正式根入口注册表缓存当前为 `20260902-34`。`scripts/test-config-center.cjs` 已覆盖列宽、表格最小宽度、正文垂直对齐、统一行高及吸顶表头的不透明底色与分隔线。

## 组织架构独立操作日志恢复

- 状态：本地完成，尚未发布。
- 目标：恢复组织架构页面右上角独立的“操作日志”弹窗；它只展示组织架构操作记录，不再调用“配置中心 → 操作日志”二级频道的共享日志。
- 范围：组织架构专属日志弹窗、专属本地 Mock、生命周期、回归测试与模块边界文档；二级频道现有公告/资讯日志的数据、筛选、导出和控制器保持不变。
- 实现：`features/config-center/config-center.js` 为组织页按钮建立专属 dialog、24 条基础组织操作 Mock、1 条导出状态 Mock、7 列表格和每页 10 条的页码状态，字段按线上组织架构日志接口语义设置；`features/config-center/config-center-content.css` 复用原站日志类名并补齐原生 dialog、表格滚动、操作类型标签和键盘焦点样式。组织页按钮不再调用 `__GAIP_OPERATION_LOG__.show()`；二级页仍只调用 `__GAIP_OPERATION_LOG__.mount(host)`，两套生命周期互不引用。
- 资源：配置中心主体 CSS 缓存当前为 `20260902-21`、交互脚本为 `20260902-24`，全部 16 个正式根入口的频道注册表缓存当前为 `20260902-34`。
- 验证：`scripts/test-config-center.cjs` 断言组织按钮不会调用共享控制器、专属弹窗字段/记录/分页/关闭正确，切到二级页才挂载共享控制器；`scripts/test-operation-log.cjs` 证明二级频道原筛选、XLSX 和生命周期未被修改。配置中心、共享日志、可展开主导航、AI Agent、弹窗预览、本地导航、JS 语法、16 个根入口缓存和差异格式检查通过。应用内浏览器安全策略不允许自动刷新 `file://`，原生顶层遮罩与最终视觉需在既有本地预览刷新后人工确认。

## 配置中心成员身份列

- 状态：本地完成，尚未发布。
- 目标：在组织架构成员表中新增“身份”列，以紧凑标签展示人管、佣金、线索管理、线索跟进；同一成员可展示多个标签，无身份时使用与其他表格字段一致的半角 `-`。本地初始 Mock 需在首屏覆盖四类身份，便于直接检查全部标签样式。
- 范围：仅配置中心组织架构表格、角色状态渲染、对应样式与回归测试；不修改顶部、主导航、AI Agent 与只读 `web/` 分包。
- 实现：`features/config-center/source-markup.js` 在姓名后增加身份表头与列宽；`config-center.js` 直接读取管理员弹窗和成员编辑共用的角色状态，按固定顺序生成身份标签，同时保留姓名列原“管理员”节点、文字与样式；`config-center-content.css` 提供四类紧凑标签的区分色与多标签换行。首屏第一、二名 Mock 成员分别覆盖人管/佣金/线索管理和人管/线索跟进，四类样式直接可见且仍遵守角色互斥规则。线索管理与线索跟进互斥，单人合法上限为 3 个身份标签；未来规则扩展时容器可自动换行。
- 资源：配置中心主体 CSS 当前为 `20260902-21`，源模板为 `20260902-1`，交互脚本当前为 `20260902-24`，全部 16 个正式根入口的频道注册表缓存当前为 `20260902-34`。
- 验证：`scripts/test-config-center.cjs` 覆盖表头、首屏四类 Mock 标签、半角空值、原管理员标签 DOM 逐节点复用、多身份顺序和两条角色保存路径；配置中心、操作日志、可展开主导航、AI Agent、弹窗预览、本地导航、JS 语法、缓存版本与差异格式回归通过。应用内浏览器安全策略禁止自动检查 `file://` 页面，因此最终像素观感需在现有本地预览刷新后人工确认。

## 弹窗预览触发错误修复

- 状态：本地完成，尚未发布。
- 范围：`全局组件/弹窗预览.html`、`全局组件/弹窗源登记.js`、`shared/scripts/popup-preview-bridge.js`、`shared/scripts/local-preview.js` 与根目录入口缓存版本。
- 修复：正式频道预览调整为 3 个并发，真实入口等待延长到 45 秒；弹窗打开后直接删除频道页面 DOM，只保留弹窗分支、原遮罩和 Portal，不再以覆盖层隐藏；客户与产品流程只点击已加载的真实内容卡片，跳过复用类名的骨架屏；AI Agent 重命名改为从工作台真实 Agent 入口打开；线索终态直接使用列表“转化”入口；产品专家按真实标题“业务线对接专家”验收；入职完成从当前真实进度逐步推进到“完成”，不再依赖按钮字距或固定初始进度。
- 边界：仍复用各频道自身弹窗及样式，不在预览页复制第二套弹窗代码；不修改只读 `web/` 分包。

### 弹窗预览按需加载与空宿主收尾

- 状态：本地完成，尚未发布。
- 目标：补完“预览只展示弹窗、不批量加载遮罩后的完整频道页面”的性能目标；首次打开预览页不得立即创建全部 iframe，配置中心弹窗预览不得先渲染组织架构整页 DOM。
- 范围：`全局组件/弹窗预览.html`、配置中心真实弹窗控制器、弹窗预览与配置中心回归测试，以及相关公共模块说明；继续调用同一真实弹窗源，不复制 DOM/CSS，不修改只读 `web/` 分包。
- 实现：父预览页先生成 30 个轻量卡片和等高占位，`IntersectionObserver` 仅在卡片进入视口前 `80px` 或用户点击“立即加载”时动态创建 iframe；15 个正式频道项仍使用最多 3 个并发的真实路由触发队列，弹窗打开后由桥接脚本删除频道背景 DOM。配置中心六类直接源弹窗共用空的 `data-gaip-dialog-preview-host`，成员、部门、组织日志和批量导入均不再调用 `renderOrganization()`；真实弹窗 CSS、工厂、状态与保存逻辑继续复用原控制器。
- 资源：配置中心交互脚本缓存提升为 `20260902-24`，正式根入口频道登记缓存提升为 `20260902-34`。
- 验证：自动索引时效和 47/30/0/17 目录回归通过；配置中心完整 DOM 回归确认六类弹窗关闭后空宿主无任何背景子节点；可展开主导航、Hash 无刷新导航、共享操作日志与 AI 入口回归通过。临时本机浏览器实测首次仅加载首屏第一个弹窗，滚动后的配置中心 iframe 只包含弹窗，正式频道“编辑对话名称”打开后工作台、侧栏和顶栏节点均被删除；临时服务和测试标签页已关闭。

## 登录页视频英雄图测试入口

- 状态：本地保留，用户明确不发布。
- 文件：`index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/`。
- 边界：后续只有在用户明确要求时才单独提交；不得为满足入口数量或发布检查而把它混入正式站点。

## AI Agent 试验素材

- 状态：本地候选，未被正式页面引用，不发布。
- 文件：`AI Agent/素材/creative-8.2ab723e3.png`、`creative-8.70d73cd4.webp`、`creative-9.1a6c0a5a.webp`、`creative-9.e00adc24.png`、`备份.mp4`。

## 财富值概览候选图

- 状态：本地候选，未被正式页面引用，不发布。
- 正式页面已发布并只引用 `assets/wealth-center/wealth-overview-user-wide.jpg`；本次发布记录见 [[2026-09-02-财富值概览背景替换]]。
- 未发布文件：
  - `assets/wealth-center/wealth-overview-envelope-trial-20260901.png`
  - `assets/wealth-center/wealth-overview-generated-20260901.jpg`
  - `assets/wealth-center/wealth-overview-lovart-clean-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-refined-walnut-glass-v5-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-style-v2-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-glass-v4-20260901.png`
  - `assets/wealth-center/wealth-overview-mkv-walnut-v3-20260901.png`

## 资讯中心参考分包

- 状态：本地参考文件，不属于正式页面依赖，不发布。
- 文件：`web/p__news__index.f64a0a2e.async.js`。
- 边界：`web/` 默认只读；除非后续有明确、可验证的构建产物同步需求，不得加入提交。

## 新任务追加格式

复制 [[../模板/变更记录模板|变更记录模板]] 的字段，在开始修改前新增一个二级标题。不要把多个不相关任务混在同一条目。
