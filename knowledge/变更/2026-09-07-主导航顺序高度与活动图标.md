---
type: release
date: 2026-09-07
status: 已发布
commit: 67d7c5e
---

# 2026-09-07 主导航顺序、高度与活动中心图标

## 发布结果

- 功能提交：`67d7c5e`（`Update sidebar order height and activity icon`）。
- 完整提交：`67d7c5e2bd34fb188c536bf168f37dfc62bb9333`。
- 分支：`main`，已推送至 GitHub `origin/main`。
- 本次提交共 27 个文件、236 行新增、66 行删除。

## 更新内容

- 主导航调整为：工作台总览 → 线索中心 → 客户中心360 → 方案中心 → 产品中心 → 保单列表 → 资讯中心 → 活动中心 → 学习中心 → 薄荷入职引导 → 财富值中心 → 配置中心。
- 排序通过 `shared/config/channels.js` 的 `sidebarOrder` 和 `channel-regions.js` 的 CSS `order` 实现，不移动 React/Umi 节点，不改变频道资源顺序、点击事件或 Hash 导航。
- 一级叶子项、可展开父项和二级项的高度/行高统一为 `36px`；排序外的字体、颜色、图标列、宽度和导航逻辑保持不变。
- 活动中心改用用户提供的独立 SVG 图标，资讯中心图标保持不变；15 个正式根入口同步共享资源缓存版本。
- 新增 `scripts/test-sidebar-order.cjs` 并接入 `npm run test:navigation`。

## 验证

- `npm test` 退出码 0：导航静态检查、弹窗目录、菜单排序与开合、配置中心、公告管理、操作日志及 AI Agent DOM 套件均通过。
- 活动图标 SVG 通过 XML 解析，保存资产与内嵌图形一致，`18×18` viewBox、资讯中心映射和正式入口版本已核对。
- `git diff --cached --check` 通过。
- 未把 JSDOM 当作浏览器视觉证明：实际菜单间距/高度、原生箭头键视觉顺序、主文档无刷新、视频/WebGL 仍需现场浏览器验证。

## 排除项

- `index-login-video-test.html`、`features/login-video-test/`、`assets/login-hero-test/` 继续留在本地，未进入提交。
- AI Agent 试验素材、财富值候选图和 `web/` 资讯参考分包继续留在本地，未进入提交。

## 关联文档

- [[../公共模块/主导航与Hash路由|主导航与 Hash 路由]]
- [[../公共模块/全局框架与样式|全局框架与样式]]
- [[../页面/活动中心|活动中心]]
- [[当前未发布变更]]
