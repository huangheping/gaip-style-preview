---
type: page
key: activity
route: /activity
entry: 活动中心.html
layout: dashboard
---

# 活动中心

- 入口：`活动中心.html`
- Hash：`#/activity`
- 页面类型：看板型
- 导航图标：共享注册表使用 `activity-center`，原图为 `shared/assets/nav-activity.svg`（用户提供）；`shared/styles/learning-nav.css` 内嵌原 SVG 并沿用菜单颜色，不再与资讯中心共用标识。
- 数据：`features/channel-data/mock-data.js`
- 报名弹窗与活动增强资源：`features/activity/activity-sync.css`、`activity-sync.js`统一登记到`shared/config/channels.js`，由公共频道加载器提供；根入口不再重复直引。运行时重复加载保留同一控制器。
- 07报名弹窗：外框、内层form与正文共同形成可收缩布局，标题/底部操作保持在正文滚动区外，内容少时保持自然高度。来源与预览直接共用；见[[../变更/当前未发布变更#07与08正文滚动及33与34重复内边距]]。
- 关联：[[../公共模块/本地Mock系统|本地 Mock 系统]]、[[../公共模块/全局框架与样式|全局框架与样式]]
- 规范：[[docs/channel-home-standard|频道首页规范]]

## 修改边界

- 轮播箭头已迁入共享 `global-carousel-controls.js/css`，活动通过原 Slick 指示器适配；原指示器 DOM、白色外观、自动轮播及报名逻辑保留。按组数隐藏无效箭头，频道卸载销毁控件；学习中心与目录调用同一工厂，见 `docs/carousel-controls-spec.md`。

07备注通过真实工厂的`resizable-textarea`声明接入共享手柄，不新增计数；保留字段限制、校验和提交。来源与预览复用同一实现，见[[../变更/当前未发布变更#07无计数备注接入共享手柄]]。

频道专属改版放入 `features/activity/` 并登记资源；共享导航、顶栏或 AI 入口不在活动中心代码中重复实现。
