---
type: page
key: customer
route: /customer
entry: 客户中心360.html
layout: master-detail
---

# 客户中心360

- 入口：`客户中心360.html`
- Hash：`#/customer`
- 页面类型：主从分栏型
- 数据：`features/channel-data/mock-data.js`
- 专属补丁：`features/customer/customer-center.css`、`features/customer/customer-center.js`
- 关联：[[../公共模块/主导航与Hash路由|主导航与 Hash 路由]]、[[../公共模块/本地Mock系统|本地 Mock 系统]]
- 规范：[[docs/channel-home-standard|频道首页规范]]

## 修改边界

频道专属改版使用 `features/customer/` 并在 `shared/config/channels.js` 登记，不直接改 `web/` 构建产物。根 HTML 只允许同步共享资源版本号。

## 当前专属补丁

- `features/customer/customer-center.js` 将旧构建中客户列表卡片右上角绝对定位的 `bindTag` “已关联保单”移动到 `cTags` 标签行，并套用参考站当前构建里的普通 `cTag` 位置逻辑。
- `features/customer/customer-center.css` 为移动前后的标签提供样式兜底，避免标签悬浮在卡片右上角。
