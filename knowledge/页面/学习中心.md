---
type: page
key: learning
route: /workspace
entry: 学习中心.html
layout: guided-learning
virtual: true
---

# 学习中心

- 最新覆盖（2026-09-10）：学情两类详情不再使用右侧抽屉，改为共享信息展示弹窗。业务与目录通过`openStudyDetail`同源打开，`learning-study-detail`/`learning-course-study-detail`均可真实预览；保留原字段、筛选、表格、权限与主列表状态。关闭/Esc销毁子组件并恢复焦点，下方较早“详情抽屉”描述为迁移前状态。样式由共享modal/position/mask拥有，业务只定义内容与容量；见[[../变更/当前未发布变更#学情详情改为共享信息弹窗]]。

- 两类学情详情统一为对象信息→共享筛选→共享明细表。学习状态选项收进筛选栏，重置同时清空状态；保留原筛选语义。主表/详情长文本两行显示并保留全文title，数值列右对齐、正文顶部对齐、状态标签直接调用共享tag。详情课程/学员信息展示已有对象属性，不增加统计计算或数据持久化。精确参数见`design-changes/learning-center.json`，本轮验证与回退位置见[[../变更/当前未发布变更#两类学情统计与详情展示统一]]。
- 学情统计Tab皮肤与键盘增强已迁入共享`global-tabs.js/css`，和产品中心/组织架构同源；本页CSS只负责返回栏位置与响应式布局。组件库新增真实交互预览，见[[../../docs/tabs-spec]]。
- 维度切换保留返回栏及同一 Tab 实例，避免重建节点中断下划线动画；下方筛选和表格重新挂载，仍重置筛选/分页及内容区滚动。真实控制器独立浏览器验证过渡中间帧与快速反向切换通过。
## 学情管理同源组件接入（2026-09-10）

- 统计维度在返回栏中间使用下划线Tab（配置中心同款视觉），窄屏另行居中；左右/Home/End键切换，重复点击当前Tab保留筛选。组织筛选现为共享`treeSelect`，主页面与课程详情弹窗直接在主输入框搜索组织节点，下拉内不另设搜索框；按稳定ID选择并包含下级，搜索输入本身不筛人员，未确认收起恢复已选组织。共享节点数据`organization-store.js`、渲染器`organization-tree.js`与配置中心主树同源。
- 已确认的Mock迁移只补`orgNodeId/scopeNodeId`并保留`legacyOrg/legacyScope`：香港顾问一组→Glory品牌顾问/测试部门/测试1部/华东业务组，二组→同父级/机构服务组，香港节点本人→同父级/测试-测试1；香港学情管理员范围→Glory品牌顾问/测试部门/测试1部。新加坡→薄荷经纪人/测试部门/测试2部，美国→外部机构渠道/测试部门/测试2部。仅已知旧Mock路径迁移一次，未知自定义路径不猜测；账号ID、课程、日志和学习记录不重建。配置节点改名通过事件更新展示路径，关联及权限按ID保持；节点数据仍延续配置中心当前会话内存模式，刷新恢复Mock名称。
- 学员/课程两种维度的原统计口径、字段与默认排序保持；`renderStats/updateStatsResults`挂载全局筛选栏及表格，与课程管理共用返回栏、白底和顶部分割线，操作日志/导出位于栏右侧。面包屑标记学情管理，无重复独立标题。
- 筛选按搜索/组织/群组/课程状态/必修排列，后两项仅课程维度展示。群组保留原单选（筛选学员范围，不是课程配置群组OR匹配）；必修保持全部/必修/非必修，不能替换二态开关丢失“仅非必修”。即时输入只更新表格，维度切换/退出销毁旧实例。导出读取最新输入并导出全部结果，不限当前页。
- 详情仍为抽屉，原状态按钮、各字段、人员/课程筛选与父页权限范围不变；筛选和表格换为共享实例，关闭销毁并恢复焦点。`openLearningLog`为两种日志的共同信息弹窗工厂，课程保持八字段且无搜索，学情保留原六字段与搜索，仅显示导出学情，非超级管理员只看本人日志。关闭不改变底页维度/筛选/分页；长详情复用四行折叠并顶部对齐。
- 新增`learning-study-log`真实源登记（46）；与业务同入口`openStudyLog`。课程日志原编号保留。学情本轮不新增或覆盖Mock；自动测试的分页/长日志样例仅在隔离测试页，不写用户数据。
- 测试`scripts/test-learning-study-browser.cjs`覆盖共享控件、字段、空态、详情、日志关闭保留、权限、导出与多宽度。独立截图已核验；完整file入口被浏览器安全策略阻止，保留现场验收缺口。

## 课程编辑页面表单与滚动容器

- 文件名链接使用本次成功选择文件的Blob，在新标签页交给浏览器打开；不使用示例预览、不写学习session。`localFiles`仅内存保存原File和Blob URL；元数据`localFileId`用于本页重进/保存后匹配，刷新后找不到引用则显示重新选择说明，不提供失效链接。失败/取消保留旧文件；无引用URL由`pruneLocalFiles`回收，保留已保存课程当前页可用的引用。首次保存同时同步底栏class与内容，修复按钮靠左；群组提示说明可学习范围，移除重复必填说明。
- 上传区显示文件信息、状态、操作三层，长文件名单行省略/完整title；替换失败只出现一次旧文件名并保留链接，无“当前文件/原文件已保留”重复段落与常驻点击说明。上传成功提示待保存；示例及模拟失败收进“演示选项”，正常上传更换操作保持可见。按钮SVG来自用户上传.svg/更换.svg，原路径内联并用currentColor；视觉参数同源记录。

- `lessonUploads`仅为编辑器临时状态，按lessonId/kind隔离内容与讲义；File仅暂留内存供成功后文件名打开，不写持久存储。文件选择通过格式/大小校验后模拟进度，默认成功；上传中的“模拟失败”用于本地验收。失败可重试，格式错误只能重新选择。替换成功前保留旧文件，成功后仍需显式保存；全局保存/单课节保存在有上传时阻止。取消/类型切换/删除/离开/销毁清理定时器；“使用示例”仍直接填充，不触发模拟流程。更换按钮黑底白字，其他按钮及封面流程不变。测试`test-learning-upload-browser.cjs`。

- 新增课节`type:null`，三种类型默认均不选；选择前内容区显示引导、不提供类型不明的上传，保存校验定位课节类型。编辑已有课节类型不改，发布锁定仍有效。封面仅补充“建议使用16:9”，不增加强制比例校验。底栏草稿说明已对齐薄荷入职引导：左侧垂直居中、按钮右侧，学习设置三列说明对齐。
- 本轮完整file入口已验证新建页面、草稿新窗口及模拟播放入口；独立Chromium覆盖新建/预览/统计和编辑交互，见`test-learning-create-flow-browser.cjs`及原编辑测试。旧轮次的现场缺口描述仅为当时记录，未宣称真实媒体、线上接口或完整全站已验收。

- 标题区不再重复放课程保存；课程级保存只在底栏。首次新建只渲染基本信息，“非必修/必修”用null表示未选，保存要求显式布尔值；编辑保留已有值。新建有“保存草稿并返回”和“保存并添加课节”，后者先保存零课节草稿，再展开未保存的第一课节并只滚动编辑区。已保存课程继续独立保存课节/排序。草稿配置页“预览课程”位于返回栏右侧，首次保存后同步出现；仍只预览已保存内容，不保存未提交输入。必修、精选的两段辅助说明已移除，字段与规则保留。

- `availableCourses`一律仅返回上架课程。草稿预览从管理列表及配置页调用同一`previewCourse`，在当前入口文件的新窗口打开`gaip-preview`参数，保留file存储上下文、使用noopener。入口校验课程管理权限与草稿状态，沿用只读预览session；存储失败不打开预览。该边界仅本地Mock，不是生产鉴权。
- `publishLesson`在任何状态/日志写入前禁止已上架课程最后一个课节下架；页面用共享“无法下架课节”提示，没有确认下架回调。44号源登记保留ID，改为中性限制提示。
- 学情表格、详情、导出统一用`studyHistory`过滤下架课节的时长、首次/最近学习；完成度用`summary`当前上架课节。未达到当前完课条件时不展示历史完课日期/计数；底层`history`与records/completions仍完整保留，重新上架后恢复参与。课程级首次/最近完成仍为历史时间，不凭下架操作伪造课节时间。本地没有data ATM接口，真实同步排除需后续对接。

- `lc-editor`是唯一编辑内容滚动层，header 与 footer 为其兄弟；底栏不能放回滚动层内用负 margin 模拟固定。标题/提示/卡片共用居中内容列，footer 按最新反馈满宽且按钮靠内容区最右（不再跟随卡片居中轴）；新增和编辑共用 renderEditor。
- 保存错误通过领域层附带的 field/lessonId 定位（不改变校验规则），showEditorError 调用全局 createFieldFeedback；控件下提示、aria-invalid/aria-describedby 同源。校验定位只滚动 lc-editor，不能统一跳到页面顶部。修正/重绘/离开销毁反馈；通用非字段错误回退为轻量摘要，上传错误也映射到封面/课节文件控件。
- `gaip-page-form`仅接入共享页面表单外观（global-page-form.css）；tokens 来自 global-modal.css，群组继续 mount 全局多选组件。无需给页面加 gaip-modal-controls 或弹窗行为。原有本地输入/单选/多选覆盖已清理；字号、边框、焦点、圆角和控件高度由共享层维护。
- 课节 badge 是编辑器展示映射：尚未存在于已存课程→未保存；published→已上架；offline 且 locked→已下架；其他已保存→未上架。保存/上下架领域值不变，locked 是既有发布历史标志。
- 回归入口：test-learning-editor-layout-browser.cjs 检查布局及共享外观，另两个 editor browser 测试保留上传、保存、拖拽、删除及滚动保护。全量入口仍需现场验收。

## V1.2 直播 Banner（本地未发布）

- 直播切换采用共享 `global-carousel-controls.js/css` 的活动同款圆形箭头与底部短条指示点，替换“上一组/页码/下一组”。手动首尾循环，指示点直达对应组；单组不挂载控件，单张仍半宽靠左，重绘保留控件焦点。无自动翻组，不改数据或课程排序。组件目录有真实预览，参数见 `design-changes/shared-carousel-controls.json`。

- 最新（2026-09-17）：首页采用用户确认的横卡，不恢复此前标题右侧轮播。单场半宽靠左、双场并排、超过两场手动按组切换；无可见直播时整区及“课程学习”分区标题隐藏。直播区在课程同一滚动层，桌面头部不增高；管理按钮按权限可见，窄屏换行。保留默认超级管理员、隐藏 Mock 栏及原课程数据。
- `mountCards` 通过 `presentation` 读取独立 `publicTitle/summary/liveStartAt`，不公开后台名称，不以广告上架时间推断开播。倒计时到点原位替换“直播中”，无开播时间显示待定；实际上下架仍由原 Banner 领域规则控制。挂载时建立局部定时刷新，退出清理；图片沿用 750:320 完整显示。三条已知示例用一次性补字段批次，不恢复删除或覆盖已填写字段。视觉台账见 `design-changes/learning-live.json`。
- `learning-live-data.js` 独立保存 Banner 和日志，`learning-live.js` 提供首页轮播与直播管理；由频道登记统一加载，`learning-app.js` 管理入口及定时器/弹层销毁。
- 草稿不会自动上架，需确认定时或立即上架；下架后只读。直播列表使用共享筛选/表格（不分页），编辑和确认使用共享弹窗真实源。用户追加要求后以 `live-examples-20260916` 一次性增量补入 8 条直播示例及 15 条模拟日志，不覆盖已有记录、不恢复删除项；图片复用本地课程素材占位。
- 课程管理日志当前升级为九列：新增“操作项目”区分课程和直播 Banner，其余长内容展开、标签、对齐与分页不变；下文八列描述是 V1.1 历史。
- 直播 ID 尚无播放地址映射，本地提示未接入；链接类型新标签页打开。本地定时不等于后台服务。范围、假设及验收见 [V1.2 本地说明](../../docs/learning-center-v12-local.md)，视觉参数见 `design-changes/learning-live.json`。

## 弹窗源登记与业务场景

- `learning-app.js`的`confirmationMessages`与`confirmScene`是课程/课节六种操作确认和未保存离开的同源文案与组件入口；业务传真实回调，预览传无写入回调，不复制DOM/CSS。上架主按钮为“立即上架”，下架为“确认下架”、删除为“删除草稿”，次按钮均“再想想”；下架/删除复用danger语义。未保存仅有“放弃修改并离开”和“保存并离开”，X/Esc仍返回编辑，不放弃修改。
- 当前预览：37课程管理操作日志、38课程上架确认、39学习中心未保存离开确认、40课程下架确认、41删除草稿课程确认、42课节上架确认、43课节下架确认、44最后课节下架限制提示（返回/我知道了，中性无写入）、45删除课节确认。编号由运行时目录计算，不在预览页另写对应逻辑；学情详情抽屉继续excluded。
- 课程日志无搜索，八列按用户图为分类/操作对象/变更字段/变更前/变更后/操作人/操作时间/IP；复用全局表格、长值展开与分页。弹窗宽度用`.gaip-modal.lc-course-log-modal`保护，不被后加载的默认480px覆盖。`mockBatches.course-logs-20260910`增量加入24条独立虚构日志（固定ID去重、模拟人员与TEST-NET IP），不改原历史、课程或学情数据。
- 新增确认场景仍须源`@gaip-modal`登记并运行目录生成器；不能仅增加到文案表就认为已自动收录。`test-learning-confirm-scenes.cjs`比对真实业务触发与预览内容、按钮以及无数据写入。

## 课程管理示例数据批次（2026-09-09）

- 数据模型追加24门本地示例课程（12上架、6草稿、6下架），覆盖群组、必修、精选和1至12课节；全新状态共32门。只用本地封面和模拟媒体。
- `gaip-learning-v11.mockBatches.course-management-20260909` 记录一次性追加；固定ID去重，不覆盖现有编辑，不恢复已删除草稿。原学习记录保留，不伪造操作日志。
- 验证：`node scripts/test-learning-mock-batch.cjs`，课程管理及V1.1 DOM回归；无本轮浏览器视觉验收，不改分页样式。

- 入口：`学习中心.html`
- 路由：复用 `#/workspace`，通过 `gaip-channel=learning` 和页面覆盖标识区分
- 专属实现：`features/learning-center/learning-center.js`（频道生命周期）、`learning-data.js`（V1.1 Mock 领域规则）、`learning-app.js`（页面与交互）；原 `learning-center.css` 保留已确认视觉，`learning-v11.css` 补管理与内容状态。
- 素材：`assets/learning/`
- 导航扩展：`shared/scripts/learning-nav.js`、`shared/styles/learning-nav.css`
- 关联：[[工作台总览]]、[[../公共模块/主导航与Hash路由|主导航与 Hash 路由]]、[[../公共模块/频道资源加载|频道资源加载]]
- QA：[[docs/learning-center-design-qa|学习中心设计 QA]]

## 修改边界

学习中心是虚拟频道，不能简单新增独立 `/learning` Hash 或把它当作普通 Umi 路由。修改前必须同时检查工作台与学习中心的选中态和返回行为。

## 2026-09-09 V1.1 本地基线

- 课程编辑基本信息已拆分为课程内容和学习设置：`courseBasics()`提供页面表单，`mountEditorGroups()`调用共享多选，群组互斥处理在业务层且不重绘整页。必修用单选，精选用共享开关皮肤；不挂筛选栏或弹窗外壳。测试`scripts/test-learning-editor-browser.cjs`，回退位置见当前未发布变更。
- 课节编辑由`courseLessons/courseLessonEditor/lessonFilePicker`生成，顶部集中名称、状态和排序/删除；类型为视频、音频、图文PDF单选，内容与讲义分区，底部保存/上下架。单选使用本页共用样式，不复制全局筛选或弹窗外壳；类型切换清空不兼容文件，锁定课节禁止改类型/内容，讲义仍可更新。保留`data-edit-lesson/data-drag/data-lc`与独立排序保存契约，编辑输入实时更新标题。小宽度分组换行，原业务限制不变。
- `keepEditorView`保留编辑滚动容器、父滚动位置和焦点；单节类型/文件更新使用`refreshLesson`，顺序更新使用`syncLessonOrder`复用原卡片。拖动手柄使用Pointer Events及整卡不可交互副本，原位占位、落点线、边缘滚动、取消/销毁清理均由本页控制。排序不自动落盘；上移/下移仍可键盘操作。
- 删除课节源场景为`lesson-delete`，通过`confirmScene(scene, callback, null, lessonName)`复用全局危险确认；确认只修改编辑数组，保存课程后生效，取消/X/Esc保持原数据。45号`learning-lesson-delete-confirm`预览传示例名称及无写入回调，保留已发布课节删除保护。

- 已补齐测试环境管理流程，并按 2026-09-08 V1.1 ZIP 实现本地规则；见 [[docs/learning-center-v11-local|实现、口径与验收说明]]。本地未发布，不等于测试环境已更新。
- 用户明确：完成度分母为当前上架课节；下架课节保留历史，重上架后重新参与。草稿预览不写学习数据，已完成回看不清零。
- 新增课节默认下架、独立排序保存、非必修回拖/时长等演示假设在说明内单列，不冒充已评审原文。
- 本地全部 Mock；视频/音频模拟，PDF 使用原文件的四页渲染副本；真实上传内容与接口未接入。
- 资源统一登记 `shared/config/channels.js`，各 HTML 不再直连旧学习脚本。使用稳定 course/lesson ID，不得退回数组索引关联新数据。
- 视觉台账 `design-changes/learning-center.json`；自动回归 `scripts/test-learning-v11.cjs`。本轮 file 页面被工具访问策略拒绝，真实浏览器视觉待验收。
- 课程管理列表试版移除独立标题，顶部集中返回/日志/新增；`learning-app.js.syncBreadcrumb` 通过共享控制器设置“课程管理”，频道生命周期不得无条件清除当前视图的面包屑。其他视图与卸载清除，列表专项回归为 `scripts/test-learning-course-manage.cjs`。字段、排序和业务规则保留。
- 课程筛选使用共享 `__GAIP_FILTER_BAR__`，当前顺序为搜索/学习群组/课程状态/只看必修课。群组为共享多选：空数组不限，多项OR匹配课程群组配置，“全部用户”匹配all课程；重置清空，连续勾选不关闭面板。即时筛选只更新结果区并回第一页，卸载销毁实例；学情已按上节独立配置接入。规范见[[docs/filter-bar-spec]]。

- 课程管理表格已改为 `__GAIP_TABLE__` 实例，页面仅提供列/数据模板与业务动作；筛选更新同一实例并回第一页，视图重绘/退出销毁。页头日志与新增按钮使用共享工具栏皮肤；课程管理日志使用信息弹窗和共享表格，关闭保留底页状态；学情与学情日志后续已按上节接入，编辑页面不变。课程日志按产品文档展示八列，结构化记录实际字段差异，支持V1.1课节上下架；旧记录只读适配且不猜测缺失值。首版行为与验收见[[../../docs/table-spec]]，日志规则见[[../../docs/learning-center-v11-local#课程操作日志]]。

- 课程管理已按用户要求撤下创建日期筛选；没有高级字段，“更多筛选”自动隐藏，重置保留。共享日期组件及表格创建时间列不变。

- 课程管理的状态/精选标签已调用共享表格标签；卡片及详情仍使用原课程标签，接口见[[../../docs/table-spec#表格内标签]]。
