---
type: page
key: product
route: /product
entry: 产品中心.html
layout: catalog
---

# 产品中心

- 分类Tab已通过共享`global-tabs.js/css`增强接入，不重建原React按钮、不改计数与筛选事件；与组织架构/学情使用同一皮肤。规范及核验边界见[[../../docs/tabs-spec]]。
- 入口：`产品中心.html`
- Hash：`#/product`
- 页面类型：列表检索型
- 数据：`features/channel-data/mock-data.js`
- 详情 Mock：`shared/scripts/local-preview.js`，正式入口统一版本 `20260831-1`。
- 本轮已有详情样式位于 `web/p__dashboard__product__index.48332667.chunk.css`；发布时通过频道配置统一加载版本 `20260915-mb-plan-flag-1`，保证从其他入口切入时不会遗漏。
- 产品卡片机构 Logo 由 `shared/scripts/product-card-logo.js` 根据机构标签匹配 `web/logos/` 正式素材，`shared/styles/product-card-logo.css` 负责右上角 `88px × 72px` 槽位、内容避让及旧装饰隐藏；两个资源均登记在产品频道配置中，直接打开和跨入口 Hash 切换行为一致。视觉参数见 `design-changes/product-card-logo.json`。
- 本次清理了构建分包中此前注入的伪机构标识；后续新增产品专属改版仍应迁入 `features/product/` 或独立共享适配器，避免继续直接修改构建产物。
- 关联：[[../公共模块/本地Mock系统|本地 Mock 系统]]、[[../公共模块/主导航与Hash路由|主导航与 Hash 路由]]
- 规范：[[docs/channel-home-standard|频道首页规范]]

## 修改边界

频道专属改版放入 `features/product/` 或职责明确的共享适配器，并通过频道配置加载；`web/` 构建产物默认只读。
