---
title: 弹窗视觉规范与盘点
type: module
status: audit
updated: 2026-09-02
tags:
  - gaip
  - design-system
  - modal
aliases:
  - 弹窗视觉盘点
  - 弹窗标准化
related:
  - "[[全局框架与样式]]"
  - "[[../决策/ADR-004-弹窗源自登记与自动索引]]"
---

# 弹窗视觉规范与盘点

> [!important] 本轮边界
> 本页先建立视觉分类、问题清单和规范草案，不修改任何真实弹窗 CSS、DOM、数据或交互。后续必须按“基础层 → 类型层 → 个性层”逐层落地，不能用一组宽泛选择器强制覆盖全部弹窗。

## 盘点口径

- 事实源：`shared/scripts/modal-registry.js`、`全局组件/弹窗源登记.js`、`全局组件/弹窗自动索引.generated.js`。
- 当前基线：31 个 `ready` 且非 `drawer` 的真实弹窗；抽屉与 `excluded` 项不进入本轮。
- 预览原则：继续调用真实打开入口和真实样式，不在预览页复制第二套弹窗。
- 关联架构：[[../决策/ADR-004-弹窗源自登记与自动索引|弹窗源自登记与自动索引]]。

## 为什么现在看起来乱

当前弹窗不是同一个视觉系统下的不同实例，而是多套历史实现同时存在。源码中已出现以下明显分叉：

| 项目 | 当前差异 | 直接后果 |
|---|---|---|
| 标题 | 16 / 18 / 20 / 24px 并存 | 同层级弹窗视觉权重不一致 |
| 表单控件 | 36 / 40 / 44 / 48px 并存 | 同一页面不同弹窗密度跳变 |
| 内边距 | 16 / 20 / 24 / 28px 等多套组合 | 标题、正文、底部按钮无法对齐 |
| 圆角 | 2 / 4 / 5 / 6 / 8 / 12 / 20px 并存 | 控件和容器像来自不同产品 |
| 文字颜色 | `#111827`、`#253b39`、`#2f3640`、`#374151` 等并存 | 主次层级不稳定 |
| 底部操作区 | 有固定 footer、正文内按钮、超大留白等多种结构 | 扫读和操作预期不一致 |
| 滚动 | 整体滚动、正文滚动、表格内滚动并存 | 长内容下标题和操作按钮容易丢失 |

> [!note] 判断
> 乱的核心不是“每个弹窗都需要完全一样”，而是缺少共同基础层和明确的个性化边界。

## 标准化分层

| 层级 | 内容 | 处理原则 |
|---|---|---|
| L1 全局令牌 | 字体、字号阶梯、文字色、品牌色、边框色、圆角、阴影、间距刻度 | 必须统一 |
| L2 弹窗外壳 | 遮罩、容器、标题区、关闭按钮、正文区、底部操作区、滚动行为 | 原则统一，尺寸按类型变化 |
| L3 基础控件 | 输入框、选择器、日期、搜索、按钮、表格、空状态、错误提示 | 必须统一状态和尺寸规则 |
| L4 类型模板 | 确认、表单、详情、表格、流程、系统提示 | 同类统一布局，不跨类型硬套 |
| L5 业务内容 | 字段组合、营销图片、步骤、表格列、领域状态、特殊说明 | 保留个性化 |

## 31 个弹窗分类

### A. 确认类（4）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 05 | `proposal-unlink` | 方案解除关联确认 | 图标区、标题、说明、双按钮、危险操作色 | 解除关联的业务文案 |
| 12 | `config-delete` | 删除部门确认 | 确认模板、按钮顺序、危险态 | 部门名称与删除影响提示 |
| 24 | `customer-meeting-delete` | 删除沟通纪要确认 | 确认模板、关闭行为 | 纪要对象和提示文案 |
| 25 | `customer-delete` | 删除客户确认 | 确认模板、危险态、二次确认层级 | 客户删除后果说明 |

### B. 表单类（13）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 04 | `proposal-owner` | 方案客户归属 | 标签、输入/选择器、错误态、footer | 客户归属字段与检索逻辑 |
| 07 | `activity-signup` | 活动报名信息 | 表单网格、必填标记、按钮 | 活动报名字段及说明 |
| 08 | `wealth-keyword` | 保司关键词设置 | 标签、帮助文案、输入态 | 关键词标签编辑方式 |
| 10 | `config-member` | 配置中心成员编辑 | 控件高度、标签、分组、footer | 持牌地区、管理员等组织字段 |
| 11 | `config-department` | 部门名称编辑 | 单字段表单模板 | 部门命名规则 |
| 16 | `config-adjust-member-node` | 调整节点 | 选择器、校验、确认区 | 节点迁移与管理员提示 |
| 17 | `agent-session-rename` | 编辑对话名称 | 单字段表单模板 | 对话名称长度限制 |
| 18 | `clues-assign` | 分配线索 | 选择器、人员项、按钮 | 分配对象与线索状态逻辑 |
| 19 | `clues-terminal` | 标记转化 / 标记关闭 | 状态表单、原因输入、确认区 | 转化/关闭两种业务分支 |
| 21 | `clues-create` | 新增线索 | 表单栅格、标签、校验、footer | 线索字段和业务规则 |
| 22 | `customer-intro` | 编辑客户资料介绍 | 文本域、字数提示、footer | 客户介绍内容 |
| 23 | `customer-meeting` | 新增 / 编辑沟通纪要 | 表单网格、日期、文本域、footer | 沟通纪要字段与编辑态 |
| 29 | `product-expert` | 业务线对接专家 | 联系人卡片、选择/复制操作 | 专家联系方式与业务线信息 |

### C. 详情 / 媒体类（6）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 02 | `poster-share` | 文章海报分享 | 标题区、关闭、分栏间距、操作按钮 | 海报画布、二维码和分享能力 |
| 06 | `proposal-file` | 方案附件预览 | 查看器外壳、工具区、加载/错误态 | 文件类型与预览内容 |
| 09 | `news-article` | 资讯文章详情 | 标题、元信息、正文排版、关闭 | 文章内容和富文本结构 |
| 20 | `clues-detail` | 线索详情 | 信息分组、键值排版、状态标签 | 线索字段、时间线和操作 |
| 26 | `customer-proposal-detail` | 历史方案详情 | 信息区、附件区、状态展示 | 方案数据和业务动作 |
| 28 | `product-detail` | 产品详情 | 信息层级、页签/分组、底部操作 | 产品参数、标签与材料 |

### D. 表格 / 列表类（3）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 03 | `operation-log` | 操作日志 | 筛选区、表头、分页、滚动、密度 | 8 列审计字段与导出 |
| 14 | `config-organization-log` | 组织架构操作日志 | 表格弹窗外壳、筛选、分页、展开内容 | 7 列组织字段与独立 Mock |
| 27 | `activity-record` | 报名记录 | 表格密度、表头、分页、空状态 | 报名字段和活动状态 |

### E. 流程类（2）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 13 | `config-admin` | 设置管理员 | 标题区、搜索、列表项、footer | 节点/管理员选择与权限语义 |
| 15 | `config-bulk-import-members` | 批量导入成员 | 步骤头、上传区、错误摘要、footer | 模板下载、文件校验和导入结果 |

### F. 系统提示类（3）

| 序号 | ID | 弹窗 | 可统一 | 必须保留 |
|---:|---|---|---|---|
| 01 | `ai-notice` | AI 内容重要提示 | 提示图标、标题、说明、单按钮 | AI 风险提示文案 |
| 30 | `induction-complete` | 完成入职引导 | 成功态结构、主按钮 | 入职完成内容与后续入口 |
| 31 | `account-switch` | 检测到账号已切换 | 警示态结构、主操作 | 账号切换的会话处理逻辑 |

## 视觉基线草案（尚未应用）

### 字体与颜色

| 令牌 | 建议值 | 用途 |
|---|---|---|
| 标题 | 20px / 28px，500，`#2F3640` | 普通弹窗标题 |
| 正文 | 14px / 22px，400，`#2F3640` | 内容、表单值、按钮 |
| 标签 | 14px / 22px，500 或 400 | 表单标签，是否加粗按信息层级 |
| 辅助文字 | 12px / 18px，`rgba(47,54,64,.62)` | 描述、时间、提示 |
| 占位文字 | `rgba(47,54,64,.38)` | 输入和选择器 placeholder |
| 品牌色 | `#025B52` | 主按钮、选中、链接、焦点 |
| 危险色 | `#CF1322` | 删除与不可逆操作 |
| 边框色 | `rgba(47,54,64,.14)` | 容器与控件边框 |
| 遮罩 | `rgba(0,0,0,.45)` | 普通模态遮罩 |

### 尺寸与间距

| 项目 | 建议规则 |
|---|---|
| 间距刻度 | 4 / 8 / 12 / 16 / 24 / 32px；不再随意新增 18、22、28 等孤立值 |
| 弹窗圆角 | 8px；营销或系统提示若需更强品牌感，可登记为个性例外 |
| 控件圆角 | 4px；标签、状态胶囊另设规则 |
| 标准控件高度 | 40px；仅大型选择/上传流程显式使用 48px |
| 标准按钮高度 | 40px；确认类与表单类保持一致 |
| 标题区 | 左右 24px，上下 20px；关闭按钮与标题视觉中心对齐 |
| 正文区 | 24px；紧凑确认类可使用 20px 类型模板 |
| 底部区 | 左右 24px、上下 16px；操作按钮右对齐，按钮间距 12px |
| 滚动 | 标题区和 footer 固定，只有 body 滚动；表格类允许表格内部滚动 |

### 建议宽度档位

| 类型 | 建议宽度 |
|---|---|
| 确认 / 系统提示 | 420–520px |
| 单字段 / 轻量表单 | 520–640px |
| 标准表单 | 720–900px |
| 详情 / 流程 | 900–1100px |
| 表格 / 日志 | 1100–1360px，并受视口最大宽度约束 |

## 不应全局统一的内容

- 不把所有弹窗固定为同一宽高。
- 不统一业务字段数量、分组顺序、表格列或富文本结构。
- 不删除海报、文件预览、批量导入、权限选择等专属交互。
- 不强制系统提示、危险确认和长表单使用同一信息密度。
- 不用 `.ant-modal *`、`dialog *` 等宽泛选择器覆盖真实业务组件。
- 不在 `全局组件/弹窗预览.html` 维护一套“看起来统一”的复制样式。

## 建议实施顺序

1. **基础令牌**：建立弹窗专用 CSS 变量，但暂不改变业务结构。
2. **外壳统一**：标题区、关闭按钮、正文、footer、遮罩和滚动行为。
3. **确认与系统提示**：先处理结构最简单、风险最低的 7 个弹窗。
4. **表单控件**：统一输入、选择、日期、搜索、校验、按钮和间距。
5. **表格弹窗**：统一筛选、表头、行密度、分页和滚动。
6. **详情与流程**：只套基础层，逐项登记个性例外。
7. **回归**：刷新真实源预览，逐个核对 31 个弹窗，不在预览页补丁修正。

> [!warning] 下一阶段的实现边界
> 建议先选“确认类 4 个 + 系统提示类 3 个”作为第一批。它们能验证标题、正文、按钮、圆角、遮罩和 footer 六项基础规则，同时不会立即碰复杂表单和表格。

## 验收清单

- [ ] 31 个真实弹窗均能从预览打开，且没有复制 DOM/CSS。
- [ ] 同类标题、正文、控件、按钮和间距使用同一规范。
- [ ] 键盘焦点、Esc、遮罩关闭和焦点恢复未被视觉修改破坏。
- [ ] 小视口下宽度不溢出，长内容只滚动 body。
- [ ] 个性化项有明确根类或 `data-*` 变体，不依赖全局覆盖顺序。
- [ ] 源弹窗变化后刷新预览即可同步。

## 关联

- [[全局框架与样式]]
- [[../决策/ADR-004-弹窗源自登记与自动索引]]
- [[../变更/当前未发布变更#弹窗视觉盘点与标准化分层]]
